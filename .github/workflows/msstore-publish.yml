# Manual Microsoft Store submission fallback. Downloads the msix-package
# artifact from a given release.yml run and submits it to the Store listing,
# for when the publish-msstore job failed but the build legs succeeded.
name: Publish to Microsoft Store (manual)

on:
  workflow_dispatch:
    inputs:
      run_id:
        description: "release.yml run id to take the msix-package artifact from"
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      actions: read
    steps:
      - name: Download MSIX artifact from source run
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: msix-package
          path: msix
          run-id: ${{ inputs.run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Microsoft Store CLI
        uses: microsoft/microsoft-store-apppublisher@cc9910a8d59f2eb55cbb83df0a3800cf3b5300e0 # v1.4
        with:
          # msstore-cli 0.4.0 (native AOT rebuild) fails the Azure blob upload; unpin after this release
          version: v0.3.9

      # msstore-cli stores credentials via libsecret, which needs a running
      # Secret Service (gnome-keyring on a D-Bus session bus) that headless
      # runners lack, so reconfigure and publish run inside one keyring session.
      - name: Publish to Microsoft Store
        env:
          MSSTORE_TENANT_ID: ${{ secrets.MSSTORE_TENANT_ID }}
          MSSTORE_SELLER_ID: ${{ secrets.MSSTORE_SELLER_ID }}
          MSSTORE_CLIENT_ID: ${{ secrets.MSSTORE_CLIENT_ID }}
          MSSTORE_CLIENT_SECRET: ${{ secrets.MSSTORE_CLIENT_SECRET }}
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y libsecret-1-0 gnome-keyring dbus
          MSIX=$(find msix -name "*.msix")
          export MSIX
          dbus-run-session -- bash -euo pipefail -c '
            echo -n "ci-keyring" | gnome-keyring-daemon --unlock --components=secrets
            msstore reconfigure \
              --tenantId "$MSSTORE_TENANT_ID" \
              --sellerId "$MSSTORE_SELLER_ID" \
              --clientId "$MSSTORE_CLIENT_ID" \
              --clientSecret "$MSSTORE_CLIENT_SECRET"
            msstore publish "$MSIX" -id 9NN162Z0WXSR
          '
