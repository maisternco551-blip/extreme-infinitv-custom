---
import Layout from "@/layouts/Layout.astro"
import {
  IconRefresh,
  IconTrash,
  IconAlertTriangle,
  IconPlus,
  IconFolderOpen,
  IconCloudDownload,
  IconBrandGithub,
  IconDeviceFloppy,
  IconDownload,
  IconUpload,
  IconHeart,
  IconCoffee,
  IconBrandPaypal,
  IconBug,
  IconMail,
  IconChevronDown,
  IconWorld,
  IconPalette,
  IconLayoutSidebar,
  IconWifi,
  IconFilter,
  IconStar,
  IconPencil,
  IconArchive,
  IconFileZip,
  IconDatabase,
  IconBrandDiscord,
  IconInfoCircle,
  IconSparkles,
  IconBolt,
  IconPlayerPlay,
  IconBook,
  IconRocket,
  IconLayoutDashboard,
  IconSearch,
  IconX,
  IconShare,
  IconExternalLink,
  IconRestore,
  IconLicense,
  IconMovie,
  IconEye,
  IconEyeOff,
  IconActivity,
  IconDeviceTv,
  IconCast,
  IconArrowsShuffle,
} from "@tabler/icons-svelte"
import HiddenCategoriesEditor from "@/components/HiddenCategoriesEditor.svelte"
import LocalePicker from "@/components/LocalePicker.svelte"
import FavoritesReorder from "@/components/FavoritesReorder.svelte"
import ChannelOverridesEditor from "@/components/ChannelOverridesEditor.svelte"
import HubStripsEditor from "@/components/HubStripsEditor.svelte"
import PlayerPicker from "@/components/PlayerPicker.svelte"
import TvOverscanCard from "@/components/TvOverscanCard.svelte"
import CloseToTrayCard from "@/components/CloseToTrayCard.svelte"
import ConnectionLimitBanner from "@/components/ConnectionLimitBanner.svelte"
import AndroidNativePlayerCard from "@/components/AndroidNativePlayerCard.svelte"
import RememberedAndroidPlayerCard from "@/components/RememberedAndroidPlayerCard.svelte"

interface NavCard { id: string; labelKey: string; label: string; conditional?: boolean }
interface NavSection { id: string; labelKey: string; label: string; cards: NavCard[]; danger?: boolean }

const navStructure: NavSection[] = [
  { id: "settings-playlists", labelKey: "settings.group.playlists", label: "Playlists", cards: [] },
  { id: "settings-appearance", labelKey: "settings.group.appearance", label: "Appearance", cards: [
    { id: "card-display",    labelKey: "settings.section.display",     label: "Display" },
    { id: "card-behavior",   labelKey: "settings.appearance.behavior", label: "Behavior" },
    { id: "card-hub-strips", labelKey: "settings.hubStrips.title",     label: "Home page sections" },
  ]},
  { id: "settings-watching", labelKey: "settings.group.watching", label: "Watching", cards: [
    { id: "card-livetv",      labelKey: "nav.livetv",             label: "Live TV" },
    { id: "playback-section", labelKey: "settings.playback.title", label: "Playback" },
  ]},
  { id: "settings-network", labelKey: "settings.group.network", label: "Network", cards: [
    { id: "card-network",          labelKey: "settings.network.title", label: "Network" },
    { id: "discord-rpc-section",   labelKey: "settings.discord.title", label: "Discord Rich Presence", conditional: true },
    { id: "card-casting",          labelKey: "settings.casting.title", label: "Casting", conditional: true },
    { id: "card-tmdb",             labelKey: "settings.tmdb.title",    label: "Metadata enrichment" },
    { id: "card-net-log",          labelKey: "settings.netLog.title",  label: "Network log", conditional: true },
  ]},
  { id: "settings-library", labelKey: "settings.group.library", label: "Library", cards: [
    { id: "card-categories", labelKey: "settings.categories.title",        label: "Categories" },
    { id: "card-favorites",  labelKey: "settings.favoritesSection.title",  label: "Favorites" },
  ]},
  { id: "settings-data", labelKey: "settings.group.data", label: "Data", cards: [
    { id: "downloads-folder-section", labelKey: "settings.downloadsSection.title", label: "Downloads" },
    { id: "card-backup",              labelKey: "settings.backup.title",           label: "Backup" },
    { id: "card-storage",             labelKey: "settings.storage.title",          label: "Storage" },
  ]},
  { id: "settings-about", labelKey: "settings.group.about", label: "About", cards: [
    { id: "update-section",    labelKey: "settings.nav.appUpdate",  label: "App update" },
    { id: "changelog-section", labelKey: "settings.about.whatsNew", label: "What's new" },
    { id: "licenses-section",  labelKey: "settings.licenses.title", label: "Licenses and attribution" },
  ]},
  { id: "settings-help", labelKey: "settings.group.help", label: "Help & feedback", cards: [
    { id: "card-docs",        labelKey: "settings.help.docsTitle",  label: "Read the docs" },
    { id: "card-feedback",    labelKey: "settings.nav.feedback",    label: "Send feedback" },
    { id: "settings-support", labelKey: "settings.group.support",   label: "Support" },
  ]},
  { id: "settings-danger",  labelKey: "settings.group.dangerZone", label: "Danger zone", cards: [], danger: true },
]
---

<Layout pageTitle="Settings" showVersion={false}>
  <main class="flex-1 min-h-0 overflow-y-auto overflow-x-clip custom-scroll">
    <div
      class="w-full max-w-480 mx-auto px-4 md:px-6 lg:px-8 pt-4 lg:pt-6 pb-12 flex flex-col gap-6 lg:gap-8">

    <header class="route-hero shrink-0">
      <h1 data-i18n="settings.title" class="route-hero__title">Settings</h1>
      <p data-i18n="page.settings.sub" class="route-hero__sub">
        Playlists, preferences, cache, and the app itself.
      </p>
    </header>

    <div class="flex flex-col gap-6 lg:flex-row lg:items-start lg:gap-10">

    <div class="settings-nav-scroller -mx-4 sm:-mx-6 lg:m-0 lg:contents">
    <nav
      aria-label="Settings sections"
      data-i18n-attr="aria-label:settings.nav.aria"
      class="settings-nav
        flex flex-row gap-1 px-4 sm:px-6 w-max
        lg:flex-col lg:gap-0.5 lg:px-0 lg:py-1 lg:w-52
        lg:sticky lg:top-6 lg:self-start">
      {navStructure.map((section) => (
        <Fragment>
          <a
            href={`#${section.id}`}
            data-i18n={section.labelKey}
            class="settings-nav-link"
            {...(section.danger ? { "data-danger": "true" } : {})}>
            {section.label}
          </a>
          {section.cards.length > 1 && (
            <ul class="settings-subnav hidden lg:flex flex-col list-none m-0 p-0">
              {section.cards.map((card) => (
                <li class="m-0" {...(card.conditional ? { "data-conditional": "true", hidden: true } : {})}>
                  <a
                    href={`#${card.id}`}
                    data-i18n={card.labelKey}
                    class="settings-subnav-link">
                    {card.label}
                  </a>
                </li>
              ))}
            </ul>
          )}
        </Fragment>
      ))}
    </nav>
    </div>

    <div class="settings-stack flex-1 min-w-0">

    <div class="lg:sticky lg:top-0 lg:z-10 -mt-2 pt-2 pb-3">
      <div class="relative">
        <span class="absolute left-3 top-1/2 -translate-y-1/2 text-fg-3 pointer-events-none inline-flex">
          <IconSearch aria-hidden="true" stroke={2} size={16} />
        </span>
        <input
          id="settings-search-input"
          type="search"
          autocomplete="off"
          autocapitalize="off"
          autocorrect="off"
          spellcheck="false"
          enterkeyhint="search"
          data-i18n-attr="placeholder:settings.search.placeholder;aria-label:settings.search.placeholder"
          placeholder="Search settings"
          class="w-full h-11 rounded-xl border border-accent bg-surface text-fg
            pl-10 pr-11 text-sm placeholder:text-fg-3
            focus-visible:border-accent outline-none
            transition-colors"
        />
        <button
          id="settings-search-clear"
          type="button"
          data-i18n-attr="aria-label:settings.search.clear"
          aria-label="Clear search"
          class="absolute right-1 top-1/2 -translate-y-1/2 size-9 rounded-md
            text-fg-3 hover:text-fg hover:bg-surface-2
            focus-visible:text-fg focus-visible:bg-surface-2
            inline-flex items-center justify-center transition-colors"
          hidden>
          <IconX aria-hidden="true" stroke={2} size={16} />
        </button>
      </div>
      <p
        id="settings-search-empty"
        data-i18n="settings.search.noMatches"
        class="mt-3 text-sm text-fg-3 px-1"
        role="status"
        aria-live="polite"
        hidden>
        No settings match your search.
      </p>
    </div>

    <!-- Group: Playlists -->
    <section id="settings-playlists" class="settings-group scroll-mt-8 lg:scroll-mt-20">
      <h2 data-i18n="settings.group.playlists" class="settings-group__title">Playlists</h2>
      <div class="rounded-2xl border border-line bg-surface p-5 sm:p-6 flex flex-col gap-4">
        <div
          id="settings-playlists-list"
          class="flex flex-col divide-y divide-line/60 rounded-xl border border-line bg-bg overflow-hidden">
          <div data-i18n="settings.playlists.empty" class="px-4 py-4 text-xs text-fg-3">No playlists yet.</div>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 pt-1">
          <a href="/login" class="btn justify-start sm:justify-center">
            <IconPlus aria-hidden="true" class="btn-icon" />
            <span data-i18n="playlist.add">Add playlist</span>
          </a>
          <button
            id="settings-refresh-active"
            type="button"
            class="btn justify-start sm:justify-center hidden">
            <IconRefresh aria-hidden="true" class="btn-icon" />
            <span data-i18n="playlist.refreshActive">Refresh active</span>
          </button>
          <button
            id="settings-add-from-website"
            type="button"
            class="btn justify-start sm:justify-center hidden">
            <IconWorld aria-hidden="true" class="btn-icon" />
            <span data-i18n="settings.playlists.addFromWebsite">Add from website</span>
          </button>
          <button
            id="settings-new-custom-playlist"
            type="button"
            class="btn justify-start sm:justify-center">
            <IconPlus aria-hidden="true" class="btn-icon" />
            <span data-i18n="editor.newCustomPlaylistBtn">New custom playlist</span>
          </button>
        </div>
      </div>
    </section>

    <!-- New custom playlist dialog -->
    <dialog
      id="settings-new-custom-playlist-dialog"
      aria-labelledby="settings-new-custom-playlist-title"
      class="fixed inset-0 m-auto rounded-2xl border border-line bg-surface text-fg p-0
             w-[min(24rem,calc(100vw-2rem))] backdrop:bg-black/60">
      <form method="dialog" class="flex flex-col gap-4 p-5">
        <h2 id="settings-new-custom-playlist-title" data-i18n="editor.newPlaylistDialogTitle" class="text-base font-semibold">
          New custom playlist
        </h2>
        <label class="flex flex-col gap-1.5">
          <span data-i18n="editor.newPlaylistNameLabel" class="text-xs font-semibold uppercase tracking-wider text-fg-3">
            Playlist name
          </span>
          <input
            id="settings-new-custom-playlist-name"
            type="text"
            class="field-input"
            data-i18n-attr="placeholder:editor.newPlaylistNamePlaceholder"
            placeholder="My playlist"
          />
        </label>
        <div class="flex gap-2 justify-end">
          <button type="button" data-role="cancel" class="btn">
            <span data-i18n="common.cancel">Cancel</span>
          </button>
          <button type="submit" data-role="create" class="btn-primary">
            <span data-i18n="editor.newPlaylistCreate">Create</span>
          </button>
        </div>
      </form>
    </dialog>

    <!-- Group: Appearance (language, theme/font, perf mode, home page sections, close behavior) -->
    <section id="settings-appearance" class="settings-group scroll-mt-8 lg:scroll-mt-20">
      <h2 data-i18n="settings.group.appearance" class="settings-group__title">Appearance</h2>
      <div class="flex flex-col gap-4">

        <!-- Surface: Display -->
        <div id="card-display" class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconPalette aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.section.display" class="text-base font-semibold">Display</h3>
          </div>
          <div class="settings-rows">

            <!-- Language -->
            <div id="card-language" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.section.language" class="text-sm font-medium leading-snug">Language</h3>
                <p data-i18n="settings.language.cardHelper" class="text-xs text-fg-3">
                  Pick a language for the interface. Translations are community-driven - missing strings fall back to English.
                </p>
              </div>
              <div class="settings-row__control w-full @2xl:w-64">
                <LocalePicker client:load />
              </div>
            </div>

            <!-- Theme + accent + font scale -->
            <div data-settings-item class="scroll-mt-6 lg:scroll-mt-20 settings-rows">
              <div class="settings-row">
                <span data-i18n="settings.theme.label" class="text-sm font-medium leading-snug">Theme</span>
                <div class="settings-row__control">
                  <div
                    id="settings-theme"
                    class="settings-seg"
                    role="radiogroup"
                    data-i18n-attr="aria-label:settings.theme.label"
                    aria-label="Theme">
                    <button type="button" role="radio" data-theme="system" aria-checked="false" data-i18n="settings.theme.system" class="theme-btn btn">System</button>
                    <button type="button" role="radio" data-theme="light"  aria-checked="false" data-i18n="settings.theme.light" class="theme-btn btn">Light</button>
                    <button type="button" role="radio" data-theme="dark"   aria-checked="false" data-i18n="settings.theme.dark" class="theme-btn btn">Dark</button>
                  </div>
                </div>
              </div>

              <div class="settings-row">
                <div class="settings-row__text">
                  <span data-i18n="settings.accent.label" class="text-sm font-medium leading-snug">Accent color</span>
                </div>
                <div class="settings-row__control">
                <div class="flex flex-col items-end gap-2">
                  <div
                    id="settings-accent"
                    class="flex flex-wrap justify-end gap-2"
                    role="radiogroup"
                    data-i18n-attr="aria-label:settings.accent.label"
                    aria-label="Accent color">
                    <button type="button" role="radio" data-accent-value="fuchsia" aria-checked="false" data-i18n-attr="title:settings.accent.fuchsia;aria-label:settings.accent.fuchsia" title="Fuchsia" aria-label="Fuchsia" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-fuchsia)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="rose" aria-checked="false" data-i18n-attr="title:settings.accent.rose;aria-label:settings.accent.rose" title="Rose" aria-label="Rose" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-rose)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="ember" aria-checked="false" data-i18n-attr="title:settings.accent.ember;aria-label:settings.accent.ember" title="Ember" aria-label="Ember" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-ember)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="emerald" aria-checked="false" data-i18n-attr="title:settings.accent.emerald;aria-label:settings.accent.emerald" title="Emerald" aria-label="Emerald" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-emerald)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="cyan" aria-checked="false" data-i18n-attr="title:settings.accent.cyan;aria-label:settings.accent.cyan" title="Cyan" aria-label="Cyan" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-cyan)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="blue" aria-checked="false" data-i18n-attr="title:settings.accent.blue;aria-label:settings.accent.blue" title="Blue" aria-label="Blue" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-blue)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="violet" aria-checked="false" data-i18n-attr="title:settings.accent.violet;aria-label:settings.accent.violet" title="Violet" aria-label="Violet" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-violet)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="gold" aria-checked="false" data-i18n-attr="title:settings.accent.gold;aria-label:settings.accent.gold" title="Gold" aria-label="Gold" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-gold)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="lime" aria-checked="false" data-i18n-attr="title:settings.accent.lime;aria-label:settings.accent.lime" title="Lime" aria-label="Lime" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-lime)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="teal" aria-checked="false" data-i18n-attr="title:settings.accent.teal;aria-label:settings.accent.teal" title="Teal" aria-label="Teal" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-teal)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="silver" aria-checked="false" data-i18n-attr="title:settings.accent.silver;aria-label:settings.accent.silver" title="Silver" aria-label="Silver" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-silver)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="white" aria-checked="false" data-i18n-attr="title:settings.accent.white;aria-label:settings.accent.white" title="White" aria-label="White" class="accent-btn btn group size-11 p-0" style="--swatch: var(--accent-swatch-white)">
                      <span class="size-5 rounded-full bg-(--swatch) group-aria-checked:ring-2 group-aria-checked:ring-(--swatch) group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                    </button>
                    <button type="button" role="radio" data-accent-value="random" aria-checked="false" data-i18n-attr="title:settings.accent.random;aria-label:settings.accent.random" title="Random" aria-label="Random" class="accent-btn btn group relative size-11 p-0">
                      <span class="size-5 rounded-full bg-[conic-gradient(red,orange,yellow,green,cyan,blue,magenta,red)] group-aria-checked:ring-2 group-aria-checked:ring-fg/50 group-aria-checked:ring-offset-2 group-aria-checked:ring-offset-surface" aria-hidden="true"></span>
                      <IconArrowsShuffle aria-hidden="true" stroke={2.5} size={12} class="absolute inset-0 m-auto text-white mix-blend-difference pointer-events-none" />
                    </button>
                  </div>
                  <p id="settings-accent-override-hint" data-i18n="settings.accent.overrideHint" class="hidden text-xs text-fg-3 text-right">
                    The active playlist overrides this color.
                  </p>
                </div>
                </div>
              </div>

              <div class="settings-row">
                <div class="settings-row__text">
                  <span data-i18n="settings.fontScale.label" class="text-sm font-medium leading-snug">Font scale</span>
                  <p data-i18n="settings.fontScale.helper" class="text-xs text-fg-3">
                    Bumps the base font size. Use Large or X-Large when viewing on a TV from across the room.
                  </p>
                </div>
                <div class="settings-row__control">
                  <div
                    id="settings-font-scale"
                    class="settings-seg"
                    role="radiogroup"
                    data-i18n-attr="aria-label:settings.fontScale.label"
                    aria-label="Font scale">
                    <button type="button" data-scale="0.875" aria-pressed="false" data-i18n="settings.fontScale.small" class="font-scale-btn btn">Small</button>
                    <button type="button" data-scale="1"     aria-pressed="false" data-i18n="settings.fontScale.default" class="font-scale-btn btn">Default</button>
                    <button type="button" data-scale="1.125" aria-pressed="false" data-i18n="settings.fontScale.medium" class="font-scale-btn btn">Medium</button>
                    <button type="button" data-scale="1.25"  aria-pressed="false" data-i18n="settings.fontScale.large" class="font-scale-btn btn">Large</button>
                    <button type="button" data-scale="1.5"   aria-pressed="false" data-i18n="settings.fontScale.xlarge" class="font-scale-btn btn">X-Large</button>
                  </div>
                </div>
              </div>

              <div class="settings-row">
                <div class="settings-row__text">
                  <span data-i18n="settings.density.label" class="text-sm font-medium leading-snug">Density</span>
                  <p data-i18n="settings.density.helper" class="text-xs text-fg-3">
                    Tightens or loosens spacing in lists and menus. Independent of font scale.
                  </p>
                </div>
                <div class="settings-row__control">
                  <div
                    id="settings-density"
                    class="settings-seg"
                    role="radiogroup"
                    data-i18n-attr="aria-label:settings.density.label"
                    aria-label="Density">
                    <button type="button" data-density="compact"     aria-pressed="false" data-i18n="settings.density.compact" class="density-btn btn">Compact</button>
                    <button type="button" data-density="cozy"        aria-pressed="false" data-i18n="settings.density.cozy" class="density-btn btn">Cozy</button>
                    <button type="button" data-density="comfortable" aria-pressed="false" data-i18n="settings.density.comfortable" class="density-btn btn">Comfortable</button>
                  </div>
                </div>
              </div>
              <script is:inline>
                try {
                  var savedFontScale = parseFloat(localStorage.getItem("xt_font_scale") || "1");
                  if (!isFinite(savedFontScale) || savedFontScale < 0.75 || savedFontScale > 2) savedFontScale = 1;
                  var fontScaleGroup = document.getElementById("settings-font-scale");
                  if (fontScaleGroup) {
                    var fontScaleBtns = fontScaleGroup.querySelectorAll(".font-scale-btn");
                    for (var i = 0; i < fontScaleBtns.length; i++) {
                      var btnScale = parseFloat(fontScaleBtns[i].getAttribute("data-scale") || "1");
                      fontScaleBtns[i].setAttribute("aria-pressed", Math.abs(btnScale - savedFontScale) < 0.001 ? "true" : "false");
                    }
                  }
                  var savedDensity = localStorage.getItem("xt_density") || "cozy";
                  if (savedDensity !== "compact" && savedDensity !== "cozy" && savedDensity !== "comfortable") savedDensity = "cozy";
                  var densityGroup = document.getElementById("settings-density");
                  if (densityGroup) {
                    var densityBtns = densityGroup.querySelectorAll(".density-btn");
                    for (var densityIndex = 0; densityIndex < densityBtns.length; densityIndex++) {
                      densityBtns[densityIndex].setAttribute("aria-pressed", densityBtns[densityIndex].getAttribute("data-density") === savedDensity ? "true" : "false");
                    }
                  }
                  var currentTheme = localStorage.getItem("xt_theme") || "system";
                  var themeGroup = document.getElementById("settings-theme");
                  if (themeGroup) {
                    var themeBtns = themeGroup.querySelectorAll(".theme-btn");
                    for (var j = 0; j < themeBtns.length; j++) {
                      themeBtns[j].setAttribute("aria-checked", themeBtns[j].getAttribute("data-theme") === currentTheme ? "true" : "false");
                    }
                  }
                  var currentAccent = localStorage.getItem("xt_accent") || "fuchsia";
                  var accentGroup = document.getElementById("settings-accent");
                  if (accentGroup) {
                    var accentBtns = accentGroup.querySelectorAll(".accent-btn");
                    for (var k = 0; k < accentBtns.length; k++) {
                      accentBtns[k].setAttribute("aria-checked", accentBtns[k].getAttribute("data-accent-value") === currentAccent ? "true" : "false");
                    }
                  }
                } catch (e) {}
              </script>
            </div>

            <!-- Performance mode: skip decorative animations + focus-glide for low-end TV WebViews -->
            <div id="card-perf-mode" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.perfMode.title" class="text-sm font-medium leading-snug">Performance mode</h3>
                <p data-i18n="settings.perfMode.helperLong" class="text-xs text-fg-3">
                  Disables decorative animations (the comet in the sidebar, hub entry stagger, sliding focus ring) for smoother scrolling and D-pad navigation on TV boxes and low-end devices.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-perf-mode"
                  class="settings-seg shrink-0"
                  role="radiogroup"
                  aria-label="Performance mode"
                  data-i18n-attr="aria-label:settings.perfMode.label">
                  <button type="button" data-perf="on"  aria-pressed="false" data-i18n="settings.perfMode.on"  class="perf-mode-btn btn">On</button>
                  <button type="button" data-perf="off" aria-pressed="false" data-i18n="settings.perfMode.off" class="perf-mode-btn btn">Off</button>
                </div>
              </div>
              <script is:inline>
                try {
                  var perfOn = localStorage.getItem("xt_perf_mode") === "1";
                  var perfGroup = document.getElementById("settings-perf-mode");
                  if (perfGroup) {
                    var perfBtns = perfGroup.querySelectorAll(".perf-mode-btn");
                    for (var pIdx = 0; pIdx < perfBtns.length; pIdx++) {
                      var perfBtnOn = perfBtns[pIdx].getAttribute("data-perf") === "on";
                      perfBtns[pIdx].setAttribute("aria-pressed", perfBtnOn === perfOn ? "true" : "false");
                    }
                  }
                } catch (e) {}
              </script>
            </div>

          </div>
        </div>

        <!-- Surface: Behavior -->
        <div id="card-behavior" class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconBolt aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.appearance.behavior" class="text-base font-semibold">Behavior</h3>
          </div>
          <div class="settings-rows">

            <!-- Interface sounds: launch chime + D-pad navigation ticks -->
            <div id="card-ui-sounds" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.uiSounds.title" class="text-sm font-medium leading-snug">Interface sounds</h3>
                <p data-i18n="settings.uiSounds.helperLong" class="text-xs text-fg-3">
                  A soft chime when the app starts and gentle ticks when moving around with a remote or keyboard. Mouse and touch stay silent.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-ui-sounds"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="Interface sounds"
                  data-i18n-attr="aria-label:settings.uiSounds.label">
                  <button type="button" data-sounds="on"  aria-pressed="false" data-i18n="settings.uiSounds.on"  class="ui-sounds-btn btn">On</button>
                  <button type="button" data-sounds="off" aria-pressed="false" data-i18n="settings.uiSounds.off" class="ui-sounds-btn btn">Off</button>
                </div>
              </div>
              <script is:inline>
                try {
                  var uiSoundsRaw = localStorage.getItem("xt_ui_sounds");
                  var uiSoundsOn =
                    uiSoundsRaw === "1" ||
                    (uiSoundsRaw !== "0" &&
                      !window.matchMedia("(prefers-reduced-motion: reduce)").matches);
                  var uiSoundsGroup = document.getElementById("settings-ui-sounds");
                  if (uiSoundsGroup) {
                    var uiSoundsBtns = uiSoundsGroup.querySelectorAll(".ui-sounds-btn");
                    for (var sIdx = 0; sIdx < uiSoundsBtns.length; sIdx++) {
                      var uiSoundsBtnOn = uiSoundsBtns[sIdx].getAttribute("data-sounds") === "on";
                      uiSoundsBtns[sIdx].setAttribute("aria-pressed", uiSoundsBtnOn === uiSoundsOn ? "true" : "false");
                    }
                  }
                } catch (e) {}
              </script>
            </div>

            <!-- Touch feedback: subtle vibration on favorite/watchlist touch actions -->
            <div id="card-haptics" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20 hidden">
              <div class="settings-row__text">
                <h3 data-i18n="settings.haptics.title" class="text-sm font-medium leading-snug">Touch feedback</h3>
                <p data-i18n="settings.haptics.helperLong" class="text-xs text-fg-3">
                  A tiny vibration when you star a favorite or add to your watchlist by touch. Follows the system touch-feedback setting.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-haptics"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="Touch feedback"
                  data-i18n-attr="aria-label:settings.haptics.label">
                  <button type="button" data-haptics="on"  aria-pressed="false" data-i18n="settings.haptics.on"  class="haptics-btn btn">On</button>
                  <button type="button" data-haptics="off" aria-pressed="false" data-i18n="settings.haptics.off" class="haptics-btn btn">Off</button>
                </div>
              </div>
              <script is:inline>
                try {
                  var hapticsOn = localStorage.getItem("xt_haptics") !== "0";
                  var hapticsGroup = document.getElementById("settings-haptics");
                  if (hapticsGroup) {
                    var hapticsBtns = hapticsGroup.querySelectorAll(".haptics-btn");
                    for (var hIdx = 0; hIdx < hapticsBtns.length; hIdx++) {
                      var hapticsBtnOn = hapticsBtns[hIdx].getAttribute("data-haptics") === "on";
                      hapticsBtns[hIdx].setAttribute("aria-pressed", hapticsBtnOn === hapticsOn ? "true" : "false");
                    }
                  }
                } catch (e) {}
              </script>
            </div>

            <!-- TV safe area -->
            <div id="card-tv-overscan" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.tvOverscan.title" class="text-sm font-medium leading-snug">TV safe area</h3>
                <p data-i18n="settings.tvOverscan.helperLong" class="text-xs text-fg-3">
                  Most TVs hide a small band at each edge of the picture (overscan). If sidebar icons or version text look cut off, increase this to inset the whole UI by a few percent.
                </p>
              </div>
              <div class="settings-row__control">
                <TvOverscanCard client:load />
              </div>
            </div>

            <CloseToTrayCard client:load />

            <!-- Rendering: Linux WebKitGTK DMA-BUF safe mode -->
            <details id="card-rendering" data-settings-item class="hidden group scroll-mt-6 lg:scroll-mt-20">
              <summary class="settings-row cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden">
                <span class="settings-row__text">
                  <h3 data-i18n="settings.rendering.title" class="text-sm font-medium leading-snug">Rendering</h3>
                  <span data-i18n="settings.rendering.helperLong" class="text-xs text-fg-3 block">
                    Some Linux graphics drivers (Raspberry Pi, Nvidia proprietary, virtual machines) show a garbled screen or crash on launch with the default renderer. Safe mode works around this at a small performance cost.
                  </span>
                  <span id="rendering-status" aria-live="polite" class="text-xs text-fg-3 block mt-1 empty:hidden"></span>
                </span>
                <span class="settings-row__control">
                  <IconChevronDown aria-hidden="true" stroke={2} class="size-4 text-fg-3 transition-transform group-open:rotate-180" />
                </span>
              </summary>
              <div class="px-5 sm:px-6 pb-4 pt-1 flex flex-col gap-3">
                <div class="flex flex-col gap-2">
                  <span data-i18n="settings.rendering.label" class="text-eyebrow font-medium uppercase text-fg-3">Rendering mode</span>
                  <select id="rendering-select" class="field-input" aria-describedby="rendering-hint">
                    <option value="auto" data-i18n="settings.rendering.auto">Auto (recommended)</option>
                    <option value="fast" data-i18n="settings.rendering.fast">Faster</option>
                    <option value="safe" data-i18n="settings.rendering.safe">Safe</option>
                  </select>
                </div>
                <p id="rendering-hint" data-i18n="settings.rendering.hint" class="text-xs text-fg-3">
                  Auto turns on safe mode only on hardware known to have problems. Faster is applied as a trial after a restart: if you do not confirm keeping it, the app switches back on the next launch.
                </p>

                <p id="rendering-env-override-note" hidden data-i18n="settings.rendering.envOverrideNote" class="text-xs text-fg-3">
                  A WebKit rendering variable is set outside the app, so this control is disabled.
                </p>

                <p id="rendering-restart-note" hidden aria-live="polite" class="text-xs text-fg-3"></p>
                <button id="rendering-restart" type="button" hidden class="btn justify-start sm:justify-center">
                  <span data-i18n="settings.about.restartNow">Restart now</span>
                </button>

                <p data-i18n-html="settings.rendering.escapeHatches" class="text-xs text-fg-3 border-t border-line/60 pt-3">
                  If the app becomes unusable, run it once with <code class="font-mono text-[0.7rem] px-1 py-0.5 rounded bg-surface-2 border border-line/60">--safe-rendering</code> from a terminal to switch back to safe rendering permanently.
                </p>
              </div>
            </details>

          </div>
        </div>

        <!-- Surface: Home page sections -->
        <div id="card-hub-strips" data-settings-item class="settings-surface scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconLayoutDashboard aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.hubStrips.title" class="text-base font-semibold">Home page sections</h3>
          </div>
          <p data-i18n="settings.hubStrips.helper" class="settings-surface__helper text-xs text-fg-3">
            Choose which sections appear on your home page and what order they're in.
          </p>
          <div class="px-5 sm:px-6 pb-5 sm:pb-6">
            <HubStripsEditor client:load />
          </div>
        </div>

      </div>
    </section>

    <!-- Group: Watching (Live TV layout, continue watching retention, EPG time zone, playback) -->
    <section id="settings-watching" class="settings-group scroll-mt-8 lg:scroll-mt-20">
      <h2 data-i18n="settings.group.watching" class="settings-group__title">Watching</h2>
      <div class="flex flex-col gap-4">

        <!-- Surface: Live TV -->
        <div id="card-livetv" class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconLayoutSidebar aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="nav.livetv" class="text-base font-semibold">Live TV</h3>
          </div>
          <div class="settings-rows">

            <!-- Live TV layout: EPG default + channel column width -->
            <div id="card-livetv-layout" data-settings-item class="scroll-mt-6 lg:scroll-mt-20 settings-rows">
              <div class="settings-row">
                <span data-i18n="settings.livetvLayout.epgDefault" class="text-sm font-medium leading-snug">Show EPG by default</span>
                <div class="settings-row__control">
                  <div
                    id="settings-epg-default"
                    class="settings-seg"
                    role="radiogroup"
                    aria-label="Show EPG by default" data-i18n-attr="aria-label:aria.showEpgDefault">
                    <button type="button" data-epg="on"  aria-pressed="false" data-i18n="settings.livetvLayout.epg.on" class="epg-default-btn btn">On</button>
                    <button type="button" data-epg="off" aria-pressed="false" data-i18n="settings.livetvLayout.epg.off" class="epg-default-btn btn">Off</button>
                  </div>
                </div>
              </div>

              <div class="settings-row">
                <span data-i18n="settings.livetvLayout.columnWidth" class="text-sm font-medium leading-snug">Channel column width</span>
                <div class="settings-row__control">
                  <div
                    id="settings-channel-width"
                    class="settings-seg"
                    role="radiogroup"
                    aria-label="Channel column width" data-i18n-attr="aria-label:aria.channelColumnWidth">
                    <button type="button" data-width="22" aria-pressed="false" data-i18n="settings.livetvLayout.width.compact" class="channel-width-btn btn">Compact</button>
                    <button type="button" data-width="28" aria-pressed="false" data-i18n="settings.livetvLayout.width.default" class="channel-width-btn btn">Default</button>
                    <button type="button" data-width="34" aria-pressed="false" data-i18n="settings.livetvLayout.width.wide" class="channel-width-btn btn">Wide</button>
                    <button type="button" data-width="42" aria-pressed="false" data-i18n="settings.livetvLayout.width.xWide" class="channel-width-btn btn">X-Wide</button>
                  </div>
                </div>
              </div>
            </div>

            <!-- EPG: time zone offset (per active playlist) -->
            <div id="epg-tz-section" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.epgTimezone.title" class="text-sm font-medium leading-snug">EPG time zone</h3>
                <p data-i18n="epg.timezoneHelper" class="text-xs text-fg-3">
                  Some providers ship XMLTV with timestamps already shifted to the server's local time, so EPG times look off. Auto-detect picks the offset where the most channels have a programme airing right now.
                </p>
              </div>
              <div class="settings-row__control w-full @2xl:w-64 flex-col items-stretch gap-1">
                <select id="epg-tz-select" disabled class="field-input" data-i18n-attr="aria-label:settings.epgTimezone.offsetLabel">
                  <option value="auto" data-i18n="epg.timezoneAutoDetect">Auto-detect</option>
                </select>
                <div
                  id="epg-tz-status"
                  aria-live="polite"
                  class="text-xs text-fg-3"></div>
              </div>
            </div>

            <!-- Continue watching: how long paused movies / episodes linger -->
            <div id="card-progress-retention" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.progressRetention.title" class="text-sm font-medium leading-snug">Continue watching</h3>
                <p data-i18n="settings.progressRetention.helper" class="text-xs text-fg-3">
                  How long to keep paused movies and episodes in Continue Watching. Older entries are dropped on the next save. The 200-item cap still applies regardless.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-progress-retention"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="Continue Watching retention"
                  data-i18n-attr="aria-label:settings.progressRetention.label">
                  <button type="button" data-retention="30"  aria-pressed="false" data-i18n="settings.progressRetention.30days"  class="retention-btn btn">30 days</button>
                  <button type="button" data-retention="90"  aria-pressed="false" data-i18n="settings.progressRetention.90days"  class="retention-btn btn">90 days</button>
                  <button type="button" data-retention="180" aria-pressed="false" data-i18n="settings.progressRetention.180days" class="retention-btn btn">180 days</button>
                  <button type="button" data-retention="0"   aria-pressed="false" data-i18n="settings.progressRetention.never"   class="retention-btn btn">Never</button>
                </div>
              </div>
            </div>

          </div>
        </div>

        <!-- Surface: Playback -->
        <div id="playback-section" data-settings-item class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconPlayerPlay aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.playback.title" class="text-base font-semibold">Playback</h3>
          </div>
          <div class="settings-rows">

            <div data-settings-item class="px-5 sm:px-6 py-4">
              <p data-i18n="settings.playback.helper" class="text-xs text-fg-3 mb-3">
                Choose how streams open. External players play 4K and HDR more reliably.
              </p>
              <PlayerPicker client:load />
            </div>

            <!-- Audio transcode auto-fix: desktop-only, unhidden by the script below -->
            <details id="audio-transcode-section" data-settings-item class="hidden group scroll-mt-6 lg:scroll-mt-20">
              <summary class="settings-row cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden">
                <span class="settings-row__text">
                  <h3 data-i18n="settings.playback.audioTranscodeAuto" class="text-sm font-medium leading-snug">Fix unsupported audio automatically</h3>
                  <span id="audio-transcode-helper" data-i18n="settings.playback.audioTranscodeAutoHelper" class="text-xs text-fg-3 block">
                    When a live channel plays video with no sound (AC-3, E-AC-3, MP2, or DTS audio), fix it automatically instead of asking each time.
                  </span>
                  <span id="ffmpeg-status" aria-live="polite" class="text-xs text-fg-3 block mt-1 empty:hidden"></span>
                </span>
                <span class="settings-row__control">
                  <IconChevronDown aria-hidden="true" stroke={2} class="size-4 text-fg-3 transition-transform group-open:rotate-180" />
                </span>
              </summary>
              <div class="px-5 sm:px-6 pb-4 pt-1 flex flex-col gap-3">
                <label for="ffmpeg-path-input" data-i18n="settings.playback.ffmpegPathLabel" class="text-eyebrow font-medium uppercase text-fg-3">FFmpeg path</label>
                <div class="flex flex-col gap-2 sm:flex-row sm:items-center">
                  <input
                    type="text"
                    id="ffmpeg-path-input"
                    spellcheck="false"
                    autocomplete="off"
                    placeholder="C:\ffmpeg\bin\ffmpeg.exe"
                    class="field-input font-mono flex-1"
                    aria-describedby="ffmpeg-path-helper" />
                  <button type="button" id="ffmpeg-browse-btn" class="btn" data-i18n="settings.playback.browse">Browse…</button>
                  <button type="button" id="ffmpeg-test-btn" class="btn" data-i18n="settings.playback.ffmpegTest">Test</button>
                </div>
                <p id="ffmpeg-path-helper" data-i18n="settings.playback.ffmpegPathHelper" class="text-xs text-fg-3">
                  Point to your own FFmpeg if the bundled one is missing or you prefer a specific build.
                </p>

                <div class="flex flex-wrap items-center gap-3 pt-3 border-t border-line/60">
                  <span data-i18n="settings.playback.audioTranscodeAuto" class="text-sm font-medium leading-snug me-auto">Fix unsupported audio automatically</span>
                  <div
                    id="settings-audio-transcode-auto"
                    class="settings-seg"
                    role="radiogroup"
                    aria-label="Fix unsupported audio automatically"
                    data-i18n-attr="aria-label:settings.playback.audioTranscodeAuto"
                    aria-describedby="audio-transcode-helper">
                    <button type="button" data-audio-transcode="on" aria-pressed="false" data-i18n="settings.playback.audioTranscodeOn" class="audio-transcode-btn btn">On</button>
                    <button type="button" data-audio-transcode="off" aria-pressed="false" data-i18n="settings.playback.audioTranscodeOff" class="audio-transcode-btn btn">Off</button>
                  </div>
                </div>
              </div>
            </details>

            <div id="card-lang-grouping" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.langGrouping.title" class="text-sm font-medium leading-snug">Group language variants</h3>
                <p data-i18n="settings.langGrouping.hint" class="text-xs text-fg-3">
                  Combine copies of the same title in different languages into one entry. Turn off if titles are grouped incorrectly for your provider.
                </p>
              </div>
              <div class="settings-row__control">
                <select id="lang-grouping-select" class="field-input">
                  <option value="on" data-i18n="settings.langGrouping.on">On</option>
                  <option value="off" data-i18n="settings.langGrouping.off">Off</option>
                </select>
              </div>
            </div>

            <div id="card-content-language" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.contentLang.title" class="text-sm font-medium leading-snug">Preferred audio language</h3>
                <p data-i18n="settings.contentLang.hint" class="text-xs text-fg-3">
                  When a title exists in several languages, open and recommend this one first. Auto follows the interface language.
                </p>
              </div>
              <div class="settings-row__control">
                <select id="content-lang-select" class="field-input"></select>
              </div>
            </div>

            <!-- HEVC extension install: Windows desktop only, unhidden by the script below -->
            <div id="card-hevc" data-settings-item class="settings-row hidden scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.playback.hevcTitle" class="text-sm font-medium leading-snug">HEVC video (H.265)</h3>
                <p data-i18n="settings.playback.hevcHelper" class="text-xs text-fg-3">Some channels use HEVC video, which Windows can't decode out of the box. Install Microsoft's HEVC extension to play them in the built-in player.</p>
              </div>
              <div class="settings-row__control flex-col items-end gap-1">
                <button id="settings-hevc-install" type="button" class="btn justify-center">
                  <IconCloudDownload aria-hidden="true" class="btn-icon" />
                  <span data-i18n="settings.playback.hevcInstall">Install</span>
                </button>
                <span id="settings-hevc-status" aria-live="polite" class="text-xs text-fg-3 empty:hidden"></span>
              </div>
            </div>

            <!-- Mono audio: downmixes embedded playback to one channel for single-earbud use -->
            <div id="card-mono-audio" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.monoAudio.title" class="text-sm font-medium leading-snug">Mono audio</h3>
                <p data-i18n="settings.monoAudio.helperLong" class="text-xs text-fg-3">
                  Play all audio in both ears as a single channel - useful with one earbud. Applies to streams played by the built-in engines; some directly played files keep their original stereo.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-mono-audio"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="Mono audio"
                  data-i18n-attr="aria-label:settings.monoAudio.label">
                  <button type="button" data-mono="on"  aria-pressed="false" data-i18n="settings.monoAudio.on"  class="mono-audio-btn btn">On</button>
                  <button type="button" data-mono="off" aria-pressed="false" data-i18n="settings.monoAudio.off" class="mono-audio-btn btn">Off</button>
                </div>
              </div>
              <script is:inline>
                try {
                  var monoAudioRaw = localStorage.getItem("xt_mono_audio");
                  var monoAudioOn = monoAudioRaw === "1";
                  var monoAudioGroup = document.getElementById("settings-mono-audio");
                  if (monoAudioGroup) {
                    var monoAudioBtns = monoAudioGroup.querySelectorAll(".mono-audio-btn");
                    for (var mIdx = 0; mIdx < monoAudioBtns.length; mIdx++) {
                      var monoAudioBtnOn = monoAudioBtns[mIdx].getAttribute("data-mono") === "on";
                      monoAudioBtns[mIdx].setAttribute("aria-pressed", monoAudioBtnOn === monoAudioOn ? "true" : "false");
                    }
                  }
                } catch (e) {}
              </script>
            </div>

            <!-- Captions auto: turns on subtitles at playback start when the title has them -->
            <div id="card-captions-auto" data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.captionsAuto.title" class="text-sm font-medium leading-snug">Captions on by default</h3>
                <p data-i18n="settings.captionsAuto.helperLong" class="text-xs text-fg-3">
                  Automatically turns on subtitles when a movie or episode has them, preferring your app language. You can still turn them off in the player. Applies to this device.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-captions-auto"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="Captions on by default"
                  data-i18n-attr="aria-label:settings.captionsAuto.label">
                  <button type="button" data-captions="on"  aria-pressed="false" data-i18n="settings.captionsAuto.on"  class="captions-auto-btn btn">On</button>
                  <button type="button" data-captions="off" aria-pressed="false" data-i18n="settings.captionsAuto.off" class="captions-auto-btn btn">Off</button>
                </div>
              </div>
              <script is:inline>
                try {
                  var captionsAutoRaw = localStorage.getItem("xt_captions_auto");
                  var captionsAutoOn = captionsAutoRaw === "1";
                  var captionsAutoGroup = document.getElementById("settings-captions-auto");
                  if (captionsAutoGroup) {
                    var captionsAutoBtns = captionsAutoGroup.querySelectorAll(".captions-auto-btn");
                    for (var cIdx = 0; cIdx < captionsAutoBtns.length; cIdx++) {
                      var captionsAutoBtnOn = captionsAutoBtns[cIdx].getAttribute("data-captions") === "on";
                      captionsAutoBtns[cIdx].setAttribute("aria-pressed", captionsAutoBtnOn === captionsAutoOn ? "true" : "false");
                    }
                  }
                } catch (e) {}
              </script>
            </div>

            <AndroidNativePlayerCard client:load />

            <RememberedAndroidPlayerCard client:load />

          </div>
        </div>

      </div>
    </section>

    <!-- Group: Network (User-Agent spoofing, Discord Rich Presence) -->
    <section id="settings-network" class="settings-group scroll-mt-8 lg:scroll-mt-20">
      <h2 data-i18n="settings.group.network" class="settings-group__title">Network</h2>
      <div class="flex flex-col gap-4">

        <!-- Surface: Network -->
        <div id="card-network" data-settings-item class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconWifi aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.network.title" class="text-base font-semibold">Network</h3>
          </div>
          <div class="settings-rows">

            <!-- User agent preset -->
            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.network.uaPreset" class="text-sm font-medium leading-snug">User-Agent preset</h3>
                <p data-i18n="settings.network.helperLong" class="text-xs text-fg-3">
                  Some IPTV providers reject the default browser User-Agent. Pick a preset or enter a custom string. Applies to provider API, M3U, and EPG fetches. Effective in the desktop build.
                </p>
              </div>
              <div class="settings-row__control w-full @2xl:w-72">
                <select
                  id="ua-preset"
                  class="field-input"
                  data-i18n-attr="aria-label:settings.network.uaPreset"
                  aria-label="User-Agent preset"></select>
              </div>
            </div>

            <!-- User agent custom override -->
            <div data-settings-item class="settings-row settings-row--stack scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.network.uaCustom" class="text-sm font-medium leading-snug">Custom (overrides preset)</h3>
              </div>
              <div class="settings-row__control w-full flex-col items-stretch gap-1">
                <div class="flex gap-2">
                  <input
                    id="ua-custom"
                    type="text"
                    autocomplete="off"
                    spellcheck="false"
                    placeholder="e.g. VLC/3.0.20 LibVLC/3.0.20"
                    data-i18n-attr="placeholder:settings.network.uaPlaceholder"
                    class="field-input font-mono flex-1 min-w-0"
                  />
                  <button id="ua-save" type="button" class="btn justify-center">
                    <IconDeviceFloppy aria-hidden="true" class="btn-icon" />
                    <span data-i18n="common.save">Save</span>
                  </button>
                </div>
                <span
                  id="ua-msg"
                  aria-live="polite"
                  class="text-xs text-fg-3 transition-opacity duration-300 empty:hidden"></span>
              </div>
            </div>

            <!-- Request timeout -->
            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.network.timeoutLabel" class="text-sm font-medium leading-snug">Request timeout</h3>
                <p id="settings-network-timeout-helper" data-i18n="settings.network.timeoutHelper" class="text-xs text-fg-3">
                  How long to wait for a provider response before giving up. Bump this when a large catalog (tens of thousands of movies / series) times out before it finishes loading.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-network-timeout"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="Request timeout"
                  aria-describedby="settings-network-timeout-helper"
                  data-i18n-attr="aria-label:settings.network.timeoutLabel">
                  <button type="button" role="radio" data-timeout="20"  aria-checked="false" data-i18n="settings.network.timeout.20s"  class="net-timeout-btn btn">20s</button>
                  <button type="button" role="radio" data-timeout="45"  aria-checked="false" data-i18n="settings.network.timeout.45s"  class="net-timeout-btn btn">45s</button>
                  <button type="button" role="radio" data-timeout="90"  aria-checked="false" data-i18n="settings.network.timeout.90s"  class="net-timeout-btn btn">90s</button>
                  <button type="button" role="radio" data-timeout="180" aria-checked="false" data-i18n="settings.network.timeout.180s" class="net-timeout-btn btn">3 min</button>
                </div>
              </div>
            </div>

            <!-- Developer mode: unlocks the network log card below -->
            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.devMode.title" class="text-sm font-medium leading-snug">Developer mode</h3>
                <p id="settings-dev-mode-helper" data-i18n="settings.devMode.helperLong" class="text-xs text-fg-3">
                  Unlocks a network log of recent provider requests, useful when troubleshooting a connection issue. Off by default.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-dev-mode"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="Developer mode"
                  aria-describedby="settings-dev-mode-helper"
                  data-i18n-attr="aria-label:settings.devMode.label">
                  <button type="button" role="radio" data-dev="on"  aria-checked="false" data-i18n="settings.devMode.on"  class="dev-mode-btn btn">On</button>
                  <button type="button" role="radio" data-dev="off" aria-checked="false" data-i18n="settings.devMode.off" class="dev-mode-btn btn">Off</button>
                </div>
              </div>
            </div>

            <!-- Discord Rich Presence: desktop-only, unhidden by the script below -->
            <details id="discord-rpc-section" data-settings-item class="hidden group scroll-mt-6 lg:scroll-mt-20">
              <summary class="settings-row cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden">
                <span class="settings-row__text">
                  <h3 class="text-sm font-medium leading-snug">
                    <IconBrandDiscord aria-hidden="true" stroke={2} class="size-4 text-fg-3 inline-block align-[-2px] me-1.5" />
                    <span data-i18n="settings.discord.title">Discord Rich Presence</span>
                  </h3>
                  <span data-i18n="settings.discord.helperLong" class="text-xs text-fg-3 line-clamp-1 block">
                    Show "Watching X on Extreme InfiniTV" on your Discord profile while you use the app. On by default; uncheck a playlist below to mute presence for that account. Desktop only - requires Discord running on the same machine.
                  </span>
                </span>
                <span class="settings-row__control">
                  <IconChevronDown aria-hidden="true" stroke={2} class="size-4 text-fg-3 transition-transform group-open:rotate-180" />
                </span>
              </summary>
              <div class="px-5 sm:px-6 pb-4 pt-1 flex flex-col gap-3">
                <input id="discord-client-id" type="hidden" />

                <div id="discord-controls" class="flex flex-col gap-2">
                  <span data-i18n="settings.discord.perPlaylist" class="text-eyebrow font-medium uppercase text-fg-3">Per-playlist</span>
                  <div
                    id="discord-playlists"
                    class="flex flex-col divide-y divide-line/60 rounded-xl border border-line bg-bg overflow-hidden">
                    <div data-i18n="settings.playlists.empty" class="px-4 py-4 text-xs text-fg-3">No playlists yet.</div>
                  </div>
                </div>
                <p id="discord-sandbox-note" data-i18n="settings.discord.sandboxNote" class="text-xs text-fg-3" hidden>
                  Discord Rich Presence isn't available in Snap installs.
                </p>
              </div>
            </details>

          </div>
        </div>

        <!-- Surface: Casting, unhidden by receiver-card.ts / play-on-tv-card.ts -->
        <div id="card-casting" data-settings-item class="settings-surface icon-mark-host hidden scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconCast aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.casting.title" class="text-base font-semibold">Casting</h3>
          </div>
          <div class="settings-rows">

            <!-- TV receiver: desktop-only, unhidden by receiver-card.ts -->
            <details id="card-tv-receiver" data-settings-item class="hidden group scroll-mt-6 lg:scroll-mt-20">
              <summary class="settings-row cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden">
                <span class="settings-row__text">
                  <h3 class="text-sm font-medium leading-snug">
                    <IconDeviceTv aria-hidden="true" stroke={2} class="size-4 text-fg-3 inline-block align-[-2px] me-1.5" />
                    <span data-i18n="settings.receiver.title">TV receiver</span>
                    <span id="receiver-live-badge" class="hidden ms-2 items-center gap-1.5 align-middle rounded-full bg-accent-soft px-2 py-0.5 text-2xs font-semibold text-accent">
                      <span class="size-1.5 rounded-full bg-ok"></span>
                      <span data-i18n="settings.receiver.on">On</span>
                    </span>
                  </h3>
                  <span id="receiver-summary-helper" data-i18n="settings.receiver.helperLong" class="text-xs text-fg-3 line-clamp-1 block">
                    Let other devices running Extreme InfiniTV send playback to this one over your local network.
                  </span>
                  <span id="receiver-summary-status" class="hidden text-xs text-fg-3 line-clamp-1 block"></span>
                </span>
                <span class="settings-row__control">
                  <IconChevronDown aria-hidden="true" stroke={2} class="size-4 text-fg-3 transition-transform group-open:rotate-180" />
                </span>
              </summary>
              <div class="px-5 sm:px-6 pb-4 pt-1 flex flex-col gap-3">

                <div data-settings-item class="settings-row">
                  <div class="settings-row__text">
                    <h3 data-i18n="settings.receiver.label" class="text-sm font-medium leading-snug">Allow casting to this device</h3>
                  </div>
                  <div class="settings-row__control">
                    <div
                      id="receiver-mode-toggle"
                      class="settings-seg"
                      role="radiogroup"
                      aria-label="Allow casting to this device"
                      data-i18n-attr="aria-label:settings.receiver.label">
                      <button type="button" role="radio" data-receiver="on"  aria-checked="false" data-i18n="settings.receiver.on"  class="receiver-mode-btn btn">On</button>
                      <button type="button" role="radio" data-receiver="off" aria-checked="false" data-i18n="settings.receiver.off" class="receiver-mode-btn btn">Off</button>
                    </div>
                  </div>
                </div>

                <div data-settings-item class="settings-row">
                  <div class="settings-row__text">
                    <h3 data-i18n="settings.receiver.deviceName" class="text-sm font-medium leading-snug">Device name</h3>
                    <p data-i18n="settings.receiver.deviceNameHelper" class="text-xs text-fg-3">Shown on the pairing screen and in other devices' cast menus.</p>
                  </div>
                  <div class="settings-row__control w-full @2xl:w-72">
                    <input
                      id="receiver-device-name-input"
                      type="text"
                      autocomplete="off"
                      spellcheck="false"
                      placeholder="This device's name"
                      data-i18n-attr="placeholder:settings.receiver.deviceNamePlaceholder"
                      class="field-input"
                    />
                  </div>
                </div>

                <div id="receiver-status-block" class="hidden flex-col gap-3">

                  <div data-settings-item class="settings-row settings-row--stack gap-1.5">
                    <p id="receiver-status-line" class="inline-flex items-center gap-2 text-sm text-fg">
                      <span class="size-2 rounded-full bg-ok"></span>
                      <span id="receiver-status-line-text"></span>
                    </p>
                    <p id="receiver-discoverable-line" class="hidden text-xs text-fg-3"></p>
                  </div>

                  <div id="receiver-firewall-warning" data-settings-item class="hidden flex-col gap-2 rounded-xl border border-warn/35 bg-warn/10 px-4 py-3">
                    <p class="inline-flex items-center gap-2 text-sm font-medium text-warn">
                      <IconAlertTriangle aria-hidden="true" stroke={2} class="size-4 flex-none" />
                      <span id="receiver-firewall-warning-title" data-i18n="settings.receiver.firewall.title">Windows Firewall may be blocking this</span>
                    </p>
                    <p id="receiver-firewall-warning-body" data-i18n="settings.receiver.firewall.body" class="text-xs text-warn/90">
                      Other devices on your network may not be able to reach this receiver. Add a firewall rule to allow it.
                    </p>
                    <button id="receiver-firewall-allow-btn" type="button" data-settings-item class="btn self-start">
                      <span data-i18n="settings.receiver.firewall.allow">Allow through firewall</span>
                    </button>
                  </div>

                  <div data-settings-item class="settings-row">
                    <div class="settings-row__text">
                      <h3 data-i18n="settings.receiver.addressLabel" class="text-sm font-medium leading-snug">Address</h3>
                    </div>
                    <div class="settings-row__control">
                      <div id="receiver-addresses-list" class="flex flex-wrap items-center justify-end gap-1.5"></div>
                    </div>
                  </div>

                  <div data-settings-item class="settings-row">
                    <div class="settings-row__text">
                      <h3 data-i18n="settings.receiver.code" class="text-sm font-medium leading-snug">Pair code</h3>
                    </div>
                    <div class="settings-row__control flex-wrap justify-end">
                      <div id="receiver-pair-code-display" class="text-3xl tracking-[0.3em] tabular-nums text-fg-3 font-semibold select-none"></div>
                      <button
                        id="receiver-code-reveal"
                        type="button"
                        class="btn flex-none w-11 justify-center p-0"
                        aria-label="Show pair code"
                        data-i18n-attr="aria-label:settings.receiver.showCode">
                        <IconEye aria-hidden="true" class="btn-icon" data-receiver-reveal-icon="show" />
                        <IconEyeOff aria-hidden="true" class="btn-icon hidden" data-receiver-reveal-icon="hide" />
                      </button>
                      <button id="receiver-regenerate-btn" type="button" data-settings-item class="btn">
                        <IconRefresh aria-hidden="true" class="btn-icon" />
                        <span data-i18n="settings.receiver.regenerate">Regenerate code</span>
                      </button>
                    </div>
                  </div>

                  <div data-settings-item class="settings-row settings-row--stack">
                    <div class="settings-row__text">
                      <h3 data-i18n="settings.receiver.pairedDevices" class="text-sm font-medium leading-snug">Paired devices</h3>
                    </div>
                    <div class="settings-row__control w-full flex-col items-stretch gap-1">
                      <div id="receiver-paired-list" class="flex flex-col divide-y divide-line/60 rounded-xl border border-line bg-bg overflow-hidden"></div>
                      <p id="receiver-no-devices" data-i18n="settings.receiver.noDevices" class="px-4 py-4 text-xs text-fg-3 hidden">No devices have paired yet. Pair one by choosing Play on TV on your phone or computer.</p>
                    </div>
                  </div>

                  <button id="receiver-open-screen" type="button" data-settings-item class="btn-primary self-end">
                    <IconExternalLink aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.receiver.openScreen">Open receiver screen</span>
                  </button>

                </div>

                <div data-settings-item class="settings-row mt-1 border-t border-line-soft pt-4">
                  <div class="settings-row__text">
                    <h3 data-i18n="settings.receiver.boot.title" class="text-sm font-medium leading-snug">Start on receiver screen</h3>
                    <p data-i18n="settings.receiver.boot.helper" class="text-xs text-fg-3">
                      Start the app on the pairing screen, like a dedicated receiver. Exit it anytime; it returns on the next launch.
                    </p>
                  </div>
                  <div class="settings-row__control">
                    <div
                      id="receiver-boot-toggle"
                      class="settings-seg"
                      role="radiogroup"
                      aria-label="Start on receiver screen"
                      data-i18n-attr="aria-label:settings.receiver.boot.label">
                      <button type="button" role="radio" data-receiver-boot="on"  aria-checked="false" data-i18n="settings.receiver.boot.on"  class="receiver-boot-btn btn">On</button>
                      <button type="button" role="radio" data-receiver-boot="off" aria-checked="false" data-i18n="settings.receiver.boot.off" class="receiver-boot-btn btn">Off</button>
                    </div>
                  </div>
                </div>

                <div id="settings-receiver-screensaver-hint" data-settings-item class="settings-row hidden">
                  <div class="settings-row__text">
                    <h3 data-i18n="settings.receiver.screensaver.title" class="text-sm font-medium leading-snug">Screensaver</h3>
                    <p data-i18n="settings.receiver.screensaver.helper" class="text-xs text-fg-3">
                      Select Extreme InfiniTV as the system screensaver to see artwork from your library while idle.
                    </p>
                  </div>
                  <div class="settings-row__control">
                    <button id="settings-receiver-screensaver-open" type="button" data-settings-item class="btn">
                      <span data-i18n="settings.receiver.screensaver.open">Open screensaver settings</span>
                    </button>
                  </div>
                </div>

              </div>
            </details>

            <!-- Play on TV: desktop + Android, unhidden by play-on-tv-card.ts -->
            <details id="card-play-on-tv" data-settings-item class="hidden group scroll-mt-6 lg:scroll-mt-20">
              <summary class="settings-row cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden">
                <span class="settings-row__text">
                  <h3 class="text-sm font-medium leading-snug">
                    <IconCast aria-hidden="true" stroke={2} class="size-4 text-fg-3 inline-block align-[-2px] me-1.5" />
                    <span data-i18n="settings.playOnTv.title">Play on TV</span>
                    <span id="play-on-tv-live-badge" class="hidden ms-2 items-center gap-1.5 align-middle rounded-full bg-accent-soft px-2 py-0.5 text-2xs font-semibold text-accent">
                      <span class="size-1.5 rounded-full bg-ok"></span>
                      <span data-i18n="settings.playOnTv.badgeCasting">Casting</span>
                    </span>
                  </h3>
                  <span id="play-on-tv-summary-helper" data-i18n="settings.playOnTv.helper" class="text-xs text-fg-3 line-clamp-1 block">
                    Pair TVs on your network. While casting, play actions go to the TV.
                  </span>
                  <span id="play-on-tv-summary-status" class="hidden text-xs text-fg-3 line-clamp-1 block"></span>
                </span>
                <span class="settings-row__control">
                  <IconChevronDown aria-hidden="true" stroke={2} class="size-4 text-fg-3 transition-transform group-open:rotate-180" />
                </span>
              </summary>
              <div class="px-5 sm:px-6 pb-4 pt-1 flex flex-col gap-3">

                <div id="play-on-tv-session" class="hidden items-center justify-between gap-3 rounded-xl border border-accent/40 bg-accent-soft px-4 py-3">
                  <div class="flex flex-col min-w-0">
                    <span id="play-on-tv-session-line" class="text-sm font-medium truncate"></span>
                    <span id="play-on-tv-session-title" class="text-xs text-fg-3 truncate"></span>
                  </div>
                  <button id="play-on-tv-stop" type="button" class="btn flex-none" data-i18n="settings.playOnTv.stopCasting">Stop casting</button>
                </div>

                <div data-settings-item class="settings-row settings-row--stack">
                  <div class="settings-row__text">
                    <h3 data-i18n="settings.playOnTv.devices" class="text-sm font-medium leading-snug">Paired TVs</h3>
                  </div>
                  <div class="settings-row__control w-full flex-col items-stretch gap-1">
                    <div id="play-on-tv-list" class="flex flex-col divide-y divide-line/60 rounded-xl border border-line bg-bg overflow-hidden"></div>
                    <p id="play-on-tv-empty" data-i18n="settings.playOnTv.noDevices" class="px-4 py-4 text-xs text-fg-3 hidden">No TVs paired yet. On the TV, open Settings, TV receiver and allow casting, then add it here.</p>
                  </div>
                </div>

                <button id="play-on-tv-add" type="button" data-settings-item class="btn-primary self-end">
                  <IconPlus aria-hidden="true" class="btn-icon" />
                  <span data-i18n="settings.playOnTv.addDevice">Add a TV</span>
                </button>

              </div>
            </details>

          </div>
        </div>

        <!-- Surface: TMDb metadata enrichment -->
        <div id="card-tmdb" data-settings-item class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconMovie aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.tmdb.title" class="text-base font-semibold">Metadata enrichment</h3>
          </div>
          <p data-i18n="settings.tmdb.helper" class="settings-surface__helper text-xs text-fg-3">
            TheTVDB works out of the box for posters, cast, and episode details. Add a TMDb key to also unlock trailers and similar titles.
          </p>
          <div class="settings-rows">

            <!-- TheTVDB enrichment: separate service, so it gets its own opt-out. -->
            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.tvdb.enable" class="text-sm font-medium leading-snug">TheTVDB metadata</h3>
                <p data-i18n="settings.tmdb.tvdbPrivacy" class="text-xs text-fg-3">
                  Titles you open are sent to TheTVDB through our proxy to look up artwork and episode details. No account or key is needed.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-tvdb-enabled"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="TheTVDB metadata"
                  data-i18n-attr="aria-label:settings.tvdb.enable">
                  <button type="button" data-tvdb="on"  aria-pressed="false" data-i18n="settings.tmdb.on"  class="tvdb-enabled-btn btn">On</button>
                  <button type="button" data-tvdb="off" aria-pressed="false" data-i18n="settings.tmdb.off" class="tvdb-enabled-btn btn">Off</button>
                </div>
              </div>
              <script is:inline>
                try {
                  var tvdbEnabledRaw = localStorage.getItem("xt_tvdb_enabled");
                  var tvdbEnabledOn = tvdbEnabledRaw !== "0";
                  var tvdbEnabledGroup = document.getElementById("settings-tvdb-enabled");
                  if (tvdbEnabledGroup) {
                    var tvdbEnabledBtns = tvdbEnabledGroup.querySelectorAll(".tvdb-enabled-btn");
                    for (var vIdx = 0; vIdx < tvdbEnabledBtns.length; vIdx++) {
                      var tvdbEnabledBtnOn = tvdbEnabledBtns[vIdx].getAttribute("data-tvdb") === "on";
                      tvdbEnabledBtns[vIdx].setAttribute("aria-pressed", tvdbEnabledBtnOn === tvdbEnabledOn ? "true" : "false");
                    }
                  }
                } catch (e) {}
              </script>
            </div>

            <!-- Enable enrichment -->
            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.tmdb.enable" class="text-sm font-medium leading-snug">Enable enrichment</h3>
                <p data-i18n="settings.tmdb.privacy" class="text-xs text-fg-3">
                  Optional: adds trailers, cast, and similar titles once you add a TMDB key below. Titles you open are sent to TMDB for matching.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-tmdb-enabled"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="Enable enrichment"
                  data-i18n-attr="aria-label:settings.tmdb.enable">
                  <button type="button" data-tmdb="on"  aria-pressed="false" data-i18n="settings.tmdb.on"  class="tmdb-enabled-btn btn">On</button>
                  <button type="button" data-tmdb="off" aria-pressed="false" data-i18n="settings.tmdb.off" class="tmdb-enabled-btn btn">Off</button>
                </div>
              </div>
              <script is:inline>
                try {
                  var tmdbEnabledRaw = localStorage.getItem("xt_tmdb_enabled");
                  var tmdbEnabledOn = tmdbEnabledRaw !== "0";
                  var tmdbEnabledGroup = document.getElementById("settings-tmdb-enabled");
                  if (tmdbEnabledGroup) {
                    var tmdbEnabledBtns = tmdbEnabledGroup.querySelectorAll(".tmdb-enabled-btn");
                    for (var tIdx = 0; tIdx < tmdbEnabledBtns.length; tIdx++) {
                      var tmdbEnabledBtnOn = tmdbEnabledBtns[tIdx].getAttribute("data-tmdb") === "on";
                      tmdbEnabledBtns[tIdx].setAttribute("aria-pressed", tmdbEnabledBtnOn === tmdbEnabledOn ? "true" : "false");
                    }
                  }
                } catch (e) {}
              </script>
            </div>

            <!-- API key -->
            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.tmdb.apiKey" class="text-sm font-medium leading-snug">API key</h3>
                <p class="text-xs text-fg-3">
                  <span data-i18n="settings.tmdb.apiKeyHelper">Paste a free TMDB API key or v4 access token.</span>
                  <a
                    href="https://www.themoviedb.org/settings/api"
                    target="_blank"
                    rel="noopener noreferrer"
                    data-external
                    data-i18n="settings.tmdb.getKey"
                    class="text-accent underline ms-1">Get a key at themoviedb.org</a>
                </p>
              </div>
              <div class="settings-row__control w-full @2xl:w-96 flex-col items-stretch gap-1">
                <div class="flex gap-2">
                  <input
                    id="settings-tmdb-key"
                    type="password"
                    autocomplete="off"
                    spellcheck="false"
                    class="field-input font-mono flex-1 min-w-0"
                  />
                  <button
                    id="settings-tmdb-reveal"
                    type="button"
                    class="btn flex-none w-11 justify-center p-0"
                    aria-label="Show key"
                    data-i18n-attr="aria-label:settings.tmdb.reveal">
                    <IconEye aria-hidden="true" class="btn-icon" data-tmdb-reveal-icon="show" />
                    <IconEyeOff aria-hidden="true" class="btn-icon hidden" data-tmdb-reveal-icon="hide" />
                  </button>
                  <button id="settings-tmdb-test" type="button" class="btn flex-none w-auto justify-center">
                    <span data-i18n="settings.tmdb.test">Test key</span>
                  </button>
                </div>
                <span
                  id="settings-tmdb-msg"
                  aria-live="polite"
                  class="text-xs text-fg-3 transition-opacity duration-300 empty:hidden"></span>
              </div>
            </div>

          </div>
          <p class="text-2xs text-fg-3 px-5 sm:px-6 pb-5 sm:pb-6 -mt-1">
            This product uses the TMDB API but is not endorsed or certified by TMDB.
          </p>
        </div>

        <!-- Network log: developer-mode only, unhidden by the script below -->
        <details id="card-net-log" data-settings-item class="changelog-outer settings-surface icon-mark-host group hidden scroll-mt-6 lg:scroll-mt-20">
          <summary class="settings-surface__header cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden pb-5 sm:pb-6">
            <span class="icon-mark">
              <IconActivity aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.netLog.title" class="text-base font-semibold">Network log</h3>
            <span class="ms-auto shrink-0">
              <IconChevronDown
                aria-hidden="true"
                stroke={1.75}
                class="size-4 text-fg-3 shrink-0 changelog-chevron transition-transform duration-200" />
            </span>
          </summary>
          <div class="flex flex-col gap-3 px-5 sm:px-6 pb-5 sm:pb-6 pt-4 sm:pt-5 border-t border-line/60">

            <p data-i18n="settings.netLog.helper" class="text-xs text-fg-3">
              Recent provider requests, with credentials stripped. Kept for this app session only and cleared when you close the app.
            </p>

            <div class="flex flex-wrap items-center gap-2">
              <button id="net-log-clear" type="button" class="btn">
                <IconTrash aria-hidden="true" class="btn-icon" />
                <span data-i18n="settings.netLog.clear">Clear</span>
              </button>
              <select
                id="net-log-kind-filter"
                class="field-input w-auto"
                data-i18n-attr="aria-label:settings.netLog.kindFilter"
                aria-label="Filter by kind">
                <option value="all" data-i18n="settings.netLog.kind.all">All kinds</option>
                <option value="api" data-i18n="settings.netLog.kind.api">API</option>
                <option value="playlist" data-i18n="settings.netLog.kind.playlist">Playlist</option>
                <option value="epg" data-i18n="settings.netLog.kind.epg">EPG</option>
                <option value="image" data-i18n="settings.netLog.kind.image">Image</option>
                <option value="media" data-i18n="settings.netLog.kind.media">Media</option>
                <option value="update" data-i18n="settings.netLog.kind.update">Update</option>
                <option value="other" data-i18n="settings.netLog.kind.other">Other</option>
              </select>
              <label class="inline-flex items-center gap-2 min-h-11 px-1 cursor-pointer">
                <input id="net-log-include-images" type="checkbox" class="size-4 accent-accent" />
                <span data-i18n="settings.netLog.includeImages" class="text-xs text-fg-2">Include image requests</span>
              </label>
              <button id="net-log-copy" type="button" class="btn ms-auto">
                <IconDeviceFloppy aria-hidden="true" class="btn-icon" />
                <span data-i18n="settings.netLog.copyJson">Copy as JSON</span>
              </button>
            </div>

            <div id="net-log-rows" class="flex flex-col divide-y divide-line/60 rounded-xl border border-line bg-bg overflow-hidden empty:hidden"></div>

            <div class="flex items-center justify-between gap-3 flex-wrap">
              <p id="net-log-count" class="text-2xs text-fg-3 tabular-nums"></p>
              <span
                id="net-log-msg"
                aria-live="polite"
                class="text-xs text-fg-3 transition-opacity duration-300 empty:hidden"></span>
            </div>

          </div>
        </details>

      </div>
    </section>

    <!-- Group: Library (per-playlist content curation: hidden categories, favorites order) -->
    <section id="settings-library" class="settings-group scroll-mt-8 lg:scroll-mt-20">
      <h2 data-i18n="settings.group.library" class="settings-group__title">Library</h2>
      <div class="flex flex-col gap-4">

        <!-- Hidden categories editor -->
        <details id="card-categories" data-settings-item class="settings-surface icon-mark-host group scroll-mt-6 lg:scroll-mt-20">
          <summary class="settings-surface__header flex-wrap pb-5 sm:pb-6 cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden">
            <span class="flex items-center gap-3 min-w-0 w-full">
              <span class="icon-mark">
                <IconFilter aria-hidden="true" stroke={2} />
              </span>
              <h3 data-i18n="settings.categories.title" class="text-base font-semibold">Categories</h3>
              <span class="ms-auto shrink-0">
                <IconChevronDown aria-hidden="true" stroke={2} class="size-4 text-fg-3 transition-transform group-open:rotate-180" />
              </span>
            </span>
            <span data-i18n="settings.categories.helperLong" class="text-xs text-fg-3 block mt-1.5">
              Per-playlist mute list. Hidden categories are hidden from
              the picker and the main grid on Live TV, Movies, and Series.
            </span>
          </summary>
          <div class="px-5 sm:px-6 pb-5 sm:pb-6 pt-1">
            <HiddenCategoriesEditor client:load />
          </div>
        </details>

        <!-- Channel overrides (rename / logo / number / hidden) -->
        <details id="card-channel-overrides" data-settings-item class="settings-surface icon-mark-host group scroll-mt-6 lg:scroll-mt-20">
          <summary class="settings-surface__header flex-wrap pb-5 sm:pb-6 cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden">
            <span class="flex items-center gap-3 min-w-0 w-full">
              <span class="icon-mark">
                <IconPencil aria-hidden="true" stroke={2} />
              </span>
              <h3 data-i18n="settings.channelOverrides.title" class="text-base font-semibold">Channel edits</h3>
              <span class="ms-auto shrink-0">
                <IconChevronDown aria-hidden="true" stroke={2} class="size-4 text-fg-3 transition-transform group-open:rotate-180" />
              </span>
            </span>
            <span data-i18n="settings.channelOverrides.helperLong" class="text-xs text-fg-3 block mt-1.5">
              Custom names, logos and numbers you set from a channel's right-click menu on Live TV,
              plus the channels you hid. Provider data is never changed, so any edit can be reset.
            </span>
          </summary>
          <div class="px-5 sm:px-6 pb-5 sm:pb-6 pt-1">
            <ChannelOverridesEditor client:load />
          </div>
        </details>

        <!-- Favorites order -->
        <details id="card-favorites" data-settings-item class="settings-surface icon-mark-host group scroll-mt-6 lg:scroll-mt-20">
          <summary class="settings-surface__header flex-wrap pb-5 sm:pb-6 cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden">
            <span class="flex items-center gap-3 min-w-0 w-full">
              <span class="icon-mark">
                <IconStar aria-hidden="true" stroke={2} />
              </span>
              <h3 data-i18n="settings.favoritesSection.title" class="text-base font-semibold">Favorites</h3>
              <span class="ms-auto shrink-0">
                <IconChevronDown aria-hidden="true" stroke={2} class="size-4 text-fg-3 transition-transform group-open:rotate-180" />
              </span>
            </span>
            <span data-i18n="settings.favoritesSection.helperLong" class="text-xs text-fg-3 block mt-1.5">
              Reorder your favorites per kind. The hub Favorites strip and
              the per-page favorites filter follow this order.
            </span>
          </summary>
          <div class="px-5 sm:px-6 pb-5 sm:pb-6 pt-1">
            <FavoritesReorder client:load />
          </div>
        </details>

      </div>
    </section>

    <!-- Group: Data (downloads folder, backup, cache storage) -->
    <section id="settings-data" class="settings-group scroll-mt-8 lg:scroll-mt-20">
      <h2 data-i18n="settings.group.data" class="settings-group__title">Data</h2>
      <div class="flex flex-col gap-4">

        <!-- Surface: Downloads (folder only - queue lives on /downloads) -->
        <div id="downloads-folder-section" data-settings-item class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconDownload aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.downloadsSection.title" class="text-base font-semibold">Downloads</h3>
            <span id="downloads-availability" class="ms-auto text-xs text-fg-3 shrink-0 truncate empty:hidden"></span>
          </div>
          <div class="px-5 sm:px-6 pt-3 empty:pt-0">
            <ConnectionLimitBanner client:load />
          </div>
          <div class="settings-rows">

            <!-- Save to -->
            <div data-settings-item class="settings-row settings-row--stack scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.downloadsFolder.savedTo" class="text-sm font-medium leading-snug">Save to</h3>
                <p data-i18n-html="settings.downloadsFolder.intro" class="text-xs text-fg-3">
                  Folder where new downloads land. Manage the queue itself on the
                  <a href="/downloads" class="text-accent underline">Downloads page</a>.
                </p>
              </div>
              <div class="settings-row__control w-full flex-col items-stretch">
                <div class="flex gap-2 flex-wrap">
                  <input
                    id="download-dir-input"
                    type="text"
                    readonly
                    placeholder="Not set - picked on first download"
                    data-i18n-attr="placeholder:settings.downloadsFolder.notSetPlaceholder;aria-label:settings.downloadsFolder.savedTo"
                    aria-label="Save to"
                    class="field-input flex-1 min-w-0 font-mono"
                  />
                  <button id="download-dir-pick" type="button" class="btn">
                    <IconFolderOpen aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.downloadsFolder.choose">Choose…</span>
                  </button>
                  <button id="download-dir-clear" type="button" class="btn text-fg-2">
                    <span data-i18n="common.clear">Clear</span>
                  </button>
                  <button id="settings-open-downloads" type="button" class="btn justify-center hidden">
                    <IconFolderOpen aria-hidden="true" class="btn-icon" />
                    <span data-i18n="downloads.action.openFolder">Open folder</span>
                  </button>
                </div>
              </div>
            </div>

            <!-- Restore from folder -->
            <div data-settings-item class="settings-row settings-row--stack scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.downloadsFolder.restoreLabel" class="text-sm font-medium leading-snug">Restore from folder</h3>
                <p data-i18n="settings.downloadsFolder.restoreHelper" class="text-xs text-fg-3">
                  After a reinstall, scan the folder above for `.xtmeta.json` sidecars and re-add the matching files to your downloads list.
                </p>
              </div>
              <div class="settings-row__control w-full flex-col items-stretch gap-1">
                <div class="flex">
                  <button id="downloads-scan" type="button" class="btn justify-start sm:justify-center">
                    <IconRefresh aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.downloadsFolder.scanFolder">Scan folder</span>
                  </button>
                </div>
                <span
                  id="downloads-scan-msg"
                  aria-live="polite"
                  class="text-xs text-fg-3 transition-opacity duration-300 empty:hidden"></span>
              </div>
            </div>

            <!-- Concurrent downloads -->
            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.downloadsFolder.concurrency" class="text-sm font-medium leading-snug">Concurrent downloads</h3>
                <p data-i18n="settings.downloadsFolder.concurrencyHelper" class="text-xs text-fg-3">
                  Most Xtream providers cap connections per account, so a second
                  parallel download fails mid-stream. Keep this at 1 unless your
                  provider explicitly allows multiple slots.
                </p>
              </div>
              <div class="settings-row__control flex-col items-end gap-1">
                <div
                  id="settings-download-concurrency"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="Concurrent downloads"
                  data-i18n-attr="aria-label:settings.downloadsFolder.concurrency">
                  <button type="button" data-concurrency="1" aria-pressed="false" class="dl-concurrency-btn btn">1</button>
                  <button type="button" data-concurrency="2" aria-pressed="false" class="dl-concurrency-btn btn">2</button>
                  <button type="button" data-concurrency="3" aria-pressed="false" class="dl-concurrency-btn btn">3</button>
                  <button type="button" data-concurrency="4" aria-pressed="false" class="dl-concurrency-btn btn">4</button>
                </div>
                <p
                  id="settings-download-concurrency-cap"
                  class="text-2xs text-fg-3 tabular-nums empty:hidden"
                  aria-live="polite"></p>
              </div>
            </div>

            <!-- Kodi .nfo sidecar: desktop only -->
            <div id="card-write-nfo" data-settings-item class="hidden settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.downloadsFolder.nfoTitle" class="text-sm font-medium leading-snug">Write Kodi metadata</h3>
                <p data-i18n="settings.downloadsFolder.nfoHelper" class="text-xs text-fg-3">
                  Save a .nfo file with title, year, plot and poster next to each finished download so Kodi, Jellyfin or Emby can index it.
                </p>
              </div>
              <div class="settings-row__control">
                <div
                  id="settings-write-nfo"
                  class="settings-seg"
                  role="radiogroup"
                  aria-label="Write Kodi metadata"
                  data-i18n-attr="aria-label:settings.downloadsFolder.nfoTitle">
                  <button type="button" data-nfo="on"  aria-pressed="false" data-i18n="settings.downloadsFolder.nfo.on"  class="write-nfo-btn btn">On</button>
                  <button type="button" data-nfo="off" aria-pressed="false" data-i18n="settings.downloadsFolder.nfo.off" class="write-nfo-btn btn">Off</button>
                </div>
              </div>
            </div>

          </div>
        </div>

        <!-- Surface: Backup / restore -->
        <div id="card-backup" data-settings-item class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconArchive aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.backup.title" class="text-base font-semibold">Backup</h3>
          </div>
          <div class="settings-rows">
            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <p data-i18n="settings.backup.helperLong" class="text-xs text-fg-3">
                  Save all playlists, favorites, recents, watched progress, hidden categories, and app settings to a file. Cache and the downloads queue are not exported.
                </p>
              </div>
              <div class="settings-row__control flex-col items-end gap-1">
                <div class="flex gap-2">
                  <button id="settings-export" type="button" class="btn justify-center">
                    <IconDownload aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.backup.export">Export to file</span>
                  </button>
                  <button id="settings-import" type="button" class="btn justify-center">
                    <IconUpload aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.backup.import">Import from file</span>
                  </button>
                </div>
                <input id="settings-import-input" type="file" accept="application/json,text/json,text/plain,.json" hidden />
                <span id="settings-backup-msg" aria-live="polite" class="text-xs text-fg-3 transition-opacity duration-300 empty:hidden"></span>
              </div>
            </div>
          </div>
        </div>

        <!-- Surface: Storage / cache -->
        <div id="card-storage" data-settings-item class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconDatabase aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.storage.title" class="text-base font-semibold">Storage</h3>
          </div>
          <div class="settings-rows">

            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.storage.size" class="text-sm font-medium leading-snug">Cache</h3>
              </div>
              <div class="settings-row__control flex-col items-end gap-0.5 text-sm">
                <span id="settings-cache-size" class="text-fg tabular-nums">-</span>
                <span class="text-xs text-fg-3">
                  <span data-i18n="settings.storage.lastUpdated">Last updated</span>
                  <span id="settings-cache-newest" class="tabular-nums ms-1">-</span>
                </span>
              </div>
            </div>

            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.storage.clear" class="text-sm font-medium leading-snug">Clear cache</h3>
                <p data-i18n="settings.storage.helperLong" class="text-xs text-fg-3">
                  Clears channel, movie, and series lists. Playlists, favorites, and recents are kept.
                </p>
              </div>
              <div class="settings-row__control flex-col items-end gap-1">
                <div class="flex flex-wrap gap-2 justify-end">
                  <button id="settings-clear-cache" type="button" class="btn justify-center">
                    <IconTrash aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.storage.clear">Clear cache</span>
                  </button>
                  <button id="settings-open-logs" type="button" class="btn justify-center hidden">
                    <IconFolderOpen aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.storage.openLogs">Open log folder</span>
                  </button>
                  <button id="settings-open-appdata" type="button" class="btn justify-center hidden">
                    <IconFolderOpen aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.storage.openAppData">Open app data folder</span>
                  </button>
                  <button id="settings-share-logs" type="button" class="btn justify-center hidden">
                    <IconShare aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.storage.shareLogs">Share logs</span>
                  </button>
                </div>
                <span
                  id="settings-cache-msg"
                  aria-live="polite"
                  class="text-xs text-fg-3 transition-opacity duration-300 empty:hidden"></span>
              </div>
            </div>

            <div data-settings-item class="settings-row scroll-mt-6 lg:scroll-mt-20">
              <div class="settings-row__text">
                <h3 data-i18n="settings.viewingHistory.title" class="text-sm font-medium leading-snug">Clear viewing history</h3>
                <p data-i18n="settings.viewingHistory.helper" class="text-xs text-fg-3">
                  Removes continue watching and recently watched items from every playlist. Favorites and watchlist are kept.
                </p>
              </div>
              <div class="settings-row__control">
                <button id="settings-clear-history" type="button" class="btn justify-center">
                  <IconTrash aria-hidden="true" class="btn-icon" />
                  <span data-i18n="settings.viewingHistory.clear">Clear history</span>
                </button>
              </div>
            </div>

          </div>
        </div>

      </div>
    </section>

    <!-- Group: About (App update + version + expiration) -->
    <section id="settings-about" class="settings-group scroll-mt-8 lg:scroll-mt-20">
      <h2 data-i18n="settings.group.about" class="settings-group__title">About</h2>
      <div class="flex flex-col gap-4">
        <div
          id="update-section"
          data-settings-item
          data-state="idle"
          class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20
                 data-[state=available]:border-accent/50 data-[state=available]:bg-accent-soft/30
                 transition-colors">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconInfoCircle aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.about.title" class="text-base font-semibold">Extreme InfiniTV</h3>
            <span
              id="update-pill"
              data-i18n="settings.about.updateAvailableShort"
              class="hidden h-5 px-2 items-center justify-center rounded-full text-label font-semibold uppercase tracking-wide bg-accent text-bg shrink-0 pulse-ring">
              Update available
            </span>
          </div>
          <div class="settings-rows">

            <div class="settings-row settings-row--stack">
              <div class="settings-row__control w-full flex-col items-stretch gap-3">
                <dl class="flex flex-col gap-1.5 sm:grid sm:grid-cols-3 sm:gap-x-6 text-sm">
                  <div class="flex items-baseline justify-between gap-2 sm:flex-col sm:gap-0.5 min-w-0">
                    <dt data-i18n="settings.about.current" class="text-fg-3 text-eyebrow font-medium uppercase">Current</dt>
                    <dd id="update-current" class="text-fg tabular-nums truncate">-</dd>
                  </div>
                  <div class="flex items-baseline justify-between gap-2 sm:flex-col sm:gap-0.5 min-w-0">
                    <dt data-i18n="settings.about.latest" class="text-fg-3 text-eyebrow font-medium uppercase">Latest</dt>
                    <dd id="update-latest" class="text-fg tabular-nums truncate">-</dd>
                  </div>
                  <div class="flex items-baseline justify-between gap-2 sm:flex-col sm:gap-0.5 min-w-0">
                    <dt data-i18n="settings.about.accountExpires" class="text-fg-3 text-eyebrow font-medium uppercase">Account expires</dt>
                    <dd data-account-expiration="raw" class="text-fg tabular-nums truncate">-</dd>
                  </div>
                </dl>

                <div
                  id="update-progress-wrap"
                  class="hidden flex-col gap-1.5">
                  <div
                    id="update-progress-track"
                    class="update-progress-track h-1.5 rounded-full bg-surface-2 overflow-hidden"
                    role="progressbar"
                    aria-valuemin="0"
                    aria-valuemax="100"
                    aria-label="Update download progress"
                    data-i18n-attr="aria-label:settings.update.progressLabel">
                    <div id="update-progress-bar" class="update-progress-fill h-full transition-[width] duration-150" style="width: 0%"></div>
                  </div>
                  <div id="update-progress-text" class="text-xs text-fg-3 tabular-nums">0%</div>
                </div>
              </div>
            </div>

            <div id="auto-update-row" class="settings-row">
              <div class="settings-row__text">
                <h3 data-i18n="settings.update.autoUpdate" class="text-sm font-medium leading-snug">Automatic updates</h3>
                <p id="auto-update-hint" data-i18n="settings.update.autoUpdateHint" class="text-xs text-fg-3">
                  When disabled, updates will not install automatically. You will still see when one is available.
                </p>
              </div>
              <div class="settings-row__control">
                <select id="auto-update-select" class="field-input" aria-describedby="auto-update-hint">
                  <option value="on" data-i18n="settings.update.autoUpdateOn">Enabled</option>
                  <option value="off" data-i18n="settings.update.autoUpdateOff">Disabled</option>
                </select>
              </div>
            </div>

            <div id="update-channel-row" class="settings-row">
              <div class="settings-row__text">
                <h3 data-i18n="settings.update.channel" class="text-sm font-medium leading-snug">Update channel</h3>
                <p id="update-channel-hint" data-i18n="settings.update.channelHint" class="text-xs text-fg-3">
                  Beta versions are early test builds and may be unstable.
                </p>
              </div>
              <div class="settings-row__control">
                <select id="update-channel-select" class="field-input" aria-describedby="update-channel-hint">
                  <option value="stable" data-i18n="settings.update.channelStable">Stable</option>
                  <option value="beta" data-i18n="settings.update.channelBeta">Beta</option>
                </select>
              </div>
            </div>

            <div class="settings-row settings-row--stack">
              <div class="settings-row__control w-full">
                <div class="grid grid-cols-1 gap-2 sm:grid-flow-col sm:auto-cols-fr">
                  <button id="update-check" type="button" class="btn justify-start sm:justify-center">
                    <IconRefresh aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.about.checkUpdate">Check for updates</span>
                  </button>
                  <button id="update-install" type="button" hidden class="btn justify-start sm:justify-center">
                    <IconCloudDownload aria-hidden="true" class="btn-icon" />
                    <span id="update-install-label">Download &amp; install</span>
                  </button>
                  <button id="update-relaunch" type="button" hidden class="btn justify-start sm:justify-center">
                    <span data-i18n="settings.about.restartNow">Restart now</span>
                  </button>
                  <button id="update-get" type="button" hidden class="btn justify-start sm:justify-center">
                    <IconExternalLink aria-hidden="true" class="btn-icon" />
                    <span id="update-get-label" data-i18n="settings.update.getFromGithub">Get it on GitHub</span>
                  </button>
                  <a
                    id="update-github"
                    href="https://github.com/infinitel8p/Extreme-InfiniTV/releases/latest"
                    target="_blank"
                    rel="noopener noreferrer"
                    data-external
                    class="btn justify-start sm:justify-center text-fg-2">
                    <IconBrandGithub aria-hidden="true" class="btn-icon" />
                    <span data-i18n="settings.support.github">GitHub</span>
                  </a>
                </div>
              </div>
            </div>

          </div>

          <div class="px-5 sm:px-6 pt-4 pb-5 flex flex-col gap-2 border-t border-line/60">
            <span
              id="update-msg"
              aria-live="polite"
              class="text-xs text-fg-3 transition-opacity duration-300 empty:hidden"></span>
            <p id="update-notes" class="hidden text-xs text-fg-3 whitespace-pre-line"></p>
            <p
              id="update-degraded"
              hidden
              data-i18n="settings.update.updaterUnreachable"
              class="text-xs text-warn leading-relaxed">
              Couldn't reach the update server, so in-app install isn't available right now. Use GitHub, or check again in a moment.
            </p>
            <p
              id="update-manual-only"
              hidden
              data-i18n="settings.update.manualOnly"
              class="text-xs text-fg-3 leading-relaxed">
              Auto-update isn't available on this build.
            </p>
            <p
              id="linux-appimage-notice"
              hidden
              data-i18n="settings.about.linuxAppImageNotice"
              class="text-xs text-fg-3 leading-relaxed">
              Auto-updates on Linux are only delivered to the AppImage build. For deb / rpm installs, grab new versions from the GitHub releases page above.
            </p>
          </div>
        </div>

        <details
          id="changelog-section"
          data-settings-item
          class="changelog-outer settings-surface icon-mark-host group scroll-mt-6 lg:scroll-mt-20">
          <summary class="settings-surface__header cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden pb-5 sm:pb-6">
            <span class="icon-mark">
              <IconSparkles aria-hidden="true" stroke={2} />
            </span>
            <span class="flex items-center gap-2 min-w-0">
              <h3 data-i18n="settings.about.whatsNew" class="text-base font-semibold">What's new</h3>
              <span
                id="changelog-status"
                data-i18n="settings.about.whatsNewLoading"
                class="text-xs text-fg-3 empty:hidden">Loading…</span>
            </span>
            <span class="ms-auto shrink-0">
              <IconChevronDown
                aria-hidden="true"
                stroke={1.75}
                class="size-4 text-fg-3 shrink-0 changelog-chevron transition-transform duration-200" />
            </span>
          </summary>
          <div id="changelog-body" class="flex flex-col gap-2 px-5 sm:px-6 pb-5 sm:pb-6 pt-1 border-t border-line/60 empty:hidden"></div>
        </details>

        <details
          id="licenses-section"
          data-settings-item
          class="changelog-outer settings-surface icon-mark-host group scroll-mt-6 lg:scroll-mt-20">
          <summary class="settings-surface__header cursor-pointer select-none list-none [&::-webkit-details-marker]:hidden pb-5 sm:pb-6">
            <span class="icon-mark">
              <IconLicense aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.licenses.title" class="text-base font-semibold">Licenses and attribution</h3>
            <span class="ms-auto shrink-0">
              <IconChevronDown
                aria-hidden="true"
                stroke={1.75}
                class="size-4 text-fg-3 shrink-0 changelog-chevron transition-transform duration-200" />
            </span>
          </summary>
          <div class="flex flex-col gap-5 px-5 sm:px-6 pb-5 sm:pb-6 pt-1 border-t border-line/60">

            <!-- FFmpeg -->
            <div class="flex flex-col gap-2">
              <div class="flex items-baseline justify-between">
                <span class="text-sm font-medium text-fg">FFmpeg</span>
                <span class="text-2xs uppercase tracking-wide text-fg-3 shrink-0 ms-3">LGPL 2.1+</span>
              </div>
              <p data-i18n="settings.licenses.ffmpegNotice" class="text-xs text-fg-3">
                The Windows and Linux desktop builds bundle a trimmed FFmpeg build (LGPL v2.1 or later), used only to convert live TV audio the app can't otherwise play.
              </p>
              <div class="grid grid-cols-1 sm:grid-cols-3 gap-2">
                <a
                  href="https://ffmpeg.org"
                  target="_blank"
                  rel="noopener noreferrer"
                  data-external
                  class="btn justify-start sm:justify-center">
                  <IconExternalLink aria-hidden="true" class="btn-icon" />
                  <span data-i18n="settings.licenses.ffmpegSite">FFmpeg project</span>
                </a>
                <a
                  href="https://www.gnu.org/licenses/old-licenses/lgpl-2.1.html"
                  target="_blank"
                  rel="noopener noreferrer"
                  data-external
                  class="btn justify-start sm:justify-center">
                  <IconExternalLink aria-hidden="true" class="btn-icon" />
                  <span data-i18n="settings.licenses.lgplText">LGPL v2.1 license text</span>
                </a>
                <a
                  href="https://github.com/infinitel8p/Extreme-InfiniTV/releases/tag/ffmpeg-sidecar-v2"
                  target="_blank"
                  rel="noopener noreferrer"
                  data-external
                  class="btn justify-start sm:justify-center">
                  <IconBrandGithub aria-hidden="true" class="btn-icon" />
                  <span data-i18n="settings.licenses.sourceRecipe">Source & build recipe</span>
                </a>
              </div>
            </div>

            <!-- TMDB -->
            <div class="flex flex-col gap-1">
              <span class="text-sm font-medium text-fg">TMDB</span>
              <p class="text-xs text-fg-3">
                This product uses the TMDB API but is not endorsed or certified by TMDB.
                <a
                  href="https://www.themoviedb.org/"
                  target="_blank"
                  rel="noopener noreferrer"
                  data-external
                  class="text-accent underline ms-1">themoviedb.org</a>
              </p>
            </div>

            <!-- Playback engines -->
            <div class="flex flex-col gap-1">
              <div class="flex items-baseline justify-between">
                <span class="text-sm font-medium text-fg">hls.js · mpegts.js · Shaka Player · Video.js · ArtPlayer</span>
                <span class="text-2xs uppercase tracking-wide text-fg-3 shrink-0 ms-3">APACHE-2.0 · MIT</span>
              </div>
              <p class="text-xs text-fg-3">
                <span data-i18n="settings.licenses.mpegtsPatch">mpegts.js ships with a small audio-track fallback patch.</span>
                <a
                  href="https://github.com/infinitel8p/Extreme-InfiniTV/blob/main/patches/mpegts.js.patch"
                  target="_blank"
                  rel="noopener noreferrer"
                  data-external
                  data-i18n="settings.licenses.patchFile"
                  class="text-accent underline ms-1">View patch</a>
              </p>
            </div>

            <!-- Fonts -->
            <div class="flex flex-col gap-1">
              <div class="flex items-baseline justify-between">
                <span class="text-sm font-medium text-fg">Geist Variable · Instrument Serif</span>
                <a
                  href="https://scripts.sil.org/OFL"
                  target="_blank"
                  rel="noopener noreferrer"
                  data-external
                  data-i18n="settings.licenses.oflText"
                  class="text-2xs uppercase tracking-wide text-accent underline shrink-0 ms-3">SIL OFL</a>
              </div>
              <p data-i18n="settings.licenses.fontsNotice" class="text-xs text-fg-3">Self-hosted typefaces.</p>
            </div>

            <!-- Framework -->
            <div class="flex flex-col gap-1">
              <div class="flex items-baseline justify-between">
                <span class="text-sm font-medium text-fg">Tauri · Tabler Icons</span>
                <span class="text-2xs uppercase tracking-wide text-fg-3 shrink-0 ms-3">MIT / APACHE-2.0</span>
              </div>
              <p data-i18n="settings.licenses.uiNotice" class="text-xs text-fg-3">App framework and icon set.</p>
            </div>

            <!-- Metadata attribution: wording is required by each provider's terms. -->
            <div class="flex flex-col gap-1">
              <div class="flex items-baseline justify-between">
                <span class="text-sm font-medium text-fg">TheTVDB</span>
                <span data-i18n="settings.licenses.metadataTag" class="text-2xs uppercase tracking-wide text-fg-3 shrink-0 ms-3">Metadata</span>
              </div>
              <p class="text-xs text-fg-3">
                Metadata provided by <a href="https://www.thetvdb.com/" data-external class="underline hover:text-fg">TheTVDB</a>.
                Please consider <a href="https://www.thetvdb.com/subscribe" data-external class="underline hover:text-fg">adding missing information or subscribing</a>.
              </p>
            </div>

            <div class="flex flex-col gap-1">
              <div class="flex items-baseline justify-between">
                <span class="text-sm font-medium text-fg">TMDB</span>
                <span data-i18n="settings.licenses.metadataTag" class="text-2xs uppercase tracking-wide text-fg-3 shrink-0 ms-3">Metadata</span>
              </div>
              <p class="text-xs text-fg-3">
                This application uses <a href="https://www.themoviedb.org/" data-external class="underline hover:text-fg">TMDB</a>
                and the TMDB APIs but is not endorsed, certified, or otherwise approved by TMDB.
              </p>
            </div>

          </div>
        </details>
      </div>
    </section>

    <!-- Group: Help & feedback -->
    <section id="settings-help" class="settings-group scroll-mt-8 lg:scroll-mt-20">
      <h2 data-i18n="settings.group.help" class="settings-group__title">Help & feedback</h2>
      <div class="flex flex-col gap-4">
        <div id="card-docs" data-settings-item class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconBook aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.help.docsTitle" class="text-base font-semibold">Read the docs</h3>
          </div>
          <p data-i18n="settings.help.docsIntro" class="settings-surface__helper text-xs text-fg-3">
            Step-by-step guides for sign-in, external players, the keyboard and D-pad bindings, and the most common things people get stuck on.
          </p>
          <div class="px-5 sm:px-6 pb-5">
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
              <a
                href="/docs"
                class="btn justify-start sm:justify-center">
                <IconBook aria-hidden="true" class="btn-icon" />
                <span data-i18n="settings.help.openDocs">Open documentation</span>
              </a>
              <a
                href="/docs?p=getting-started"
                class="btn justify-start sm:justify-center">
                <IconRocket aria-hidden="true" class="btn-icon" />
                <span data-i18n="settings.help.openQuickStart">Quick start</span>
              </a>
            </div>
          </div>
        </div>

        <div id="card-feedback" data-settings-item class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark">
              <IconMail aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.help.title" class="text-base font-semibold">Found a bug or have a suggestion?</h3>
          </div>
          <p data-i18n="settings.help.intro" class="settings-surface__helper text-xs text-fg-3">
            Bug reports, feature requests, questions, and general feedback all go to GitHub - search existing issues first, or pick a template and open a new one.
          </p>
          <div class="px-5 sm:px-6 pb-5">
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
              <a
                href="https://github.com/infinitel8p/Extreme-InfiniTV/issues/new/choose"
                target="_blank"
                rel="noopener noreferrer"
                data-external
                class="btn justify-start sm:justify-center">
                <IconBug aria-hidden="true" class="btn-icon" />
                <span data-i18n="settings.help.reportIssue">Report a bug</span>
              </a>
              <a
                href="https://github.com/infinitel8p/Extreme-InfiniTV/discussions/new/choose"
                target="_blank"
                rel="noopener noreferrer"
                data-external
                class="btn justify-start sm:justify-center">
                <IconMail aria-hidden="true" class="btn-icon" />
                <span data-i18n="settings.help.sendFeedback">Send feedback</span>
              </a>
            </div>
            <button id="settings-export-diagnostics" type="button" class="btn justify-start sm:justify-center w-full mt-2">
              <IconFileZip aria-hidden="true" class="btn-icon" />
              <span data-i18n="settings.help.exportDiagnostics">Export diagnostics</span>
            </button>
            <p data-i18n="settings.help.exportDiagnosticsHelper" class="text-xs text-fg-3 mt-2">
              Save a single file with app and device details, recent network activity, playlist types and the newest log files. Attach it to a bug report so the problem can be traced.
            </p>
            <p data-i18n="settings.help.diagnosticsPrivacy" class="text-xs text-fg-3 mt-1">
              Usernames, passwords and tokens are stripped out. Server names can still appear, so review the file before posting it publicly.
            </p>
            <span id="settings-export-diagnostics-msg" aria-live="polite" class="text-xs text-fg-3 break-all transition-opacity duration-300 empty:hidden mt-1 block"></span>
          </div>

          <div data-settings-item class="settings-row mt-1 border-t border-line-soft pt-4">
            <div class="settings-row__text">
              <h3 data-i18n="settings.help.discordTitle" class="text-sm font-medium leading-snug">Discord community</h3>
              <p data-i18n="settings.help.discordHelper" class="text-xs text-fg-3">
                Questions, feedback, and beta chat - come say hi.
              </p>
            </div>
            <div class="settings-row__control">
              <a
                href="https://discord.gg/GH5V3AkzfA"
                target="_blank"
                rel="noopener noreferrer"
                data-external
                class="btn justify-center">
                <IconBrandDiscord aria-hidden="true" class="btn-icon" />
                <span data-i18n="settings.help.discordJoin">Join</span>
              </a>
            </div>
          </div>
        </div>

        <div id="settings-support" data-settings-item class="settings-surface icon-mark-host scroll-mt-6 lg:scroll-mt-20">
          <div class="settings-surface__header">
            <span class="icon-mark icon-mark--heart">
              <IconHeart aria-hidden="true" stroke={2} />
            </span>
            <h3 data-i18n="settings.support.title" class="text-base font-semibold">Like the app?</h3>
          </div>
          <div class="px-5 sm:px-6 pb-5 pt-4 flex flex-col gap-4">
            <p data-i18n="settings.support.intro" class="text-sm text-fg-2">
              Extreme InfiniTV is free and open source. If it saves you time or sweetens your living-room setup, a small tip helps cover hosting, signing certs, and the next round of features.
            </p>
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-2">
              <a
                href="https://github.com/sponsors/infinitel8p"
                target="_blank"
                rel="noopener noreferrer"
                data-external
                class="btn justify-start sm:justify-center">
                <IconBrandGithub aria-hidden="true" class="btn-icon" />
                <span data-i18n="settings.support.githubSponsors">GitHub Sponsors</span>
              </a>
              <a
                href="https://ko-fi.com/infinitel8p"
                target="_blank"
                rel="noopener noreferrer"
                data-external
                class="btn justify-start sm:justify-center">
                <IconCoffee aria-hidden="true" class="btn-icon" />
                <span data-i18n="settings.support.kofi">Ko-fi</span>
              </a>
              <a
                href="https://www.paypal.com/paypalme/ludovicoferrara"
                target="_blank"
                rel="noopener noreferrer"
                data-external
                class="btn justify-start sm:justify-center">
                <IconBrandPaypal aria-hidden="true" class="btn-icon" />
                <span data-i18n="settings.support.paypal">PayPal</span>
              </a>
            </div>
            <p data-i18n="settings.support.fineprint" class="text-2xs text-fg-3">
              No tracking, no ads, no telemetry - just a thank-you button.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Group: Danger zone (always last) -->
    <section id="settings-danger" class="settings-group settings-group--danger scroll-mt-8 lg:scroll-mt-20">
      <h2 data-i18n="settings.group.dangerZone" class="settings-group__title">Danger zone</h2>
      <article class="icon-mark-host rounded-2xl border border-bad/30 bg-bad/[0.03] p-5 sm:p-6 flex flex-col gap-4">
        <div class="flex items-center gap-3">
          <span class="icon-mark icon-mark--bad">
            <IconAlertTriangle aria-hidden="true" stroke={2} />
          </span>
          <h3 data-i18n="settings.danger.heading" class="text-base font-semibold text-bad">Reset everything</h3>
        </div>
        <p data-i18n-html="settings.danger.helperLong" class="text-sm text-fg-2">
          Wipes <span class="text-fg font-medium">all</span> app data: playlists, cached lists, favorites, recents, and locally stored EPG references. Next launch starts from a clean slate.
        </p>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 pt-1">
          <button id="settings-reset-all" type="button" class="btn-danger justify-start sm:justify-center">
            <IconTrash aria-hidden="true" class="btn-icon" />
            <span data-i18n="settings.danger.resetAll">Reset everything</span>
          </button>
          <button id="settings-restore-last" type="button" class="btn justify-start sm:justify-center" hidden>
            <IconRestore aria-hidden="true" class="btn-icon" />
            <span data-i18n="settings.danger.restoreLast">Restore last backup</span>
          </button>
        </div>
        <p id="settings-restore-last-helper" data-i18n="settings.danger.restoreLastHelper" class="text-2xs text-fg-3" hidden>
          Restores the snapshot saved automatically at your last export or right before your last reset.
        </p>
        <span
          id="settings-reset-msg"
          aria-live="polite"
          class="text-xs text-fg-3 transition-opacity duration-300 empty:hidden"></span>
      </article>
    </section>

    </div><!-- /settings-stack -->
    </div><!-- /lg:flex two-column wrapper -->

    </div><!-- /page wrap -->
  </main>

  <!-- Reset confirmation dialog -->
  <dialog
    id="settings-reset-dialog"
    aria-labelledby="settings-reset-title"
    aria-describedby="settings-reset-desc"
    class="fixed inset-0 m-auto rounded-2xl border border-line bg-surface text-fg p-0
           w-[min(28rem,calc(100vw-2rem))] backdrop:bg-black/60">
    <div class="icon-mark-host flex flex-col p-5 gap-4">
      <div class="flex items-start gap-3">
        <span class="icon-mark icon-mark--bad icon-mark--lg">
          <IconAlertTriangle aria-hidden="true" stroke={2} />
        </span>
        <div class="flex flex-col gap-1 min-w-0 pt-0.5">
          <h2 id="settings-reset-title" data-i18n="settings.danger.dialogTitle" class="text-base font-semibold">
            Reset everything?
          </h2>
          <p id="settings-reset-desc" data-i18n-html="settings.danger.dialogBody" class="text-sm text-fg-2">
            This wipes <span class="text-fg font-medium">all</span> playlists, favorites,
            recents, and cached lists. This can't be undone.
          </p>
        </div>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 pt-1">
        <button
          id="settings-reset-cancel"
          type="button"
          class="btn justify-start sm:justify-center">
          <span data-i18n="common.cancel">Cancel</span>
        </button>
        <button
          id="settings-reset-confirm"
          type="button"
          class="btn-danger justify-start sm:justify-center">
          <IconTrash aria-hidden="true" class="btn-icon" />
          <span data-i18n="settings.danger.resetAll">Reset everything</span>
        </button>
      </div>
    </div>
  </dialog>

  <!-- Import confirmation dialog -->
  <dialog
    id="settings-import-dialog"
    aria-labelledby="settings-import-title"
    aria-describedby="settings-import-desc"
    class="fixed inset-0 m-auto rounded-2xl border border-line bg-surface text-fg p-0
           w-[min(28rem,calc(100vw-2rem))] backdrop:bg-black/60">
    <div class="icon-mark-host flex flex-col p-5 gap-4">
      <div class="flex items-start gap-3">
        <span class="icon-mark icon-mark--bad icon-mark--lg">
          <IconAlertTriangle aria-hidden="true" stroke={2} />
        </span>
        <div class="flex flex-col gap-1 min-w-0 pt-0.5">
          <h2 id="settings-import-title" class="text-base font-semibold">
            Replace everything with this backup?
          </h2>
          <p id="settings-import-desc" class="text-sm text-fg-2">
            Import will replace all playlists, favorites, recents, watched
            progress, hidden categories, and app settings on this device.
          </p>
        </div>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 pt-1">
        <button id="settings-import-cancel" type="button" class="btn justify-start sm:justify-center">
          <span data-i18n="common.cancel">Cancel</span>
        </button>
        <button id="settings-import-confirm" type="button" class="btn-danger justify-start sm:justify-center">
          <IconUpload aria-hidden="true" class="btn-icon" />
          <span>Continue</span>
        </button>
      </div>
    </div>
  </dialog>

  <script>
    import { Store } from "@tauri-apps/plugin-store"
    import { log } from "@/scripts/lib/log.js"
    import { t, LOCALE_EVENT, getActiveLocale, initI18n } from "@/scripts/lib/i18n.js"
    import { LANGUAGE_TOKENS, languageTagLabel } from "@/scripts/lib/language-tags.ts"
    import { ICON_CHECK } from "@/scripts/lib/icons.ts"
    import {
      getActiveEntry,
      getEntries,
      refreshActive,
      isTauri,
      loadCreds,
      addEntry,
    } from "@/scripts/lib/creds.js"
    import {
      getNewestCacheTimeAsync,
      getCacheSizeAsync,
      clearAll as clearAllCache,
      hydrate as hydrateCache,
    } from "@/scripts/lib/cache.js"
    import { clearImageCache } from "@/scripts/lib/img-cache.ts"
    import {
      renderPlaylistRow,
      getPlaylistListEmptyCopy,
    } from "@/scripts/lib/playlist-rows.js"
    import { clearViewingHistory } from "@/scripts/lib/preferences.js"
    import { confirmDialog } from "@/scripts/lib/confirm-dialog.ts"
    import { snifferAvailable } from "@/scripts/lib/stream-sniffer.ts"
    import { fetchReleases, renderMarkdown, filterReleasesForDisplay } from "@/scripts/lib/changelog.ts"
    import {
      checkForUpdate,
      isStoreBuild,
      isPlayStoreInstall,
      getPlayStoreUrl,
      getCurrentAppVersion,
      resolveUpdateFeedUrl,
      withUpdaterRetry,
      isTransientUpdaterError,
      MS_STORE_UPDATES_URL,
    } from "@/scripts/lib/update-check.js"
    import { sandboxRuntimeSync } from "@/scripts/lib/sandbox.ts"
    import {
      getCompositingState,
      setCompositingSetting,
      relaunchApp as relaunchForCompositing,
    } from "@/scripts/lib/compositing.ts"
    import { openExternal } from "@/scripts/lib/external-link.js"
    import { attachDialogSpatialNav } from "@/scripts/lib/dialog-spatial-nav.js"
    import {
      UA_PRESETS,
      getUserAgent,
      setUserAgent,
      getDownloadDir,
      setDownloadDir,
      getDownloadConcurrency,
      setDownloadConcurrency,
      getWriteNfoEnabled,
      setWriteNfoEnabled,
      getPerfMode,
      setPerfMode,
      getUiSoundsEnabled,
      setUiSoundsEnabled,
      getHapticsEnabled,
      setHapticsEnabled,
      getMonoAudioEnabled,
      setMonoAudioEnabled,
      getCaptionsAutoEnabled,
      setCaptionsAutoEnabled,
      getAccent,
      setAccent,
      ACCENT_PRESETS,
      getDensity,
      setDensity,
      getProgressRetentionDays,
      setProgressRetentionDays,
      getNetworkTimeoutSeconds,
      setNetworkTimeoutSeconds,
      getUpdateChannel,
      setUpdateChannel,
      getAutoUpdateEnabled,
      setAutoUpdateEnabled,
      getContentLanguage,
      setContentLanguage,
      getLanguageGroupingEnabled,
      setLanguageGroupingEnabled,
      getAudioTranscodeAuto,
      setAudioTranscodeAuto,
      getFfmpegPath,
      setFfmpegPath,
      getDevModeEnabled,
      setDevModeEnabled,
      DEV_MODE_EVENT,
    } from "@/scripts/lib/app-settings.js"
    import { playUiSound } from "@/scripts/lib/ui-sounds.ts"
    import { hapticsAvailable, performHaptic } from "@/scripts/lib/haptics.ts"
    import { audioTranscodeStatus } from "@/scripts/lib/audio-proxy.ts"
    import { ensureHevcDecodable, isWindowsDesktop as isWindowsDesktopEnv } from "@/scripts/lib/hevc-extension.ts"
    import { deviceSupportsHevc } from "@/scripts/lib/codec-hints.ts"
    import {
      ensureUserInfo,
      getMaxConnectionsSync,
      getActiveConnectionsSync,
      USER_INFO_LOADED_EVENT,
    } from "@/scripts/lib/account-info.js"
    import { isDownloadable, scanDownloadsFolder } from "@/scripts/lib/downloads.js"
    import {
      getDiscordClientId,
      setDiscordClientId,
      isDiscordEnabledForPlaylist,
      setDiscordEnabledForPlaylist,
      DISCORD_RPC_EVENT,
    } from "@/scripts/lib/app-settings.js"
    import { discordRpcSupported } from "@/scripts/lib/discord-rpc.js"
    import {
      getTmdbApiKey,
      setTmdbApiKey,
      getTmdbEnabled,
      setTmdbEnabled,
      getTvdbEnabled,
      setTvdbEnabled,
    } from "@/scripts/lib/app-settings.js"
    import { validateTmdbKey } from "@/scripts/lib/tmdb.ts"
    import { bindExternalLinks } from "@/scripts/lib/external-link.js"
    import { bindBackupRestore } from "@/scripts/lib/restore-backup.js"
    import {
      saveBackupSnapshot,
      loadBackupSnapshot,
      hasBackupSnapshot,
    } from "@/scripts/lib/backup-snapshot.ts"
    import {
      getNetworkLog,
      clearNetworkLog,
      getNetLogIncludeImages,
      setNetLogIncludeImages,
      NET_LOG_EVENT,
    } from "@/scripts/lib/net-log.ts"
    import { fmtAgo } from "@/scripts/lib/playlist-health.ts"
    import { writeClipboardText } from "@/scripts/lib/clipboard.ts"

    bindExternalLinks()
    import { toast, toastSuccess, toastError } from "@/scripts/lib/toast.js"
    import {
      getOffsetSetting,
      setOffsetSetting,
      getProgrammesSync,
    } from "@/scripts/lib/epg-data.js"

    // Linux notice: surface that auto-updates only land on the AppImage
    // build. Shown on any Tauri Linux session - AppImage users will see
    // the line as a confirmation, deb / rpm users as guidance. Skipped in
    // Snap/Flatpak, where the store handles updates instead.
    try {
      const ua = navigator.userAgent || ""
      const isTauriRuntime =
        !!window.__TAURI_INTERNALS__ || !!window.__TAURI__
      const isLinux = ua.includes("Linux") && !ua.includes("Android")
      if (isTauriRuntime && isLinux && !sandboxRuntimeSync()) {
        document.getElementById("linux-appimage-notice")?.removeAttribute("hidden")
      }
    } catch {}

    // A small thank-you for the curious - prints once per session when
    // someone pops the devtools open on Settings.
    try {
      if (!sessionStorage.getItem("xt_console_signed")) {
        sessionStorage.setItem("xt_console_signed", "1")
        const heading = "background: oklch(0.5 0.2 330); color: white; padding: 4px 10px; border-radius: 6px; font-weight: 600;"
        const body = "color: inherit; padding: 4px 0;"
        const link = "color: oklch(0.78 0.15 330); text-decoration: underline;"
        // eslint-disable-next-line no-console
        console.log(
          "%cExtreme InfiniTV%c  thanks for poking around. %csource & issues -> github.com/infinitel8p/Extreme-InfiniTV",
          heading,
          body,
          link,
        )
      }
    } catch {}

    // -------------------------------------------------------------------
    // What's new: lazy-fetch GitHub releases, render the latest 10 with
    // tag + date, latest expanded by default, others collapsed.
    // -------------------------------------------------------------------
    function loadChangelog() {
      const body = document.getElementById("changelog-body")
      const status = document.getElementById("changelog-status")
      if (!body) return
      Promise.all([fetchReleases(), getCurrentAppVersion()])
        .then(([allReleases, currentVersion]) => {
          const releases = filterReleasesForDisplay(allReleases, currentVersion || "").slice(0, 3)
          if (!releases.length) {
            if (status) {
              status.textContent = t("settings.about.whatsNewEmpty")
              status.removeAttribute("data-i18n")
            }
            return
          }
          if (status) {
            status.textContent = ""
            status.removeAttribute("data-i18n")
          }
          body.replaceChildren()
          const chevronSvg =
            '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" class="size-4 shrink-0 changelog-chevron transition-transform duration-200"><path d="M6 9l6 6 6-6"/></svg>'
          for (const release of releases) {
            const details = document.createElement("details")
            details.className =
              "changelog-entry border border-line/60 rounded-xl bg-bg overflow-hidden"

            const summary = document.createElement("summary")
            summary.className =
              "px-4 py-3 cursor-pointer flex items-center gap-3 hover:bg-surface-2 transition-colors list-none"

            const left = document.createElement("div")
            left.className = "flex items-center gap-3 flex-1 min-w-0"

            const tag = document.createElement("span")
            tag.className = "font-medium text-sm tabular-nums truncate"
            tag.textContent = release.name || release.tagName
            left.appendChild(tag)

            if (release.publishedAt) {
              const date = document.createElement("time")
              date.className = "text-xs text-fg-3 shrink-0 tabular-nums"
              date.dateTime = release.publishedAt
              const parsed = new Date(release.publishedAt)
              date.textContent = parsed.toLocaleDateString(undefined, {
                year: "numeric",
                month: "short",
                day: "numeric",
              })
              left.appendChild(date)
            }

            summary.appendChild(left)
            const chevronWrap = document.createElement("span")
            chevronWrap.className = "text-fg-3 shrink-0"
            chevronWrap.innerHTML = chevronSvg
            summary.appendChild(chevronWrap)

            details.appendChild(summary)

            const content = document.createElement("div")
            content.className =
              "changelog-md px-4 py-3 border-t border-line/60 text-sm text-fg-2"
            // marked + DOMPurify are dynamically imported the first time
            // this runs; resolves to the sanitized HTML.
            renderMarkdown(release.body || "").then((html) => {
              content.innerHTML = html
            })
            details.appendChild(content)

            body.appendChild(details)
          }
        })
        .catch(() => {
          if (status) {
            status.textContent = t("settings.about.whatsNewError")
            status.removeAttribute("data-i18n")
          }
        })
    }
    if (typeof window.requestIdleCallback === "function") {
      window.requestIdleCallback(loadChangelog, { timeout: 2000 })
    } else {
      setTimeout(loadChangelog, 500)
    }

    const playlistsEl = document.getElementById("settings-playlists-list")
    const refreshBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-refresh-active"))
    const addFromWebsiteBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-add-from-website"))
    if (snifferAvailable) addFromWebsiteBtn?.classList.remove("hidden")
    addFromWebsiteBtn?.addEventListener("click", async () => {
      const { openAddFromWebsiteDialog } = await import("@/scripts/lib/add-from-website-dialog.ts")
      await openAddFromWebsiteDialog()
    })

    const newCustomPlaylistBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-new-custom-playlist"))
    const newCustomPlaylistDialog = /** @type {HTMLDialogElement|null} */ (document.getElementById("settings-new-custom-playlist-dialog"))
    const newCustomPlaylistNameInput = /** @type {HTMLInputElement|null} */ (document.getElementById("settings-new-custom-playlist-name"))
    if (newCustomPlaylistDialog) {
      attachDialogSpatialNav(newCustomPlaylistDialog, { defaultElement: "#settings-new-custom-playlist-name" })
    }
    newCustomPlaylistBtn?.addEventListener("click", () => {
      if (!newCustomPlaylistDialog || !newCustomPlaylistNameInput) return
      newCustomPlaylistNameInput.value = ""
      newCustomPlaylistDialog.showModal()
    })
    newCustomPlaylistDialog
      ?.querySelector('[data-role="cancel"]')
      ?.addEventListener("click", () => newCustomPlaylistDialog.close())
    newCustomPlaylistDialog?.querySelector("form")?.addEventListener("submit", async (ev) => {
      ev.preventDefault()
      const title = newCustomPlaylistNameInput?.value.trim() || ""
      newCustomPlaylistDialog?.close()
      try {
        const entry = await addEntry(title ? { type: "custom", title } : { type: "custom" })
        location.href = `/playlist-editor?id=${encodeURIComponent(entry._id)}`
      } catch (e) {
        log.error("[settings:new-custom-playlist] failed:", e)
        toastError(t("editor.newPlaylistCreateFail"))
      }
    })

    const cacheSizeEl = document.getElementById("settings-cache-size")
    const cacheNewestEl = document.getElementById("settings-cache-newest")
    const cacheMsgEl = document.getElementById("settings-cache-msg")
    const clearCacheBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-clear-cache"))
    const clearHistoryBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-clear-history"))
    const resetBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-reset-all"))
    const resetMsgEl = document.getElementById("settings-reset-msg")
    const resetDialog = /** @type {HTMLDialogElement|null} */ (document.getElementById("settings-reset-dialog"))
    if (resetDialog) attachDialogSpatialNav(resetDialog, { defaultElement: "#settings-reset-cancel" })
    const resetCancelBtn = document.getElementById("settings-reset-cancel")
    const resetConfirmBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-reset-confirm"))
    const restoreLastBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-restore-last"))
    const restoreLastHelperEl = document.getElementById("settings-restore-last-helper")

    function fmtAge(ts) {
      if (!ts) return "Never"
      const ms = Date.now() - ts
      if (ms < 60_000) return "Just now"
      const m = Math.floor(ms / 60_000)
      if (m < 60) return `${m}m ago`
      const h = Math.floor(m / 60)
      if (h < 24) return `${h}h ago`
      const d = Math.floor(h / 24)
      return `${d}d ago`
    }

    function fmtBytes(n) {
      if (n === 0) return "Empty"
      if (n < 1024) return `${n} B`
      if (n < 1024 * 1024) return `${(n / 1024).toFixed(1)} KB`
      return `${(n / (1024 * 1024)).toFixed(1)} MB`
    }

    /** @type {Map<HTMLElement, number>} */
    const msgTimers = new Map()
    function flashMsg(el, text) {
      if (!el) return
      const prev = msgTimers.get(el)
      if (prev) window.clearTimeout(prev)
      el.textContent = text
      el.classList.remove("opacity-0")
      const t = window.setTimeout(() => {
        el.classList.add("opacity-0")
        const t2 = window.setTimeout(() => {
          if (el.textContent === text) el.textContent = ""
          el.classList.remove("opacity-0")
          msgTimers.delete(el)
        }, 320)
        msgTimers.set(el, t2)
      }, 3500)
      msgTimers.set(el, t)
    }

    function setLoading(btn, loading) {
      if (!btn) return
      if (loading) {
        btn.setAttribute("data-loading", "true")
        btn.setAttribute("aria-busy", "true")
        btn.disabled = true
      } else {
        btn.removeAttribute("data-loading")
        btn.removeAttribute("aria-busy")
        btn.disabled = false
      }
    }

    /** @type {WeakMap<HTMLElement, number>} */
    const successTimers = new WeakMap()
    /** Briefly swap a button's leading icon for the project's check-draw
     *  glyph, then restore. Reuses the same vocabulary PlaylistSwitcher
     *  uses when an entry is selected. */
    function flashSuccess(btn) {
      if (!btn) return
      const icon = btn.querySelector(".btn-icon")
      if (!icon) return
      const prev = successTimers.get(btn)
      if (prev) {
        window.clearTimeout(prev)
        const stash = btn.querySelector('[data-icon-stash="1"]')
        if (stash) stash.remove()
      }
      const original = icon.cloneNode(true)
      original.setAttribute("data-icon-stash", "1")
      original.classList.add("hidden")
      btn.insertBefore(original, icon)

      const tmp = document.createElement("span")
      tmp.innerHTML = ICON_CHECK
      const check = tmp.firstElementChild
      if (!check) return
      check.setAttribute("aria-hidden", "true")
      check.classList.add("btn-icon", "check-draw", "text-accent")
      icon.replaceWith(check)

      const timer = window.setTimeout(() => {
        const stash = btn.querySelector('[data-icon-stash="1"]')
        const live = btn.querySelector(".btn-icon:not([data-icon-stash])")
        if (stash && live) {
          stash.classList.remove("hidden")
          stash.removeAttribute("data-icon-stash")
          live.remove()
        }
        successTimers.delete(btn)
      }, 1100)
      successTimers.set(btn, timer)
    }

    let playlistsRenderGeneration = 0

    async function renderPlaylists() {
      if (!playlistsEl) return
      const generation = ++playlistsRenderGeneration
      const [entries, active] = await Promise.all([
        getEntries(),
        getActiveEntry(),
      ])

      if (!entries.length) {
        playlistsEl.innerHTML = `<div class="px-4 py-4 text-xs text-fg-3">${getPlaylistListEmptyCopy()}</div>`
        refreshBtn?.classList.add("hidden")
        return
      }

      const buildRows = () => {
        const frag = document.createDocumentFragment()
        for (const entry of entries) {
          frag.appendChild(
            renderPlaylistRow({
              entry,
              isActive: active?._id === entry._id,
              density: "full",
            })
          )
        }
        playlistsEl.replaceChildren(frag)
      }

      // Rows paint from memory immediately; hydration only refreshes counts.
      buildRows()

      if (active) refreshBtn?.classList.remove("hidden")
      else refreshBtn?.classList.add("hidden")

      void Promise.allSettled(
        entries.flatMap((entry) => [
          hydrateCache(entry._id, "live"),
          hydrateCache(entry._id, "m3u"),
          hydrateCache(entry._id, "vod"),
          hydrateCache(entry._id, "series"),
        ])
      ).then(() => {
        if (generation === playlistsRenderGeneration) buildRows()
      })
    }

    async function syncCache() {
      try {
        const bytes = await getCacheSizeAsync()
        if (cacheSizeEl) cacheSizeEl.textContent = fmtBytes(bytes)
        if (clearCacheBtn) clearCacheBtn.disabled = bytes === 0
      } catch {}
      try {
        const a = await getActiveEntry()
        const newest = a ? await getNewestCacheTimeAsync(a._id) : null
        if (cacheNewestEl) cacheNewestEl.textContent = fmtAge(newest)
      } catch {}
    }

    refreshBtn?.addEventListener("click", async () => {
      setLoading(refreshBtn, true)
      let ok = false
      try {
        await refreshActive()
        ok = true
      } catch (e) {
        toastError(t("settings.toast.refreshFail"), {
          description: (e && e.message) || t("settings.toast.checkConnLogin"),
          duration: 6000,
        })
      } finally {
        setLoading(refreshBtn, false)
        syncCache()
        if (ok) {
          flashSuccess(refreshBtn)
          toastSuccess(t("settings.toast.refreshOk"))
        }
      }
    })

    let cacheTweenRaf = 0
    function tweenBytes(el, from, to, duration) {
      if (!el) return
      if (cacheTweenRaf) cancelAnimationFrame(cacheTweenRaf)
      const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches
      if (reduceMotion || duration <= 0) {
        el.textContent = fmtBytes(to)
        return
      }
      const start = performance.now()
      const frame = (now) => {
        const elapsed = now - start
        const progress = Math.min(1, elapsed / duration)
        const eased = progress === 1 ? 1 : 1 - Math.pow(2, -10 * progress)
        const value = Math.round(from + (to - from) * eased)
        el.textContent = fmtBytes(value)
        if (progress < 1) {
          cacheTweenRaf = requestAnimationFrame(frame)
        } else {
          cacheTweenRaf = 0
        }
      }
      cacheTweenRaf = requestAnimationFrame(frame)
    }

    clearCacheBtn?.addEventListener("click", async () => {
      setLoading(clearCacheBtn, true)
      let removed = 0
      let ok = false
      let startBytes = 0
      try {
        startBytes = await getCacheSizeAsync()
      } catch {}
      try {
        removed = await clearAllCache()
        removed += await clearImageCache()
        ok = true
        if (removed > 0) {
          toastSuccess(t("settings.toast.cacheCleared"), {
            description: `${removed} ${removed === 1 ? "entry" : "entries"} removed.`,
          })
        } else {
          flashMsg(cacheMsgEl, "Cache was already empty.")
        }
      } catch {
        toastError(t("settings.toast.cacheClearFail"))
      } finally {
        setLoading(clearCacheBtn, false)
        if (ok && startBytes > 0) {
          tweenBytes(cacheSizeEl, startBytes, 0, 720)
          window.setTimeout(syncCache, 760)
        } else {
          syncCache()
        }
        if (ok && removed > 0) flashSuccess(clearCacheBtn)
      }
    })

    clearHistoryBtn?.addEventListener("click", async () => {
      const confirmed = await confirmDialog({
        title: t("settings.viewingHistory.confirmTitle"),
        message: t("settings.viewingHistory.confirm"),
        confirmLabel: t("settings.viewingHistory.clear"),
        destructive: true,
      })
      if (!confirmed) return
      setLoading(clearHistoryBtn, true)
      try {
        const removed = await clearViewingHistory()
        if (removed > 0) {
          toastSuccess(t("settings.toast.historyCleared"))
          flashSuccess(clearHistoryBtn)
        } else {
          toast({ title: t("settings.toast.historyEmpty") })
        }
      } catch {
        toastError(t("settings.toast.historyClearFail"))
      } finally {
        setLoading(clearHistoryBtn, false)
      }
    })

    const openLogsBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-open-logs"))
    const shareLogsBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-share-logs"))
    const isTauriEnv =
      typeof window !== "undefined" && (!!window.__TAURI_INTERNALS__ || !!window.__TAURI__)
    const isAndroidEnv = /Android/i.test(navigator.userAgent || "")
    if (openLogsBtn && isTauriEnv && !isAndroidEnv) {
      openLogsBtn.classList.remove("hidden")
      openLogsBtn.addEventListener("click", async () => {
        let dir = ""
        try {
          const { appLogDir } = await import("@tauri-apps/api/path")
          dir = await appLogDir()
          try {
            const { invoke } = await import("@tauri-apps/api/core")
            dir = await invoke("resolve_explorer_path", { path: dir })
          } catch {}
          const { openPath } = await import("@tauri-apps/plugin-opener")
          await openPath(dir)
        } catch (err) {
          log.warn("[settings] open log folder failed:", err)
          if (dir) {
            try {
              const { writeClipboardText } = await import("@/scripts/lib/clipboard.ts")
              await writeClipboardText(dir)
              flashMsg(cacheMsgEl, t("settings.storage.logPathCopied", { dir }))
              return
            } catch {}
          }
          toastError(t("settings.storage.openLogsFail") || "Couldn't open the log folder.")
        }
      })
    }

    const openAppDataBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("settings-open-appdata")
    )
    if (openAppDataBtn && isTauriEnv && !isAndroidEnv) {
      openAppDataBtn.classList.remove("hidden")
      openAppDataBtn.addEventListener("click", async () => {
        try {
          const { appDataDir } = await import("@tauri-apps/api/path")
          let dir = await appDataDir()
          try {
            const { invoke } = await import("@tauri-apps/api/core")
            dir = await invoke("resolve_explorer_path", { path: dir })
          } catch {}
          const { openPath } = await import("@tauri-apps/plugin-opener")
          await openPath(dir)
        } catch (err) {
          log.warn("[settings] open app data folder failed:", err)
          toastError(
            t("settings.storage.openAppDataFail") || "Couldn't open the app data folder."
          )
        }
      })
    }

    if (shareLogsBtn && isTauriEnv && isAndroidEnv) {
      shareLogsBtn.classList.remove("hidden")
      shareLogsBtn.addEventListener("click", async () => {
        try {
          const { appLogDir } = await import("@tauri-apps/api/path")
          const dir = await appLogDir()
          const bridge = /** @type {any} */ (window).AndroidLog
          const shared =
            typeof bridge?.shareNewestLog === "function" ? bridge.shareNewestLog(dir) : false
          if (!shared) {
            flashMsg(cacheMsgEl, t("settings.storage.noLogs") || "No log file yet.")
          }
        } catch (err) {
          log.warn("[settings] share logs failed:", err)
          toastError(t("settings.storage.shareLogsFail") || "Couldn't share the log file.")
        }
      })
    }

    // -------------------------
    // Backup: export / import
    // -------------------------
    const exportBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-export"))
    const importBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("settings-import"))
    const importInput = /** @type {HTMLInputElement|null} */ (document.getElementById("settings-import-input"))
    const backupMsgEl = document.getElementById("settings-backup-msg")

    async function downloadBlobWeb(name, content, mime = "application/json") {
      const blob = new Blob([content], { type: mime })
      const url = URL.createObjectURL(blob)
      const a = document.createElement("a")
      a.href = url
      a.download = name
      document.body.appendChild(a)
      a.click()
      a.remove()
      setTimeout(() => URL.revokeObjectURL(url), 5000)
    }

    exportBtn?.addEventListener("click", async () => {
      setLoading(exportBtn, true)
      try {
        const { exportAll, suggestedFilename } = await import("@/scripts/lib/backup.js")
        const snapshot = await exportAll()
        const json = JSON.stringify(snapshot, null, 2)
        const filename = suggestedFilename()
        let savedTo = ""
        if (isTauri && isAndroid) {
          const androidFs = await import("@/scripts/lib/android-fs.js")
          let pickerError = null
          try {
            const written = await androidFs.saveJsonFile(filename, json)
            if (!written) {
              setLoading(exportBtn, false)
              return
            }
          } catch (e) {
            pickerError = e
            log.warn(
              "[settings:export] SAF save picker failed, falling back to public Download/:",
              e
            )
          }
          if (pickerError) {
            try {
              const publicPath = await androidFs.savePublicJsonFile(filename, json)
              savedTo = publicPath || ""
            } catch (publicErr) {
              log.error("[settings:export] public Download/ save failed:", publicErr)
              toastError(t("settings.toast.backupExportFail"), {
                description:
                  (publicErr && publicErr.message) ||
                  (pickerError && pickerError.message) ||
                  "Android FS is not available.",
                duration: 7000,
              })
              return
            }
          }
        } else if (isTauri) {
          const { save } = await import("@tauri-apps/plugin-dialog")
          const target = await save({
            defaultPath: filename,
            filters: [{ name: "JSON", extensions: ["json"] }],
          })
          if (!target) {
            setLoading(exportBtn, false)
            return
          }
          const { writeTextFile } = await import("@tauri-apps/plugin-fs")
          await writeTextFile(target, json)
          savedTo = target
        } else {
          await downloadBlobWeb(filename, json)
        }
        toastSuccess(t("settings.toast.backupExported"), {
          description: savedTo
            ? `${snapshot.creds.entries.length} playlist(s) - ${savedTo}`
            : `${snapshot.creds.entries.length} playlist(s).`,
          duration: 6000,
        })
        flashMsg(backupMsgEl, "Saved.")
        saveBackupSnapshot()
          .then(refreshRestoreLastVisibility)
          .catch((err) => log.warn("[settings:export] backup snapshot refresh failed:", err))
      } catch (e) {
        log.error("[settings:export] failed:", e)
        toastError(t("settings.toast.backupExportFail"), {
          description: (e && e.message) || "See console.",
        })
      } finally {
        setLoading(exportBtn, false)
      }
    })

    // -------------------------
    // Diagnostics export
    // -------------------------
    const exportDiagnosticsBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("settings-export-diagnostics")
    )
    const exportDiagnosticsMsgEl = document.getElementById("settings-export-diagnostics-msg")

    exportDiagnosticsBtn?.addEventListener("click", async () => {
      setLoading(exportDiagnosticsBtn, true)
      try {
        const { collectDiagnosticBundle } = await import("@/scripts/lib/diagnostic-bundle.js")
        const bundle = await collectDiagnosticBundle()
        let savedTo = ""
        if (isTauriEnv && isAndroidEnv) {
          const androidFs = await import("@/scripts/lib/android-fs.js")
          let pickerError = null
          try {
            const written = await androidFs.saveBinaryFile(bundle.filename, bundle.bytes, "application/zip")
            if (!written) return
            try {
              await androidFs.shareFile(written)
            } catch (shareErr) {
              log.warn("[settings:export-diagnostics] share failed:", shareErr)
            }
          } catch (e) {
            pickerError = e
            log.warn(
              "[settings:export-diagnostics] SAF save picker failed, falling back to public Download/:",
              e
            )
          }
          if (pickerError) {
            try {
              const publicPath = await androidFs.savePublicBinaryFile(
                bundle.filename,
                bundle.bytes,
                "application/zip"
              )
              savedTo = publicPath || ""
            } catch (publicErr) {
              log.error("[settings:export-diagnostics] public Download/ save failed:", publicErr)
              toastError(t("settings.toast.diagnosticsExportFail"), {
                description:
                  (publicErr && publicErr.message) ||
                  (pickerError && pickerError.message) ||
                  "Android FS is not available.",
                duration: 7000,
              })
              return
            }
          }
        } else if (isTauriEnv) {
          const { save } = await import("@tauri-apps/plugin-dialog")
          const target = await save({
            defaultPath: bundle.filename,
            filters: [{ name: "ZIP", extensions: ["zip"] }],
          })
          if (!target) return
          const { writeFile } = await import("@tauri-apps/plugin-fs")
          await writeFile(target, bundle.bytes)
          savedTo = target
        } else {
          await downloadBlobWeb(bundle.filename, bundle.bytes, "application/zip")
        }
        toastSuccess(t("settings.toast.diagnosticsExported"), {
          description: savedTo
            ? `${bundle.fileNames.length} file(s) included - ${savedTo}`
            : `${bundle.fileNames.length} file(s) included.`,
          duration: 6000,
        })
        flashSuccess(exportDiagnosticsBtn)
        flashMsg(exportDiagnosticsMsgEl, "Saved.")
      } catch (e) {
        log.error("[settings:export-diagnostics] failed:", e)
        toastError(t("settings.toast.diagnosticsExportFail"), {
          description: (e && e.message) || "See console.",
        })
      } finally {
        setLoading(exportDiagnosticsBtn, false)
      }
    })

    const importDialog = /** @type {HTMLDialogElement|null} */ (
      document.getElementById("settings-import-dialog")
    )
    const importConfirmBtn = document.getElementById("settings-import-confirm")
    const importCancelBtn = document.getElementById("settings-import-cancel")
    if (importDialog)
      attachDialogSpatialNav(importDialog, { defaultElement: "#settings-import-cancel" })

    function askImportConfirm() {
      return new Promise((resolve) => {
        if (!importDialog) {
          resolve(false)
          return
        }
        const cleanup = () => {
          importConfirmBtn?.removeEventListener("click", onYes)
          importCancelBtn?.removeEventListener("click", onNo)
          importDialog.removeEventListener("close", onClose)
          importDialog.removeEventListener("click", onBackdrop)
        }
        const onYes = () => {
          cleanup()
          importDialog.close?.()
          resolve(true)
        }
        const onNo = () => {
          cleanup()
          importDialog.close?.()
          resolve(false)
        }
        const onClose = () => {
          cleanup()
          resolve(false)
        }
        const onBackdrop = (event) => {
          if (event.target === importDialog) onNo()
        }
        importConfirmBtn?.addEventListener("click", onYes)
        importCancelBtn?.addEventListener("click", onNo)
        importDialog.addEventListener("close", onClose)
        importDialog.addEventListener("click", onBackdrop)
        if (typeof importDialog.showModal === "function") importDialog.showModal()
        else importDialog.setAttribute("open", "")
        requestAnimationFrame(() => {
          window.SpatialNavigation?.makeFocusable?.()
          importCancelBtn?.focus?.()
        })
      })
    }

    bindBackupRestore(importBtn, {
      fileInput: importInput,
      logTag: "settings:import",
      confirm: askImportConfirm,
      successDuration: 6000,
      onRestored: async () => {
        flashMsg(backupMsgEl, "Imported.")
        // Force a hub/sidebar refresh by re-rendering playlists.
        await renderPlaylists()
        syncCache()
      },
    })

    function openResetDialog() {
      if (!resetDialog) return
      if (typeof resetDialog.showModal === "function") resetDialog.showModal()
      else resetDialog.setAttribute("open", "")
      requestAnimationFrame(() => {
        window.SpatialNavigation?.makeFocusable?.()
        resetCancelBtn?.focus?.()
      })
    }

    function closeResetDialog() {
      resetDialog?.close?.()
    }

    resetBtn?.addEventListener("click", openResetDialog)
    resetCancelBtn?.addEventListener("click", closeResetDialog)
    resetDialog?.addEventListener("click", (e) => {
      if (e.target === resetDialog) closeResetDialog()
    })
    resetDialog?.addEventListener("close", () => resetBtn?.focus())

    resetConfirmBtn?.addEventListener("click", async () => {
      if (!resetConfirmBtn) return
      setLoading(resetConfirmBtn, true)
      try {
        try {
          await saveBackupSnapshot()
        } catch (err) {
          log.warn("[settings:reset] backup snapshot failed:", err)
        }
        try { await clearAllCache() } catch {}
        try { localStorage.clear() } catch {}
        try { sessionStorage.clear() } catch {}
        try {
          const all = document.cookie.split(";")
          for (const c of all) {
            const eq = c.indexOf("=")
            const name = (eq > -1 ? c.slice(0, eq) : c).trim()
            if (!name) continue
            document.cookie = `${name}=; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=/`
          }
        } catch {}

        if (isTauri) {
          try {
            const store = await Store.load(".xtream.creds.json")
            const keys = await store.keys()
            for (const k of keys) await store.delete(k)
            await store.save()
          } catch (e) {
            log.warn("Tauri store reset failed:", e)
          }
        }

        if (resetMsgEl) {
          resetMsgEl.textContent = "Reset complete. Reloading…"
          resetMsgEl.classList.remove("opacity-0")
        }
        closeResetDialog()
        // Hard reload so module-level caches in stream.js / movies.js / etc.
        // are re-initialised from the now-empty stores.
        setTimeout(() => {
          window.location.href = "/"
        }, 400)
      } finally {
        setLoading(resetConfirmBtn, false)
      }
    })

    async function refreshRestoreLastVisibility() {
      const available = await hasBackupSnapshot()
      if (restoreLastBtn) restoreLastBtn.hidden = !available
      if (restoreLastHelperEl) restoreLastHelperEl.hidden = !available
    }
    refreshRestoreLastVisibility()

    restoreLastBtn?.addEventListener("click", async () => {
      if (!(await askImportConfirm())) return
      setLoading(restoreLastBtn, true)
      try {
        const text = await loadBackupSnapshot()
        if (!text) {
          toastError(t("settings.toast.backupRestoreFail"), {
            description: t("settings.danger.restoreLastMissing"),
          })
          return
        }
        let parsed
        try {
          parsed = JSON.parse(text)
        } catch (parseError) {
          log.warn("[settings:restore-last] backup JSON parse failed:", parseError)
          toastError(t("settings.toast.backupParseFail"), { description: "Not valid JSON." })
          return
        }
        const { importAll } = await import("@/scripts/lib/backup.js")
        const summary = await importAll(parsed)
        toastSuccess(t("settings.toast.backupRestored"), {
          description: `${summary.playlists} playlist(s), ${summary.prefsPlaylists} preference set(s).`,
          duration: 6000,
        })
        flashMsg(backupMsgEl, "Imported.")
        await renderPlaylists()
        syncCache()
      } catch (error) {
        log.error("[settings:restore-last] backup import failed:", error)
        toastError(t("settings.toast.backupRestoreFail"), {
          description: (error && error.message) || "See console.",
        })
      } finally {
        setLoading(restoreLastBtn, false)
      }
    })

    document.addEventListener("xt:active-changed", () => {
      renderPlaylists()
      syncCache()
    })
    document.addEventListener("xt:entries-updated", () => {
      renderPlaylists()
      syncCache()
    })
    document.addEventListener("xt:catalog-warmed", () => {
      renderPlaylists()
      syncCache()
    })

    renderPlaylists()
    syncCache()

    // ---------------------------------------------------------------------
    // Font scale - applied inline in <head> on every page load (see
    // Layout.astro). Here we just expose the picker UI and persist changes.
    // ---------------------------------------------------------------------
    const fontScaleGroup = document.getElementById("settings-font-scale")
    function readFontScale() {
      const s = parseFloat(localStorage.getItem("xt_font_scale") || "1")
      return Number.isFinite(s) && s >= 0.75 && s <= 2 ? s : 1
    }
    /** @type {number | null} */
    let fontScaleAnimTimer = null
    function applyFontScale(s) {
      const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches
      const root = document.documentElement
      if (!reduceMotion) {
        root.classList.add("xt-fontscale-animating")
        if (fontScaleAnimTimer !== null) window.clearTimeout(fontScaleAnimTimer)
        fontScaleAnimTimer = window.setTimeout(() => {
          root.classList.remove("xt-fontscale-animating")
          fontScaleAnimTimer = null
        }, 420)
      }
      root.style.setProperty("--xt-font-scale", String(s))
      try {
        if (s === 1) localStorage.removeItem("xt_font_scale")
        else localStorage.setItem("xt_font_scale", String(s))
      } catch {}
      syncFontScaleButtons()
    }
    function syncFontScaleButtons() {
      const current = readFontScale()
      for (const btn of fontScaleGroup?.querySelectorAll(".font-scale-btn") ||
        []) {
        const v = parseFloat(btn.dataset.scale || "1")
        btn.setAttribute("aria-pressed", String(Math.abs(v - current) < 0.001))
      }
    }
    fontScaleGroup?.addEventListener("click", (e) => {
      const t = /** @type {HTMLElement} */ (e.target)
      const btn = t.closest(".font-scale-btn")
      if (!btn) return
      const v = parseFloat(btn.dataset.scale || "1")
      if (!Number.isFinite(v)) return
      applyFontScale(v)
    })
    syncFontScaleButtons()

    // ---------------------------------------------------------------------
    // Density: applied inline in <head> on every page load (see
    // Layout.astro). Here we just expose the picker UI and persist changes.
    // No preview/animation logic - density flips are discrete.
    // ---------------------------------------------------------------------
    const densityGroup = document.getElementById("settings-density")
    function syncDensityButtons() {
      const currentDensity = getDensity()
      for (const densityButton of densityGroup?.querySelectorAll(".density-btn") ||
        []) {
        densityButton.setAttribute(
          "aria-pressed",
          String(densityButton.dataset.density === currentDensity)
        )
      }
    }
    densityGroup?.addEventListener("click", (event) => {
      const clickedElement = /** @type {HTMLElement} */ (event.target)
      const densityButton = clickedElement.closest(".density-btn")
      if (!densityButton) return
      setDensity(densityButton.dataset.density)
      syncDensityButtons()
    })
    syncDensityButtons()

    // ---------------------------------------------------------------------
    // Theme: light / dark / system. Persisted to xt_theme; applied by the
    // inline script in <head> on page load. CSS uses `light-dark()` so the
    // single `colorScheme` property on :root drives the whole palette.
    // ---------------------------------------------------------------------
    const themeGroup = document.getElementById("settings-theme")
    function readTheme() {
      try {
        return localStorage.getItem("xt_theme") || "system"
      } catch {
        return "system"
      }
    }
    function commitTheme(t) {
      if (t === "light") document.documentElement.style.colorScheme = "light"
      else if (t === "dark") document.documentElement.style.colorScheme = "dark"
      else document.documentElement.style.colorScheme = ""
      try {
        localStorage.setItem("xt_theme", t)
      } catch {}
      try {
        window.__applyAndroidStatusBar?.(t)
      } catch {}
      syncThemeButtons()
    }
    function applyTheme(t, originX, originY) {
      const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches
      const supportsVT = typeof document.startViewTransition === "function"
      if (!supportsVT || reduceMotion) {
        commitTheme(t)
        return
      }
      const root = document.documentElement
      const x = Number.isFinite(originX) ? originX : window.innerWidth / 2
      const y = Number.isFinite(originY) ? originY : window.innerHeight / 2
      const farX = Math.max(x, window.innerWidth - x)
      const farY = Math.max(y, window.innerHeight - y)
      const radius = Math.hypot(farX, farY)
      root.style.setProperty("--xt-theme-x", `${x}px`)
      root.style.setProperty("--xt-theme-y", `${y}px`)
      root.style.setProperty("--xt-theme-r", `${radius}px`)
      root.classList.add("xt-theme-switching")
      const transition = document.startViewTransition(() => commitTheme(t))
      transition.finished.finally(() => {
        root.classList.remove("xt-theme-switching")
        root.style.removeProperty("--xt-theme-x")
        root.style.removeProperty("--xt-theme-y")
        root.style.removeProperty("--xt-theme-r")
      })
    }
    function syncThemeButtons() {
      const current = readTheme()
      for (const btn of themeGroup?.querySelectorAll(".theme-btn") || []) {
        btn.setAttribute("aria-checked", String(btn.dataset.theme === current))
      }
    }
    themeGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".theme-btn")
      if (!btn) return
      applyTheme(btn.dataset.theme || "system", event.clientX, event.clientY)
    })
    syncThemeButtons()

    // ---------------------------------------------------------------------
    // Accent color: persisted to xt_accent, applied via `data-accent` on <html>.
    // ---------------------------------------------------------------------
    const accentGroup = document.getElementById("settings-accent")
    function syncAccentButtons() {
      const current = getAccent()
      for (const btn of accentGroup?.querySelectorAll(".accent-btn") || []) {
        btn.setAttribute("aria-checked", String(btn.dataset.accentValue === current))
      }
    }
    accentGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".accent-btn")
      if (!btn) return
      setAccent(btn.dataset.accentValue || "fuchsia")
      syncAccentButtons()
    })
    syncAccentButtons()

    // Hint that the active playlist overrides this, so the picker doesn't look broken.
    const accentOverrideHint = document.getElementById("settings-accent-override-hint")
    async function syncAccentOverrideHint() {
      if (!accentOverrideHint) return
      const active = await getActiveEntry()
      const hasOverride =
        typeof active?.accent === "string" && ACCENT_PRESETS.includes(active.accent)
      accentOverrideHint.classList.toggle("hidden", !hasOverride)
    }
    document.addEventListener("xt:active-changed", syncAccentOverrideHint)
    document.addEventListener("xt:entries-updated", syncAccentOverrideHint)
    syncAccentOverrideHint()

    // ---------------------------------------------------------------------
    // Performance mode: persisted to xt_perf_mode; the inline script in
    // Layout.astro mirrors it to `data-perf-mode` on <html> before paint
    // so the brand-mark + entry animations don't flash on first frame.
    // focus-glide.ts re-attaches/detaches its listeners on
    // `xt:perf-mode-changed`, so toggling here takes effect immediately.
    // ---------------------------------------------------------------------
    const perfModeGroup = document.getElementById("settings-perf-mode")
    function syncPerfModeButtons() {
      const current = getPerfMode()
      for (const btn of perfModeGroup?.querySelectorAll(".perf-mode-btn") || []) {
        const isOn = btn.dataset.perf === "on"
        btn.setAttribute("aria-pressed", String(isOn === current))
      }
    }
    perfModeGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".perf-mode-btn")
      if (!btn) return
      setPerfMode(btn.dataset.perf === "on")
      syncPerfModeButtons()
    })
    syncPerfModeButtons()

    // ---------------------------------------------------------------------
    // Interface sounds
    // ---------------------------------------------------------------------
    const uiSoundsGroup = document.getElementById("settings-ui-sounds")
    function syncUiSoundsButtons() {
      const current = getUiSoundsEnabled()
      for (const btn of uiSoundsGroup?.querySelectorAll(".ui-sounds-btn") || []) {
        const isOn = btn.dataset.sounds === "on"
        btn.setAttribute("aria-pressed", String(isOn === current))
      }
    }
    uiSoundsGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".ui-sounds-btn")
      if (!btn) return
      const next = btn.dataset.sounds === "on"
      setUiSoundsEnabled(next)
      syncUiSoundsButtons()
      if (next) void playUiSound("confirm")
    })
    syncUiSoundsButtons()

    // ---------------------------------------------------------------------
    // Touch feedback (Android / touch devices only)
    // ---------------------------------------------------------------------
    const hapticsCard = document.getElementById("card-haptics")
    const hapticsGroup = document.getElementById("settings-haptics")
    if (hapticsCard && hapticsAvailable()) {
      hapticsCard.classList.remove("hidden")
    }
    function syncHapticsButtons() {
      const current = getHapticsEnabled()
      for (const btn of hapticsGroup?.querySelectorAll(".haptics-btn") || []) {
        const isOn = btn.dataset.haptics === "on"
        btn.setAttribute("aria-pressed", String(isOn === current))
      }
    }
    hapticsGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".haptics-btn")
      if (!btn) return
      const next = btn.dataset.haptics === "on"
      setHapticsEnabled(next)
      syncHapticsButtons()
      if (next) performHaptic("confirm")
    })
    syncHapticsButtons()

    // ---------------------------------------------------------------------
    // Rendering (Linux desktop only): WebKitGTK DMA-BUF safe mode
    // ---------------------------------------------------------------------
    const renderingCard = document.getElementById("card-rendering")
    const renderingStatusEl = document.getElementById("rendering-status")
    const renderingSelectEl = /** @type {HTMLSelectElement|null} */ (
      document.getElementById("rendering-select")
    )
    const renderingEnvOverrideNoteEl = document.getElementById("rendering-env-override-note")
    const renderingRestartNoteEl = document.getElementById("rendering-restart-note")
    const renderingRestartBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("rendering-restart")
    )

    function renderingDetectionLabel(detection) {
      if (detection === "raspberry-pi") return t("settings.rendering.detectionRaspberryPi")
      if (detection === "nvidia") return t("settings.rendering.detectionNvidia")
      if (detection === "vm") return t("settings.rendering.detectionVm")
      return ""
    }

    async function initRenderingCard() {
      const state = await getCompositingState()
      if (!renderingCard || !state?.platformSupported) return
      renderingCard.classList.remove("hidden")

      if (renderingSelectEl) {
        renderingSelectEl.value = state.setting === "fast-trial" ? "fast" : state.setting
      }
      if (renderingStatusEl) {
        const detectionLabel = renderingDetectionLabel(state.detection)
        if (state.active === "safe") {
          renderingStatusEl.textContent = detectionLabel
            ? t("settings.rendering.statusSafeWithDetection", { detection: detectionLabel })
            : t("settings.rendering.statusSafe")
        } else {
          renderingStatusEl.textContent = detectionLabel
            ? t("settings.rendering.statusFastWithDetection", { detection: detectionLabel })
            : t("settings.rendering.statusFast")
        }
      }
      if (state.envOverride) {
        renderingSelectEl?.setAttribute("disabled", "")
        renderingEnvOverrideNoteEl?.removeAttribute("hidden")
      }

      renderingSelectEl?.addEventListener("change", async () => {
        const chosen = renderingSelectEl.value
        const isTrial = chosen === "fast"
        try {
          await setCompositingSetting(isTrial ? "fast-trial" : chosen)
        } catch (err) {
          log.error("Rendering setting update failed:", err)
          return
        }
        if (renderingRestartNoteEl) {
          renderingRestartNoteEl.textContent = isTrial
            ? t("settings.rendering.trialRestartNote")
            : t("settings.rendering.restartNote")
          renderingRestartNoteEl.removeAttribute("hidden")
        }
        renderingRestartBtn?.removeAttribute("hidden")
      })

      renderingRestartBtn?.addEventListener("click", async () => {
        try {
          await relaunchForCompositing()
        } catch (err) {
          log.error("Rendering relaunch failed:", err)
        }
      })
    }
    initRenderingCard()

    // ---------------------------------------------------------------------
    // Mono audio
    // ---------------------------------------------------------------------
    const monoAudioGroup = document.getElementById("settings-mono-audio")
    function syncMonoAudioButtons() {
      const current = getMonoAudioEnabled()
      for (const btn of monoAudioGroup?.querySelectorAll(".mono-audio-btn") || []) {
        const isOn = btn.dataset.mono === "on"
        btn.setAttribute("aria-pressed", String(isOn === current))
      }
    }
    monoAudioGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".mono-audio-btn")
      if (!btn) return
      setMonoAudioEnabled(btn.dataset.mono === "on")
      syncMonoAudioButtons()
    })
    syncMonoAudioButtons()

    // ---------------------------------------------------------------------
    // Captions on by default
    // ---------------------------------------------------------------------
    const captionsAutoGroup = document.getElementById("settings-captions-auto")
    function syncCaptionsAutoButtons() {
      const current = getCaptionsAutoEnabled()
      for (const btn of captionsAutoGroup?.querySelectorAll(".captions-auto-btn") || []) {
        const isOn = btn.dataset.captions === "on"
        btn.setAttribute("aria-pressed", String(isOn === current))
      }
    }
    captionsAutoGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".captions-auto-btn")
      if (!btn) return
      setCaptionsAutoEnabled(btn.dataset.captions === "on")
      syncCaptionsAutoButtons()
    })
    syncCaptionsAutoButtons()

    const langGroupingSelectEl = /** @type {HTMLSelectElement|null} */ (
      document.getElementById("lang-grouping-select")
    )
    if (langGroupingSelectEl) langGroupingSelectEl.value = getLanguageGroupingEnabled() ? "on" : "off"
    langGroupingSelectEl?.addEventListener("change", () => {
      setLanguageGroupingEnabled(langGroupingSelectEl.value === "on")
    })

    const contentLangSelectEl = /** @type {HTMLSelectElement|null} */ (
      document.getElementById("content-lang-select")
    )
    // initI18n() may not have settled yet, so await it before reading locale-dependent labels.
    async function populateContentLangSelect() {
      if (!contentLangSelectEl) return
      await initI18n()
      contentLangSelectEl.replaceChildren()

      const autoOption = document.createElement("option")
      autoOption.value = ""
      autoOption.textContent = t("settings.contentLang.auto")
      contentLangSelectEl.appendChild(autoOption)

      const activeLocale = getActiveLocale()
      const languageOptions = Object.keys(LANGUAGE_TOKENS)
        .map((tag) => ({ tag, label: languageTagLabel(tag, activeLocale) }))
        .sort((firstOption, secondOption) => firstOption.label.localeCompare(secondOption.label, activeLocale))
      for (const languageOption of languageOptions) {
        const option = document.createElement("option")
        option.value = languageOption.tag
        option.textContent = `${languageOption.label} (${languageOption.tag})`
        contentLangSelectEl.appendChild(option)
      }

      contentLangSelectEl.value = getContentLanguage()
    }
    contentLangSelectEl?.addEventListener("change", () => {
      setContentLanguage(contentLangSelectEl.value)
    })
    document.addEventListener(LOCALE_EVENT, populateContentLangSelect)
    populateContentLangSelect()

    // ---------------------------------------------------------------------
    // Audio transcode auto-fix: desktop-only section (visible whether or not
    // ffmpeg is currently resolvable - it's the recovery UI for a missing
    // or broken ffmpeg, so it can't gate on the probe result).
    // ---------------------------------------------------------------------
    const audioTranscodeSection = document.getElementById("audio-transcode-section")
    const audioTranscodeGroup = document.getElementById("settings-audio-transcode-auto")
    const ffmpegStatusEl = document.getElementById("ffmpeg-status")
    const ffmpegPathInput = /** @type {HTMLInputElement|null} */ (document.getElementById("ffmpeg-path-input"))
    const ffmpegBrowseBtn = document.getElementById("ffmpeg-browse-btn")
    const ffmpegTestBtn = document.getElementById("ffmpeg-test-btn")

    function syncAudioTranscodeButtons() {
      const current = getAudioTranscodeAuto()
      for (const btn of audioTranscodeGroup?.querySelectorAll(".audio-transcode-btn") || []) {
        const isOn = btn.dataset.audioTranscode === "on"
        btn.setAttribute("aria-pressed", String(isOn === current))
      }
    }
    audioTranscodeGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".audio-transcode-btn")
      if (!btn) return
      setAudioTranscodeAuto(btn.dataset.audioTranscode === "on")
      syncAudioTranscodeButtons()
    })
    syncAudioTranscodeButtons()

    function ffmpegSourceLabel(source) {
      if (source === "custom") return t("settings.playback.ffmpegSourceCustom") || "custom"
      if (source === "system") return t("settings.playback.ffmpegSourceSystem") || "system"
      return t("settings.playback.ffmpegSourceBundled") || "bundled"
    }

    function stripErrorPrefix(raw) {
      const idx = raw.indexOf(":")
      return idx > 0 ? raw.slice(idx + 1) : raw
    }

    function renderFfmpegStatus(status) {
      const available = !!status?.available
      for (const btn of audioTranscodeGroup?.querySelectorAll(".audio-transcode-btn") || []) {
        btn.disabled = !available
      }
      if (!ffmpegStatusEl) return
      if (available) {
        ffmpegStatusEl.textContent =
          t("settings.playback.ffmpegStatusReady", {
            version: status.version || "",
            source: ffmpegSourceLabel(status.source),
          }) || `Ready (${status.version || "unknown"}, ${ffmpegSourceLabel(status.source)})`
        ffmpegStatusEl.classList.remove("text-bad")
        ffmpegStatusEl.classList.add("text-ok")
      } else {
        const missing = t("settings.playback.ffmpegStatusMissing") || "FFmpeg not found. Install ffmpeg or set a custom path below."
        const detail = status?.customError ? stripErrorPrefix(status.customError) : ""
        ffmpegStatusEl.textContent = detail ? `${missing} (${detail})` : missing
        ffmpegStatusEl.classList.remove("text-ok")
        ffmpegStatusEl.classList.add("text-bad")
      }
    }

    async function refreshFfmpegStatus() {
      renderFfmpegStatus(await audioTranscodeStatus())
    }

    if (isTauriEnv && !isAndroidEnv) {
      audioTranscodeSection?.classList.remove("hidden")
      if (ffmpegPathInput) ffmpegPathInput.value = getFfmpegPath()
      refreshFfmpegStatus()

      let ffmpegPathSaveTimer = null
      function flushFfmpegPath() {
        if (ffmpegPathSaveTimer) {
          window.clearTimeout(ffmpegPathSaveTimer)
          ffmpegPathSaveTimer = null
        }
        if (ffmpegPathInput) setFfmpegPath(ffmpegPathInput.value)
      }

      ffmpegPathInput?.addEventListener("input", () => {
        if (ffmpegPathSaveTimer) window.clearTimeout(ffmpegPathSaveTimer)
        ffmpegPathSaveTimer = window.setTimeout(() => {
          ffmpegPathSaveTimer = null
          if (ffmpegPathInput) setFfmpegPath(ffmpegPathInput.value)
        }, 350)
      })
      ffmpegPathInput?.addEventListener("blur", flushFfmpegPath)

      ffmpegBrowseBtn?.addEventListener("click", async () => {
        try {
          const { open } = await import("@tauri-apps/plugin-dialog")
          const filters = /Win/i.test(navigator.userAgent || "")
            ? [{ name: "Executable", extensions: ["exe"] }]
            : undefined
          const picked = await open({ multiple: false, directory: false, filters })
          if (typeof picked !== "string" || !picked) return
          if (ffmpegPathInput) ffmpegPathInput.value = picked
          flushFfmpegPath()
          await refreshFfmpegStatus()
        } catch (err) {
          log.warn("[settings] ffmpeg browse failed:", err)
          toastError(t("settings.toast.pickFileFail"), {
            description: (err && err.message) || String(err) || "See console.",
          })
        }
      })

      ffmpegTestBtn?.addEventListener("click", async () => {
        flushFfmpegPath()
        await refreshFfmpegStatus()
      })
    }

    const hevcRow = document.getElementById("card-hevc")
    const hevcInstallBtn = document.getElementById("settings-hevc-install")
    const hevcStatus = document.getElementById("settings-hevc-status")
    function renderHevcRow() {
      const hevcSupported = deviceSupportsHevc()
      if (hevcStatus) hevcStatus.textContent = hevcSupported ? (t("settings.playback.hevcSupported") || "Already supported on this device.") : ""
      if (hevcInstallBtn) hevcInstallBtn.classList.toggle("hidden", hevcSupported)
    }
    if (isWindowsDesktopEnv()) {
      hevcRow?.classList.remove("hidden")
      renderHevcRow()
      hevcInstallBtn?.addEventListener("click", async () => {
        hevcInstallBtn.disabled = true
        try {
          await ensureHevcDecodable({ generic: true })
        } finally {
          hevcInstallBtn.disabled = false
        }
        renderHevcRow()
      })
    }

    // ---------------------------------------------------------------------
    // Continue Watching retention: 30 / 90 / 180 days or Never. Read by
    // trimBucket() in preferences.js on every progress save, so flipping
    // here takes effect on the next playback tick - no reload required.
    // ---------------------------------------------------------------------
    const retentionGroup = document.getElementById("settings-progress-retention")
    function syncRetentionButtons() {
      const current = String(getProgressRetentionDays())
      for (const btn of retentionGroup?.querySelectorAll(".retention-btn") || []) {
        btn.setAttribute(
          "aria-pressed",
          String(btn.dataset.retention === current)
        )
      }
    }
    retentionGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".retention-btn")
      if (!btn) return
      setProgressRetentionDays(Number(btn.dataset.retention))
      syncRetentionButtons()
    })
    syncRetentionButtons()

    // ---------------------------------------------------------------------
    // Live TV layout: default EPG visibility + channel column width.
    // ---------------------------------------------------------------------
    const epgDefaultGroup = document.getElementById("settings-epg-default")
    function readEpgDefault() {
      try {
        const v = localStorage.getItem("xt_epg_open_default")
        return v === null ? true : v === "true"
      } catch {
        return true
      }
    }
    function applyEpgDefault(on) {
      try {
        localStorage.setItem("xt_epg_open_default", String(on))
        // Reset the per-session override so the new default applies on the
        // next visit to /livetv.
        localStorage.removeItem("xt_epg_visible")
      } catch {}
      syncEpgDefaultButtons()
    }
    function syncEpgDefaultButtons() {
      const on = readEpgDefault()
      for (const btn of epgDefaultGroup?.querySelectorAll(".epg-default-btn") ||
        []) {
        const isOn = btn.dataset.epg === "on"
        btn.setAttribute("aria-pressed", String(isOn === on))
      }
    }
    epgDefaultGroup?.addEventListener("click", (e) => {
      const t = /** @type {HTMLElement} */ (e.target)
      const btn = t.closest(".epg-default-btn")
      if (!btn) return
      applyEpgDefault(btn.dataset.epg === "on")
    })
    syncEpgDefaultButtons()

    const channelWidthGroup = document.getElementById("settings-channel-width")
    const CHANNEL_WIDTH_DEFAULT = 28
    function readChannelWidth() {
      const raw = parseFloat(localStorage.getItem("xt_channels_w") || "")
      return Number.isFinite(raw) && raw >= 18 && raw <= 60
        ? raw
        : CHANNEL_WIDTH_DEFAULT
    }
    function applyChannelWidth(rem) {
      document.documentElement.style.setProperty("--xt-channels-w", `${rem}rem`)
      try {
        if (rem === CHANNEL_WIDTH_DEFAULT) localStorage.removeItem("xt_channels_w")
        else localStorage.setItem("xt_channels_w", String(rem))
      } catch {}
      syncChannelWidthButtons()
    }
    function syncChannelWidthButtons() {
      const current = readChannelWidth()
      for (const btn of channelWidthGroup?.querySelectorAll(".channel-width-btn") ||
        []) {
        const v = parseFloat(btn.dataset.width || "0")
        btn.setAttribute("aria-pressed", String(Math.abs(v - current) < 0.001))
      }
    }
    channelWidthGroup?.addEventListener("click", (e) => {
      const t = /** @type {HTMLElement} */ (e.target)
      const btn = t.closest(".channel-width-btn")
      if (!btn) return
      const v = parseFloat(btn.dataset.width || "0")
      if (!Number.isFinite(v) || v <= 0) return
      applyChannelWidth(v)
    })
    syncChannelWidthButtons()

    // ---------------------------------------------------------------------
    // EPG time zone offset (per active playlist)
    // ---------------------------------------------------------------------
    const tzSelect = /** @type {HTMLSelectElement|null} */ (
      document.getElementById("epg-tz-select")
    )
    const tzStatus = document.getElementById("epg-tz-status")
    if (tzSelect) {
      tzSelect.replaceChildren()
      const autoOpt = document.createElement("option")
      autoOpt.value = "auto"
      autoOpt.textContent = "Auto-detect"
      tzSelect.appendChild(autoOpt)
      for (let mins = -12 * 60; mins <= 14 * 60; mins += 30) {
        const sign = mins < 0 ? "-" : "+"
        const abs = Math.abs(mins)
        const h = Math.floor(abs / 60)
        const m = abs % 60
        const label = `UTC${sign}${String(h).padStart(2, "0")}:${String(m).padStart(2, "0")}`
        const opt = document.createElement("option")
        opt.value = String(mins)
        opt.textContent = label
        tzSelect.appendChild(opt)
      }
    }

    let tzActiveId = ""
    async function syncTz() {
      const active = await getActiveEntry()
      if (!tzSelect || !tzStatus) return
      if (!active) {
        tzActiveId = ""
        tzSelect.disabled = true
        tzSelect.value = "auto"
        tzStatus.textContent = "No playlist selected."
        return
      }
      tzActiveId = active._id
      tzSelect.disabled = false
      const setting = getOffsetSetting(active._id)
      tzSelect.value = setting === "auto" ? "auto" : String(setting)
      const state = getProgrammesSync(active._id)
      if (state) {
        const sign = state.offsetMin < 0 ? "-" : "+"
        const abs = Math.abs(state.offsetMin)
        const label = `UTC${sign}${String(Math.floor(abs / 60)).padStart(2, "0")}:${String(abs % 60).padStart(2, "0")}`
        tzStatus.textContent = state.offsetIsAuto
          ? `Auto-detected ${label}.`
          : `Using manual ${label}.`
      } else {
        tzStatus.textContent =
          "EPG hasn't been loaded for this playlist yet - open Live TV or the EPG grid first."
      }
    }
    tzSelect?.addEventListener("change", () => {
      if (!tzActiveId) return
      const raw = tzSelect.value
      setOffsetSetting(tzActiveId, raw === "auto" ? "auto" : Number(raw))
      if (tzStatus) tzStatus.textContent = "Saved. Applies next time EPG loads."
    })
    document.addEventListener("xt:active-changed", syncTz)
    document.addEventListener("xt:epg-loaded", syncTz)
    document.addEventListener("xt:epg-offset-changed", syncTz)
    syncTz()

    // ---------------------------------------------------------------------
    // Network / User-Agent
    // ---------------------------------------------------------------------
    const uaPresetEl = /** @type {HTMLSelectElement|null} */ (
      document.getElementById("ua-preset")
    )
    const uaCustomEl = /** @type {HTMLInputElement|null} */ (
      document.getElementById("ua-custom")
    )
    const uaSaveBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("ua-save")
    )
    const uaMsgEl = document.getElementById("ua-msg")

    if (uaPresetEl) {
      for (const p of UA_PRESETS) {
        const opt = document.createElement("option")
        opt.value = p.id
        opt.textContent = p.label
        uaPresetEl.appendChild(opt)
      }
      const opt = document.createElement("option")
      opt.value = "custom"
      opt.textContent = "Custom"
      uaPresetEl.appendChild(opt)

      const current = getUserAgent()
      const matchPreset = UA_PRESETS.find((p) => p.value === current)
      if (current && !matchPreset) {
        uaPresetEl.value = "custom"
        if (uaCustomEl) uaCustomEl.value = current
      } else {
        uaPresetEl.value = matchPreset ? matchPreset.id : "default"
        if (uaCustomEl) uaCustomEl.value = ""
      }
    }

    function readUaFromForm() {
      if (!uaPresetEl) return ""
      const custom = (uaCustomEl?.value || "").trim()
      if (custom) return custom
      if (uaPresetEl.value === "custom") return ""
      const preset = UA_PRESETS.find((p) => p.id === uaPresetEl.value)
      return preset ? preset.value : ""
    }

    uaSaveBtn?.addEventListener("click", () => {
      setUserAgent(readUaFromForm())
      flashMsg(
        uaMsgEl,
        isTauri
          ? "Saved. Reload pages to apply."
          : "Saved. Active in the desktop build only."
      )
      flashSuccess(uaSaveBtn)
    })

    // ---------------------------------------------------------------------
    // Network timeout: how long providerFetch waits before aborting. The
    // setting is read on every fetch, so the change takes effect on the
    // next catalog refresh without a reload.
    // ---------------------------------------------------------------------
    const netTimeoutGroup = document.getElementById("settings-network-timeout")
    function syncNetTimeoutButtons() {
      const current = String(getNetworkTimeoutSeconds())
      for (const btn of netTimeoutGroup?.querySelectorAll(".net-timeout-btn") || []) {
        btn.setAttribute(
          "aria-checked",
          String(btn.dataset.timeout === current)
        )
      }
    }
    netTimeoutGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".net-timeout-btn")
      if (!btn) return
      setNetworkTimeoutSeconds(Number(btn.dataset.timeout))
      syncNetTimeoutButtons()
    })
    syncNetTimeoutButtons()

    // ---------------------------------------------------------------------
    // Developer mode: unlocks the network log card below.
    // ---------------------------------------------------------------------
    const devModeGroup = document.getElementById("settings-dev-mode")
    function syncDevModeButtons() {
      const current = getDevModeEnabled()
      for (const btn of devModeGroup?.querySelectorAll(".dev-mode-btn") || []) {
        btn.setAttribute("aria-checked", String((btn.dataset.dev === "on") === current))
      }
    }
    function applyDevModeVisibility() {
      document.getElementById("card-net-log")?.classList.toggle("hidden", !getDevModeEnabled())
    }
    devModeGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".dev-mode-btn")
      if (!btn) return
      setDevModeEnabled(btn.dataset.dev === "on")
      syncDevModeButtons()
      applyDevModeVisibility()
    })
    document.addEventListener(DEV_MODE_EVENT, () => {
      syncDevModeButtons()
      applyDevModeVisibility()
    })
    syncDevModeButtons()
    applyDevModeVisibility()

    // ---------------------------------------------------------------------
    // Downloads folder
    // ---------------------------------------------------------------------
    const dirInput = /** @type {HTMLInputElement|null} */ (
      document.getElementById("download-dir-input")
    )
    const dirPickBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("download-dir-pick")
    )
    const dirClearBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("download-dir-clear")
    )
    const downloadsAvailEl = document.getElementById("downloads-availability")

    const isAndroid = /Android/i.test(navigator.userAgent || "")

    function prettifyAndroidUri(stored) {
      if (!stored) return ""
      let raw = stored
      try {
        const parsed = JSON.parse(stored)
        if (parsed && typeof parsed.uri === "string") raw = parsed.uri
      } catch {}
      try {
        const u = decodeURIComponent(raw)
        const m = u.match(/document\/(?:tree\/)?([^/]+)$/) ||
                  u.match(/tree\/([^/]+)$/)
        if (m && m[1]) return m[1].replace(/^primary:/, "")
        return u
      } catch {
        return raw
      }
    }

    function syncDir() {
      if (!dirInput) return
      let v = getDownloadDir() || ""
      if (isAndroid && v === "[object Object]") {
        setDownloadDir("")
        v = ""
      }
      if (isAndroid) {
        dirInput.value = v
          ? prettifyAndroidUri(v)
          : "/Download/Extreme InfiniTV/ (default)"
      } else {
        dirInput.value = v
      }
      if (dirClearBtn) dirClearBtn.disabled = !v
    }
    syncDir()

    if (downloadsAvailEl) {
      downloadsAvailEl.textContent = isDownloadable() ? "" : "Desktop only"
    }
    if (!isDownloadable()) {
      if (dirPickBtn) dirPickBtn.disabled = true
      if (dirClearBtn) dirClearBtn.disabled = true
    }

    const openDownloadsBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("settings-open-downloads")
    )
    if (openDownloadsBtn && isTauriEnv && !isAndroidEnv) {
      openDownloadsBtn.classList.remove("hidden")
      openDownloadsBtn.addEventListener("click", async () => {
        try {
          let dir = getDownloadDir()
          if (!dir) {
            const { downloadDir } = await import("@tauri-apps/api/path")
            dir = await downloadDir()
          }
          const { openPath } = await import("@tauri-apps/plugin-opener")
          await openPath(dir)
        } catch (err) {
          log.warn("[settings] open downloads folder failed:", err)
          toastError(
            t("downloads.action.openFolderFail") || "Couldn't open the downloads folder."
          )
        }
      })
    }

    dirPickBtn?.addEventListener("click", async () => {
      if (!isDownloadable() || !dirPickBtn) return
      setLoading(dirPickBtn, true)
      try {
        if (isAndroid) {
          const { pickDirectory, serializeUri } = await import(
            "@/scripts/lib/android-fs.js"
          )
          const picked = await pickDirectory()
          if (picked) {
            setDownloadDir(serializeUri(picked))
            syncDir()
          }
        } else {
          const { open } = await import("@tauri-apps/plugin-dialog")
          const picked = await open({
            directory: true,
            title: "Choose download folder",
          })
          if (typeof picked === "string" && picked) {
            setDownloadDir(picked)
            syncDir()
          }
        }
      } catch (e) {
        if (e && e.name === "NoDirectoryPickerError") {
          // Nothing to pick with, but the public Downloads folder still works.
          if (dirPickBtn) dirPickBtn.disabled = true
          toast({
            title: t("settings.toast.noFolderPicker"),
            description: t("settings.toast.noFolderPickerHint"),
            duration: 7000,
          })
        } else {
          log.error("Pick folder failed:", e)
          toastError(t("settings.toast.pickFolderFail"), {
            description: (e && e.message) || String(e) || "See console.",
            duration: 7000,
          })
        }
      } finally {
        setLoading(dirPickBtn, false)
      }
    })

    const scanBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("downloads-scan")
    )
    const scanMsgEl = document.getElementById("downloads-scan-msg")
    if (!isDownloadable() || isAndroid) {
      scanBtn?.setAttribute("disabled", "")
      if (scanBtn) {
        scanBtn.classList.add("opacity-50", "cursor-not-allowed")
        scanBtn.title = "Folder scan is desktop-only."
      }
    }
    scanBtn?.addEventListener("click", async () => {
      if (!scanBtn) return
      setLoading(scanBtn, true)
      try {
        const summary = await scanDownloadsFolder()
        if (summary.error) {
          toastError(t("settings.toast.scanFail"), { description: summary.error })
          flashMsg(scanMsgEl, summary.error)
        } else if (summary.imported > 0) {
          toastSuccess(t("settings.toast.imported", { count: summary.imported }), {
            description: `Scanned ${summary.scanned} sidecar${summary.scanned === 1 ? "" : "s"}.`,
          })
          flashMsg(scanMsgEl, `Imported ${summary.imported} item(s).`)
          flashSuccess(scanBtn)
        } else if (summary.scanned === 0) {
          flashMsg(scanMsgEl, "No `.xtmeta.json` sidecars found in this folder.")
        } else {
          flashMsg(
            scanMsgEl,
            `Already up to date. ${summary.skipped} skipped, ${summary.missing} missing media.`
          )
        }
      } catch (error) {
        log.error("[settings:scan] failed:", error)
        toastError(t("settings.toast.scanFail"), {
          description: (error && error.message) || "See console.",
        })
      } finally {
        setLoading(scanBtn, false)
      }
    })

    dirClearBtn?.addEventListener("click", async () => {
      const prev = getDownloadDir()
      if (isAndroid && prev) {
        const { releasePickedDir, deserializeUri } = await import(
          "@/scripts/lib/android-fs.js"
        )
        const uri = deserializeUri(prev)
        if (uri) releasePickedDir(uri).catch(() => {})
      }
      setDownloadDir("")
      syncDir()
    })

    const concurrencyGroup = document.getElementById(
      "settings-download-concurrency"
    )
    const concurrencyCapEl = document.getElementById(
      "settings-download-concurrency-cap"
    )

    function readSelectedPlaylistId() {
      try {
        const active = JSON.parse(
          localStorage.getItem("xt_playlists") || '{"selectedId":""}'
        )
        return active?.selectedId || ""
      } catch {
        return ""
      }
    }
    function readProviderCap() {
      // 0 means "unknown" (M3U source or cache cold). UI falls back to the
      // previous behaviour: all four buttons enabled, no cap message.
      return getMaxConnectionsSync(readSelectedPlaylistId())
    }
    function readActiveCons() {
      return getActiveConnectionsSync(readSelectedPlaylistId())
    }

    function syncConcurrencyButtons() {
      if (!concurrencyGroup) return
      const current = getDownloadConcurrency()
      const cap = readProviderCap()
      for (const btn of concurrencyGroup.querySelectorAll(".dl-concurrency-btn")) {
        const v = parseInt(btn.getAttribute("data-concurrency") || "1", 10)
        btn.setAttribute("aria-pressed", v === current ? "true" : "false")
        const exceedsCap = cap > 0 && v > cap
        btn.disabled = exceedsCap
        btn.classList.toggle("opacity-50", exceedsCap)
        btn.classList.toggle("cursor-not-allowed", exceedsCap)
        btn.title = exceedsCap
          ? `Your provider only allows ${cap} connection${cap === 1 ? "" : "s"}.`
          : ""
      }
      if (concurrencyCapEl) {
        if (cap <= 0) {
          concurrencyCapEl.textContent = ""
        } else {
          const activeCons = readActiveCons()
          const slots = `${cap} connection${cap === 1 ? "" : "s"}`
          concurrencyCapEl.textContent = activeCons > 0
            ? `Provider allows ${slots} (${activeCons} in use).`
            : `Provider allows ${slots}.`
        }
      }
    }
    syncConcurrencyButtons()
    concurrencyGroup?.addEventListener("click", (e) => {
      const btn = /** @type {HTMLElement} */ (e.target).closest(
        ".dl-concurrency-btn"
      )
      if (!btn || btn.disabled) return
      const v = parseInt(btn.getAttribute("data-concurrency") || "1", 10)
      setDownloadConcurrency(v)
      syncConcurrencyButtons()
    })

    const nfoCard = document.getElementById("card-write-nfo")
    if (nfoCard && isTauriEnv && !isAndroidEnv) nfoCard.classList.remove("hidden")

    const writeNfoGroup = document.getElementById("settings-write-nfo")
    function syncWriteNfoButtons() {
      const current = getWriteNfoEnabled()
      for (const btn of writeNfoGroup?.querySelectorAll(".write-nfo-btn") || []) {
        const isOn = btn.dataset.nfo === "on"
        btn.setAttribute("aria-pressed", String(isOn === current))
      }
    }
    writeNfoGroup?.addEventListener("click", (event) => {
      const target = /** @type {HTMLElement} */ (event.target)
      const btn = target.closest(".write-nfo-btn")
      if (!btn) return
      setWriteNfoEnabled(btn.dataset.nfo === "on")
      syncWriteNfoButtons()
    })
    syncWriteNfoButtons()

    // Refresh user_info on every active-playlist change and on Settings load
    // so the cap reflects the current provider, not whatever was warmed last.
    // The connection-limit banner subscribes to USER_INFO_LOADED_EVENT itself
    // (see ConnectionLimitBanner.svelte); we just kick the hydration.
    async function refreshUserInfoForActive() {
      try {
        const active = await getActiveEntry()
        if (!active) {
          syncConcurrencyButtons()
          return
        }
        const creds = await loadCreds()
        await ensureUserInfo(creds, active._id)
        syncConcurrencyButtons()
      } catch {
        syncConcurrencyButtons()
      }
    }
    refreshUserInfoForActive()
    document.addEventListener(USER_INFO_LOADED_EVENT, () => {
      syncConcurrencyButtons()
    })
    document.addEventListener("xt:active-changed", refreshUserInfoForActive)

    const updateSectionEl = document.getElementById("update-section")
    const updatePillEl = document.getElementById("update-pill")
    const updateCurrentEl = document.getElementById("update-current")
    const updateLatestEl = document.getElementById("update-latest")
    const updateNotesEl = document.getElementById("update-notes")
    const updateMsgEl = document.getElementById("update-msg")
    const updateGithubLink = /** @type {HTMLAnchorElement|null} */ (
      document.getElementById("update-github")
    )
    const updateCheckBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("update-check")
    )
    const updateInstallBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("update-install")
    )
    const updateInstallLabelEl = document.getElementById("update-install-label")
    const updateRelaunchBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("update-relaunch")
    )
    const updateGetBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("update-get")
    )
    const updateGetLabelEl = document.getElementById("update-get-label")
    const updateManualOnlyEl = document.getElementById("update-manual-only")
    const updateDegradedEl = document.getElementById("update-degraded")
    const updateProgressWrap = document.getElementById("update-progress-wrap")
    const updateProgressTrack = document.getElementById("update-progress-track")
    const updateProgressBar = document.getElementById("update-progress-bar")
    const updateProgressText = document.getElementById("update-progress-text")
    const updateChannelRowEl = document.getElementById("update-channel-row")
    const updateChannelSelectEl = /** @type {HTMLSelectElement|null} */ (
      document.getElementById("update-channel-select")
    )
    const autoUpdateRowEl = document.getElementById("auto-update-row")
    const autoUpdateSelectEl = /** @type {HTMLSelectElement|null} */ (
      document.getElementById("auto-update-select")
    )

    const GITHUB_RELEASES_URL =
      "https://github.com/infinitel8p/Extreme-InfiniTV/releases"

    function setUpdateState(state) {
      if (updateSectionEl) updateSectionEl.setAttribute("data-state", state)
      if (updatePillEl) {
        if (state === "available") {
          updatePillEl.classList.remove("hidden")
          updatePillEl.classList.add("inline-flex")
        } else {
          updatePillEl.classList.add("hidden")
          updatePillEl.classList.remove("inline-flex")
        }
      }
    }

    const isWindowsDesktop =
      isTauri && /Windows/i.test(navigator.userAgent || "")
    const isLinuxDesktop =
      isTauri &&
      /Linux/i.test(navigator.userAgent || "") &&
      !/Android/i.test(navigator.userAgent || "")
    const playStoreInstall = isPlayStoreInstall()
    // Snap/Flatpak: store owns updates, same as MS Store.
    const sandboxed = isLinuxDesktop ? sandboxRuntimeSync() : null
    // Resolved below; checkViaGithub/checkForUpdates close over these.
    let storeBuild = false
    let canUseTauriUpdater = false

    if (updateChannelSelectEl) updateChannelSelectEl.value = getUpdateChannel()
    if (autoUpdateSelectEl) autoUpdateSelectEl.value = getAutoUpdateEnabled() ? "on" : "off"

    if (updateCurrentEl) {
      import("@tauri-apps/api/app")
        .then(({ getVersion }) => getVersion())
        .then((version) => {
          updateCurrentEl.textContent = version
        })
        .catch(() => {
          const tag = document.querySelector(
            'meta[name="x-app-version"]'
          )?.getAttribute("content")
          updateCurrentEl.textContent = tag || "-"
        })
    }

    // Off the top-level await chain: a stuck isStoreBuild() IPC must not block this script.
    const updaterConfigReady = (async () => {
      storeBuild = await isStoreBuild()
      const noInAppUpdate = storeBuild || !!sandboxed
      // Linux falls back to the GitHub check below if `check()` throws (non-AppImage installs).
      canUseTauriUpdater = (isWindowsDesktop && !storeBuild) || (isLinuxDesktop && !sandboxed)

      if (noInAppUpdate) updateChannelRowEl?.setAttribute("hidden", "")
      if (!canUseTauriUpdater) autoUpdateRowEl?.setAttribute("hidden", "")

      if (!canUseTauriUpdater) {
        updateManualOnlyEl?.removeAttribute("hidden")
        // data-i18n stays in sync with locale changes; textContent updates it immediately.
        if (updateGetLabelEl && storeBuild) {
          updateGetLabelEl.setAttribute("data-i18n", "settings.update.getFromStore")
          updateGetLabelEl.textContent = t("settings.update.getFromStore")
        } else if (updateGetLabelEl && playStoreInstall) {
          updateGetLabelEl.setAttribute("data-i18n", "settings.update.getFromPlay")
          updateGetLabelEl.textContent = t("settings.update.getFromPlay")
        }
      }

      checkForUpdates()
    })().catch(() => {})

    let pendingUpdate = null
    // "plugin": pendingUpdate is a @tauri-apps/plugin-updater Update object.
    // "invoke": pendingUpdate is the {version, notes, pubDate} payload from updater_check_from (beta channel).
    let updateInstallMode = "plugin"

    let progressFrame = 0
    let queuedProgress = null

    function setProgress(pct, text) {
      if (progressFrame) {
        cancelAnimationFrame(progressFrame)
        progressFrame = 0
        queuedProgress = null
      }
      paintProgress(pct, text)
    }

    // Progress events can arrive far faster than frames; paint at most once per frame.
    function queueProgress(pct, text) {
      queuedProgress = { pct, text }
      if (progressFrame) return
      progressFrame = requestAnimationFrame(() => {
        progressFrame = 0
        if (!queuedProgress) return
        paintProgress(queuedProgress.pct, queuedProgress.text)
        queuedProgress = null
      })
    }

    function paintProgress(pct, text) {
      if (!updateProgressWrap || !updateProgressBar || !updateProgressText) return
      if (pct == null) {
        updateProgressWrap.classList.add("hidden")
        updateProgressWrap.classList.remove("flex")
        return
      }
      updateProgressWrap.classList.remove("hidden")
      updateProgressWrap.classList.add("flex")
      if (pct < 0) {
        updateProgressBar.classList.add("indeterminate")
        updateProgressBar.style.width = "40%"
        updateProgressText.textContent = text || ""
        // Absent aria-valuenow is how ARIA signals an indeterminate progressbar.
        updateProgressTrack?.removeAttribute("aria-valuenow")
        return
      }
      updateProgressBar.classList.remove("indeterminate")
      const clamped = Math.max(0, Math.min(100, pct))
      updateProgressBar.style.width = `${clamped}%`
      updateProgressText.textContent = text || `${clamped.toFixed(0)}%`
      updateProgressTrack?.setAttribute("aria-valuenow", String(Math.round(clamped)))
    }

    function fmtUpdateBytes(n) {
      if (!n) return "0 B"
      if (n < 1024) return `${n} B`
      if (n < 1024 * 1024) return `${(n / 1024).toFixed(1)} KB`
      return `${(n / (1024 * 1024)).toFixed(1)} MB`
    }

    function setInstallLabel(text) {
      if (updateInstallLabelEl) updateInstallLabelEl.textContent = text
    }

    let updateGetTarget = GITHUB_RELEASES_URL

    async function checkViaBetaInvoke() {
      const { invoke } = await import("@tauri-apps/api/core")
      const url = await resolveUpdateFeedUrl()
      const update = await invoke("updater_check_from", { url })
      updateInstallMode = "invoke"
      if (!update) {
        pendingUpdate = null
        if (updateLatestEl) updateLatestEl.textContent = t("settings.about.upToDate")
        if (updateMsgEl) updateMsgEl.textContent = t("settings.update.upToDateMsg")
        if (updateGithubLink) {
          updateGithubLink.href = `${GITHUB_RELEASES_URL}/latest`
        }
        setUpdateState("up-to-date")
        return
      }
      pendingUpdate = update
      if (updateLatestEl) {
        updateLatestEl.textContent = update.version
          ? `v${update.version}`
          : t("settings.update.available")
      }
      if (updateNotesEl && update.notes) {
        updateNotesEl.textContent = update.notes
        updateNotesEl.classList.remove("hidden")
      }
      if (updateMsgEl) {
        updateMsgEl.textContent = update.pubDate
          ? t("settings.update.releasedOn", { date: new Date(update.pubDate).toLocaleDateString() })
          : t("settings.update.updateAvailableMsg")
      }
      if (updateGithubLink) {
        updateGithubLink.href = update.version
          ? `${GITHUB_RELEASES_URL}/tag/v${update.version}`
          : `${GITHUB_RELEASES_URL}/latest`
      }
      updateInstallBtn?.removeAttribute("hidden")
      setUpdateState("available")
    }

    async function checkViaTauriUpdater() {
      if (getUpdateChannel() === "beta") {
        await checkViaBetaInvoke()
        return
      }
      updateInstallMode = "plugin"
      const { check } = await import("@tauri-apps/plugin-updater")
      const update = await check()
      if (!update) {
        pendingUpdate = null
        if (updateLatestEl) updateLatestEl.textContent = t("settings.about.upToDate")
        if (updateMsgEl) updateMsgEl.textContent = t("settings.update.upToDateMsg")
        if (updateGithubLink) {
          updateGithubLink.href = `${GITHUB_RELEASES_URL}/latest`
        }
        setUpdateState("up-to-date")
        return
      }
      pendingUpdate = update
      if (updateLatestEl) {
        updateLatestEl.textContent = update.version
          ? `v${update.version}`
          : t("settings.update.available")
      }
      if (updateNotesEl && update.body) {
        updateNotesEl.textContent = update.body
        updateNotesEl.classList.remove("hidden")
      }
      if (updateMsgEl) {
        updateMsgEl.textContent = update.date
          ? t("settings.update.releasedOn", { date: new Date(update.date).toLocaleDateString() })
          : t("settings.update.updateAvailableMsg")
      }
      if (updateGithubLink) {
        updateGithubLink.href = update.version
          ? `${GITHUB_RELEASES_URL}/tag/v${update.version}`
          : `${GITHUB_RELEASES_URL}/latest`
      }
      updateInstallBtn?.removeAttribute("hidden")
      setUpdateState("available")
    }

    async function checkViaGithub() {
      const status = await checkForUpdate()
      if (!status) throw new Error("couldn't reach GitHub")
      if (updateGithubLink) updateGithubLink.href = status.latestUrl
      if (!status.updateAvailable) {
        if (updateLatestEl) updateLatestEl.textContent = t("settings.about.upToDate")
        if (updateMsgEl) updateMsgEl.textContent = t("settings.update.upToDateMsg")
        updateGetBtn?.setAttribute("hidden", "")
        updateDegradedEl?.setAttribute("hidden", "")
        setUpdateState("up-to-date")
        return
      }
      updateGetTarget = storeBuild
        ? MS_STORE_UPDATES_URL
        : playStoreInstall
          ? getPlayStoreUrl()
          : status.latestUrl
      if (updateLatestEl) updateLatestEl.textContent = `v${status.latest}`
      if (updateMsgEl) {
        updateMsgEl.textContent = status.publishedAt
          ? t("settings.update.releasedOn", { date: new Date(status.publishedAt).toLocaleDateString() })
          : t("settings.update.updateAvailableMsg")
      }
      if (updateNotesEl && status.notes) {
        updateNotesEl.textContent = status.notes
        updateNotesEl.classList.remove("hidden")
      }
      // No package-manager deep link to send Snap/Flatpak users to.
      if (sandboxed) updateGetBtn?.setAttribute("hidden", "")
      else updateGetBtn?.removeAttribute("hidden")
      setUpdateState("available")
    }

    async function checkForUpdates() {
      // Give the store-build detection a moment; degrades to the GitHub path if it's still stuck.
      await Promise.race([updaterConfigReady, new Promise((resolve) => setTimeout(resolve, 2000))])
      if (updateMsgEl) updateMsgEl.textContent = t("settings.update.checking")
      setUpdateState("checking")
      updateCheckBtn?.setAttribute("disabled", "")
      updateInstallBtn?.setAttribute("hidden", "")
      updateRelaunchBtn?.setAttribute("hidden", "")
      updateGetBtn?.setAttribute("hidden", "")
      updateDegradedEl?.setAttribute("hidden", "")
      setInstallLabel(t("settings.about.installUpdate"))
      setProgress(null)
      if (updateNotesEl) {
        updateNotesEl.textContent = ""
        updateNotesEl.classList.add("hidden")
      }

      if (canUseTauriUpdater) {
        try {
          await withUpdaterRetry(checkViaTauriUpdater)
          updateCheckBtn?.removeAttribute("disabled")
          return
        } catch (e) {
          log.warn("Tauri update check failed, falling back to GitHub check:", e)
          if (isTransientUpdaterError(e)) updateDegradedEl?.removeAttribute("hidden")
        }
      }

      try {
        await checkViaGithub()
      } catch (e) {
        log.error("Update check failed:", e)
        if (updateMsgEl) {
          updateMsgEl.textContent = t("settings.update.checkFailed", { error: String(e?.message || e) })
        }
        setUpdateState("error")
      } finally {
        updateCheckBtn?.removeAttribute("disabled")
      }
    }

    async function installUpdateViaInvoke() {
      updateInstallBtn?.setAttribute("disabled", "")
      updateCheckBtn?.setAttribute("disabled", "")
      let total = 0
      let received = 0
      let unlisten = null
      setProgress(0, t("settings.update.preparing"))
      try {
        const { listen } = await import("@tauri-apps/api/event")
        const { invoke } = await import("@tauri-apps/api/core")
        unlisten = await listen("xt:updater-progress", (event) => {
          const payload = event.payload
          if (payload.event === "Started") {
            total = payload.data?.contentLength || 0
            setProgress(total ? 0 : -1, total ? `0 / ${fmtUpdateBytes(total)}` : t("settings.update.starting"))
          } else if (payload.event === "Progress") {
            received += payload.data?.chunkLength || 0
            const pct = total ? (received / total) * 100 : -1
            queueProgress(
              pct,
              total
                ? `${fmtUpdateBytes(received)} / ${fmtUpdateBytes(total)}`
                : fmtUpdateBytes(received)
            )
          } else if (payload.event === "Finished") {
            setProgress(100, t("settings.update.installed"))
            if (updateMsgEl) updateMsgEl.textContent = t("settings.update.installedRestart")
            setInstallLabel(t("settings.about.installUpdate"))
            updateRelaunchBtn?.removeAttribute("hidden")
          }
        })
        await invoke("updater_install")
        // Windows exits the process here; the "Finished" event above is the terminal UI state.
      } catch (e) {
        log.error("Update install failed:", e)
        if (updateMsgEl) {
          const message = String(e?.message || e || "").replace(/^OTHER:/, "")
          updateMsgEl.textContent = t("settings.update.downloadFailed", { error: message })
        }
        setProgress(null)
        setInstallLabel(t("settings.update.reDownload"))
        updateInstallBtn?.removeAttribute("disabled")
      } finally {
        if (unlisten) unlisten()
        updateCheckBtn?.removeAttribute("disabled")
      }
    }

    async function installUpdate() {
      if (!pendingUpdate) return
      if (updateInstallMode === "invoke") {
        await installUpdateViaInvoke()
        return
      }
      updateInstallBtn?.setAttribute("disabled", "")
      updateCheckBtn?.setAttribute("disabled", "")
      let total = 0
      let received = 0
      setProgress(0, t("settings.update.preparing"))
      try {
        await pendingUpdate.downloadAndInstall((event) => {
          if (event.event === "Started") {
            total = event.data?.contentLength || 0
            setProgress(total ? 0 : -1, total ? `0 / ${fmtUpdateBytes(total)}` : t("settings.update.starting"))
          } else if (event.event === "Progress") {
            received += event.data?.chunkLength || 0
            const pct = total ? (received / total) * 100 : -1
            queueProgress(
              pct,
              total
                ? `${fmtUpdateBytes(received)} / ${fmtUpdateBytes(total)}`
                : fmtUpdateBytes(received)
            )
          } else if (event.event === "Finished") {
            setProgress(100, t("settings.update.installed"))
          }
        })
        if (updateMsgEl) {
          updateMsgEl.textContent = t("settings.update.installedRestart")
        }
        setInstallLabel(t("settings.about.installUpdate"))
        updateRelaunchBtn?.removeAttribute("hidden")
      } catch (e) {
        log.error("Update install failed:", e)
        if (updateMsgEl) {
          updateMsgEl.textContent = t("settings.update.downloadFailed", { error: String(e?.message || e) })
        }
        setProgress(null)
        setInstallLabel(t("settings.update.reDownload"))
        updateInstallBtn?.removeAttribute("disabled")
      } finally {
        updateCheckBtn?.removeAttribute("disabled")
      }
    }

    async function relaunchApp() {
      try {
        const { relaunch } = await import("@tauri-apps/plugin-process")
        await relaunch()
      } catch (e) {
        log.error("Relaunch failed:", e)
        if (updateMsgEl) {
          updateMsgEl.textContent = `Restart failed - ${String(e?.message || e)}`
        }
      }
    }

    updateCheckBtn?.addEventListener("click", checkForUpdates)
    updateInstallBtn?.addEventListener("click", installUpdate)
    updateRelaunchBtn?.addEventListener("click", relaunchApp)
    updateGetBtn?.addEventListener("click", () => {
      openExternal(updateGetTarget)
    })
    updateChannelSelectEl?.addEventListener("change", () => {
      setUpdateChannel(updateChannelSelectEl.value)
      checkForUpdates()
    })

    autoUpdateSelectEl?.addEventListener("change", () => {
      setAutoUpdateEnabled(autoUpdateSelectEl.value === "on")
    })

    // -------------------------------------------------------------------
    // Discord Rich Presence
    // -------------------------------------------------------------------
    const discordSection = document.getElementById("discord-rpc-section")
    const discordClientIdInput = /** @type {HTMLInputElement|null} */ (
      document.getElementById("discord-client-id")
    )
    const discordPlaylistsEl = document.getElementById("discord-playlists")

    if (discordSection && discordRpcSupported) {
      discordSection.classList.remove("hidden")
      // Flatpak grants the IPC socket (xdg-run/discord-ipc-0); snap doesn't.
      if (sandboxed === "snap") {
        document.getElementById("discord-controls")?.setAttribute("hidden", "")
        document.getElementById("discord-sandbox-note")?.removeAttribute("hidden")
      }
    }

    if (discordClientIdInput) {
      discordClientIdInput.value = getDiscordClientId()
      let saveTimer = null
      discordClientIdInput.addEventListener("input", () => {
        if (saveTimer) window.clearTimeout(saveTimer)
        saveTimer = window.setTimeout(() => {
          setDiscordClientId(discordClientIdInput.value)
        }, 350)
      })
      discordClientIdInput.addEventListener("blur", () => {
        if (saveTimer) {
          window.clearTimeout(saveTimer)
          saveTimer = null
        }
        setDiscordClientId(discordClientIdInput.value)
      })
    }

    async function renderDiscordPlaylists() {
      if (!discordPlaylistsEl) return
      const entries = await getEntries()
      if (!entries.length) {
        discordPlaylistsEl.innerHTML =
          '<div class="px-4 py-4 text-xs text-fg-3">No playlists yet.</div>'
        return
      }
      const frag = document.createDocumentFragment()
      for (const entry of entries) {
        const row = document.createElement("label")
        row.className =
          "flex items-center justify-between gap-3 px-4 py-3 hover:bg-surface-2 transition-colors cursor-pointer"
        const labelWrap = document.createElement("div")
        labelWrap.className = "flex flex-col gap-0.5 min-w-0"
        const title = document.createElement("span")
        title.className = "truncate text-sm font-medium text-fg"
        title.textContent = entry.title || "(untitled)"
        const sub = document.createElement("span")
        sub.className = "truncate text-2xs text-fg-3"
        sub.textContent =
          entry.type === "xtream"
            ? "Xtream"
            : entry.type === "local-m3u"
            ? "Local file"
            : "M3U source"
        labelWrap.append(title, sub)
        row.appendChild(labelWrap)

        const toggle = document.createElement("input")
        toggle.type = "checkbox"
        toggle.className = "size-5 accent-accent"
        toggle.checked = isDiscordEnabledForPlaylist(entry._id)
        toggle.setAttribute(
          "aria-label",
          `Show Discord presence for ${entry.title || "this playlist"}`
        )
        toggle.addEventListener("change", () => {
          setDiscordEnabledForPlaylist(entry._id, toggle.checked)
        })
        row.appendChild(toggle)
        frag.appendChild(row)
      }
      discordPlaylistsEl.replaceChildren(frag)
    }

    renderDiscordPlaylists()
    document.addEventListener("xt:entries-updated", renderDiscordPlaylists)
    document.addEventListener(DISCORD_RPC_EVENT, () => {
      // Keep the input in sync if the value changed elsewhere.
      if (discordClientIdInput) {
        const next = getDiscordClientId()
        if (discordClientIdInput.value !== next) discordClientIdInput.value = next
      }
    })

    // -------------------------------------------------------------------
    // TMDb metadata enrichment
    // -------------------------------------------------------------------
    const tmdbEnabledGroup = document.getElementById("settings-tmdb-enabled")
    const tmdbKeyInput = /** @type {HTMLInputElement|null} */ (
      document.getElementById("settings-tmdb-key")
    )
    const tmdbRevealBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("settings-tmdb-reveal")
    )
    const tmdbTestBtn = /** @type {HTMLButtonElement|null} */ (
      document.getElementById("settings-tmdb-test")
    )
    const tmdbMsgEl = document.getElementById("settings-tmdb-msg")

    function syncTmdbEnabled() {
      if (!tmdbEnabledGroup) return
      const enabled = getTmdbEnabled()
      for (const btn of tmdbEnabledGroup.querySelectorAll(".tmdb-enabled-btn")) {
        const isOn = btn.getAttribute("data-tmdb") === "on"
        btn.setAttribute("aria-pressed", isOn === enabled ? "true" : "false")
      }
    }
    syncTmdbEnabled()

    tmdbEnabledGroup?.addEventListener("click", (event) => {
      const btn = /** @type {HTMLElement} */ (event.target).closest(".tmdb-enabled-btn")
      if (!btn) return
      setTmdbEnabled(btn.getAttribute("data-tmdb") === "on")
      syncTmdbEnabled()
    })

    const tvdbEnabledGroup = document.getElementById("settings-tvdb-enabled")

    function syncTvdbEnabled() {
      if (!tvdbEnabledGroup) return
      const enabled = getTvdbEnabled()
      for (const btn of tvdbEnabledGroup.querySelectorAll(".tvdb-enabled-btn")) {
        const isOn = btn.getAttribute("data-tvdb") === "on"
        btn.setAttribute("aria-pressed", isOn === enabled ? "true" : "false")
      }
    }
    syncTvdbEnabled()

    tvdbEnabledGroup?.addEventListener("click", (event) => {
      const btn = /** @type {HTMLElement} */ (event.target).closest(".tvdb-enabled-btn")
      if (!btn) return
      setTvdbEnabled(btn.getAttribute("data-tvdb") === "on")
      syncTvdbEnabled()
    })

    function syncTmdbTestDisabled() {
      if (tmdbTestBtn) tmdbTestBtn.disabled = !(tmdbKeyInput?.value || "").trim()
    }

    if (tmdbKeyInput) {
      tmdbKeyInput.value = getTmdbApiKey()
      let tmdbSaveTimer = null
      tmdbKeyInput.addEventListener("input", () => {
        syncTmdbTestDisabled()
        if (tmdbSaveTimer) window.clearTimeout(tmdbSaveTimer)
        tmdbSaveTimer = window.setTimeout(() => {
          setTmdbApiKey(tmdbKeyInput.value)
        }, 350)
      })
      tmdbKeyInput.addEventListener("blur", () => {
        if (tmdbSaveTimer) {
          window.clearTimeout(tmdbSaveTimer)
          tmdbSaveTimer = null
        }
        if (tmdbKeyInput.value !== getTmdbApiKey()) setTmdbApiKey(tmdbKeyInput.value)
      })
      syncTmdbTestDisabled()
    }

    tmdbRevealBtn?.addEventListener("click", () => {
      if (!tmdbKeyInput) return
      const revealed = tmdbKeyInput.type === "text"
      tmdbKeyInput.type = revealed ? "password" : "text"
      tmdbRevealBtn.setAttribute("aria-label", t(revealed ? "settings.tmdb.reveal" : "settings.tmdb.conceal"))
      tmdbRevealBtn.querySelector('[data-tmdb-reveal-icon="show"]')?.classList.toggle("hidden", !revealed)
      tmdbRevealBtn.querySelector('[data-tmdb-reveal-icon="hide"]')?.classList.toggle("hidden", revealed)
    })

    tmdbTestBtn?.addEventListener("click", async () => {
      const key = (tmdbKeyInput?.value || getTmdbApiKey() || "").trim()
      setLoading(tmdbTestBtn, true)
      try {
        const result = await validateTmdbKey(key)
        if (result.ok) {
          flashMsg(tmdbMsgEl, t("settings.tmdb.testOk"))
          flashSuccess(tmdbTestBtn)
        } else if (result.status === 401 || result.status === 403 || result.status === 404) {
          flashMsg(tmdbMsgEl, t("settings.tmdb.testFail"))
        } else {
          flashMsg(tmdbMsgEl, t("settings.tmdb.testNetwork"))
        }
      } finally {
        setLoading(tmdbTestBtn, false)
      }
    })

    // -------------------------------------------------------------------
    // Network log: recent provider requests, dev-mode gated.
    // -------------------------------------------------------------------
    const netLogCard = /** @type {HTMLDetailsElement|null} */ (
      document.getElementById("card-net-log")
    )
    const netLogRowsEl = document.getElementById("net-log-rows")
    const netLogCountEl = document.getElementById("net-log-count")
    const netLogMsgEl = document.getElementById("net-log-msg")
    const netLogKindFilterEl = /** @type {HTMLSelectElement|null} */ (
      document.getElementById("net-log-kind-filter")
    )
    const netLogIncludeImagesEl = /** @type {HTMLInputElement|null} */ (
      document.getElementById("net-log-include-images")
    )
    const netLogClearBtn = document.getElementById("net-log-clear")
    const netLogCopyBtn = /** @type {HTMLButtonElement|null} */ (document.getElementById("net-log-copy"))

    const NET_LOG_RENDER_CAP = 100
    const NET_LOG_BADGE = "shrink-0 px-2 py-0.5 rounded-full text-2xs font-medium bg-surface-2 text-fg-3 uppercase tracking-wide"

    function netLogTone(entry) {
      if (entry.outcome === "error") return "bad"
      if (entry.status != null && entry.status >= 500) return "bad"
      if (entry.outcome === "aborted") return "warn"
      if (entry.status != null && entry.status >= 400) return "warn"
      if (entry.outcome === "ok" && entry.status != null && entry.status >= 200 && entry.status < 300) return "good"
      return "neutral"
    }

    function netLogRow(entry) {
      const row = document.createElement("div")
      row.className = "flex flex-col gap-1 px-3 py-2 text-2xs"

      const tone = netLogTone(entry)
      const toneClass =
        tone === "good" ? "text-good" : tone === "warn" ? "text-warn" : tone === "bad" ? "text-bad" : "text-fg-2"

      const metaRow = document.createElement("div")
      metaRow.className = "flex flex-wrap items-center gap-2"

      const time = document.createElement("span")
      time.className = "tabular-nums text-fg-3 shrink-0"
      time.textContent = fmtAgo(entry.startedAt)

      const method = document.createElement("span")
      method.className = "font-mono text-fg-3 shrink-0"
      method.textContent = entry.method

      // No HTTP status: show the outcome word, not just a colour, so failure isn't hue-only.
      const status = document.createElement("span")
      status.className = `tabular-nums font-medium shrink-0 ${toneClass}`
      status.textContent = entry.status != null ? String(entry.status) : entry.outcome.toUpperCase()

      const duration = document.createElement("span")
      duration.className = "tabular-nums text-fg-3 shrink-0"
      duration.textContent = `${entry.durationMs}ms`

      const kindBadge = document.createElement("span")
      kindBadge.className = NET_LOG_BADGE
      kindBadge.textContent = entry.kind

      const transportBadge = document.createElement("span")
      transportBadge.className = NET_LOG_BADGE
      transportBadge.textContent = entry.transport

      metaRow.append(time, method, status, duration, kindBadge, transportBadge)
      row.appendChild(metaRow)

      const url = document.createElement("div")
      url.className = "font-mono text-fg-2 break-all"
      url.textContent = entry.url
      row.appendChild(url)

      if (entry.error) {
        const errorLine = document.createElement("div")
        errorLine.className = "text-bad break-all"
        errorLine.textContent = entry.error
        row.appendChild(errorLine)
      }

      return row
    }

    function netLogCardVisible() {
      return !!netLogCard && !netLogCard.classList.contains("hidden") && netLogCard.open
    }

    function repaintNetLog() {
      if (!netLogRowsEl || !netLogCardVisible()) return

      const snapshot = getNetworkLog()
      const kindFilter = netLogKindFilterEl?.value || "all"
      const filtered = kindFilter === "all"
        ? snapshot.entries
        : snapshot.entries.filter((entry) => entry.kind === kindFilter)
      const newestFirst = filtered.slice().reverse().slice(0, NET_LOG_RENDER_CAP)

      netLogRowsEl.replaceChildren()
      if (!newestFirst.length) {
        const empty = document.createElement("div")
        empty.className = "px-4 py-4 text-xs text-fg-3"
        empty.textContent = t("settings.netLog.empty")
        netLogRowsEl.appendChild(empty)
      } else {
        for (const entry of newestFirst) netLogRowsEl.appendChild(netLogRow(entry))
      }

      if (netLogCountEl) {
        const base = t("settings.netLog.count", { count: snapshot.entries.length, capacity: snapshot.capacity })
        netLogCountEl.textContent = snapshot.dropped > 0
          ? `${base} ${t("settings.netLog.droppedCount", { count: snapshot.dropped })}`
          : base
      }
    }

    // NET_LOG_EVENT can fire dozens of times a second during a catalog load;
    // coalesce repaints into one per frame so it doesn't wipe text selection.
    let netLogRepaintFrame = 0
    function scheduleNetLogRepaint() {
      if (netLogRepaintFrame) return
      netLogRepaintFrame = requestAnimationFrame(() => {
        netLogRepaintFrame = 0
        repaintNetLog()
      })
    }

    if (netLogIncludeImagesEl) netLogIncludeImagesEl.checked = getNetLogIncludeImages()

    netLogCard?.addEventListener("toggle", repaintNetLog)
    netLogKindFilterEl?.addEventListener("change", repaintNetLog)
    netLogIncludeImagesEl?.addEventListener("change", () => {
      setNetLogIncludeImages(netLogIncludeImagesEl.checked)
      repaintNetLog()
    })
    netLogClearBtn?.addEventListener("click", () => {
      clearNetworkLog()
      repaintNetLog()
      flashMsg(netLogMsgEl, t("settings.netLog.cleared"))
    })
    netLogCopyBtn?.addEventListener("click", async () => {
      try {
        await writeClipboardText(JSON.stringify(getNetworkLog(), null, 2))
        toastSuccess(t("settings.netLog.copied"))
        flashSuccess(netLogCopyBtn)
      } catch (copyError) {
        log.warn("[settings] network log copy failed:", copyError)
        toast({ title: t("toast.copyError"), variant: "warn", duration: 2800 })
      }
    })
    document.addEventListener(NET_LOG_EVENT, scheduleNetLogRepaint)
    document.addEventListener(LOCALE_EVENT, repaintNetLog)
    repaintNetLog()
  </script>

  <style>
    /* Mobile chip rail wraps the nav in a scroll container so the nav
       itself can be content-width (w-max) while the wrapper is parent
       width with overflow-x:auto. On lg+ the wrapper becomes
       display:contents and the nav takes over as the sticky vertical
       rail directly inside the flex-row parent. */
    .settings-nav-scroller {
      overflow-x: auto;
      overflow-y: hidden;
      scrollbar-width: none;
      -ms-overflow-style: none;
      max-width: 100vw;
    }
    .settings-nav-scroller::-webkit-scrollbar { display: none; }

    /* Item styling: ghost text-only link with bg-tint hover/active. No
       border outlines - both the chip pill border and the rail's left bar
       are gone, since those were the visuals that read as "two navs".
       No `outline: none` - the global `:focus-visible` rule in Layout
       paints a 2px accent outline for keyboard focus. */
    .settings-nav-link {
      display: inline-flex;
      align-items: center;
      white-space: nowrap;
      flex-shrink: 0;
      color: var(--color-fg-3);
      font-size: 0.8125rem;
      font-weight: 500;
      text-decoration: none;
      padding: 0.5rem 0.875rem;
      border-radius: 0.5rem;
      min-height: 2.75rem;
      transition:
        color 150ms cubic-bezier(0.16, 1, 0.3, 1),
        background-color 150ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    @media (min-width: 64em) {
      .settings-nav-link {
        padding: 0.45rem 0.75rem;
        min-height: 0;
      }
    }
    .settings-nav-link:hover,
    .settings-nav-link:focus-visible {
      color: var(--color-fg);
      background: var(--color-surface-2);
    }
    .settings-nav-link[aria-current="true"] {
      color: var(--color-accent);
      background: var(--color-accent-soft);
    }

    /* Danger-zone entry uses the bad token so it reads as destructive. */
    .settings-nav-link[data-danger="true"] {
      color: color-mix(in oklab, var(--color-bad) 65%, var(--color-fg-3));
    }
    .settings-nav-link[data-danger="true"]:hover,
    .settings-nav-link[data-danger="true"]:focus-visible {
      color: var(--color-bad);
    }
    .settings-nav-link[data-danger="true"][aria-current="true"] {
      color: var(--color-bad);
      background: color-mix(in oklch, var(--color-bad) 10%, transparent);
    }

    .settings-stack[data-search-active] section[data-search="miss"],
    .settings-stack[data-search-active] article[data-search="miss"],
    .settings-stack[data-search-active] [data-settings-item][data-search="miss"] {
      display: none;
    }
    .settings-stack[data-search-active]
      .settings-surface:not([data-settings-item]):not(
        :has([data-settings-item]:not([data-search="miss"]))
      ) {
      display: none;
    }
    .settings-nav-link[data-search-miss],
    .settings-subnav-link[data-search-miss] {
      display: none;
    }

    /* Card-level subnav (lg+ rail only). Each section's cards are listed
       directly after its nav link in the DOM (no wrapper, so the mobile
       horizontal chip rail keeps scrolling cleanly). Visible only when
       the preceding section link is active. */
    @media (min-width: 64em) {
      .settings-subnav {
        display: none;
        margin: 0.1rem 0 0.6rem 0.6rem;
        padding-left: 0.7rem;
        border-left: 1px solid var(--color-line-soft);
        gap: 0.05rem;
      }
      .settings-nav-link[aria-current="true"] + .settings-subnav {
        display: flex;
        flex-direction: column;
      }
      .settings-subnav-link {
        position: relative;
        display: block;
        color: var(--color-fg-3);
        font-size: 0.78rem;
        line-height: 1.4;
        font-weight: 400;
        text-decoration: none;
        padding: 0.3rem 0 0.3rem 0.5rem;
        border-radius: 0.25rem;
        transition: color 120ms cubic-bezier(0.16, 1, 0.3, 1);
      }
      .settings-subnav-link:hover,
      .settings-subnav-link:focus-visible {
        color: var(--color-fg);
      }
      .settings-subnav-link[aria-current="true"] {
        color: var(--color-accent);
        font-weight: 500;
      }
      .settings-subnav-link[aria-current="true"]::before {
        content: "";
        position: absolute;
        left: calc(-0.7rem - 1px);
        top: 0.45rem;
        bottom: 0.45rem;
        width: 2px;
        background: var(--color-accent);
        border-radius: 1px;
      }
    }
  </style>

  <script>
    {
      // Mobile chip rail: convert vertical mouse-wheel input into
      // horizontal scrolling so a desktop user with a narrow window can
      // reach off-screen chips without shift+wheel. Skipped on lg+ where
      // the wrapper is display:contents and the rail is vertical.
      const scroller = document.querySelector<HTMLElement>(".settings-nav-scroller")
      if (scroller) {
        scroller.addEventListener("wheel", (event) => {
          if (window.matchMedia("(min-width: 64em)").matches) return
          if (event.deltaY === 0 || event.shiftKey) return
          // Only intercept when there's actually horizontal overflow to
          // scroll, otherwise let the page scroll vertically.
          if (scroller.scrollWidth <= scroller.clientWidth) return
          event.preventDefault()
          scroller.scrollLeft += event.deltaY
        }, { passive: false })
      }

      const sections = Array.from(
        document.querySelectorAll("section.settings-group[id]")
      ) as HTMLElement[]
      const links = Array.from(
        document.querySelectorAll(".settings-nav-link")
      )
      const subnavLinks = Array.from(
        document.querySelectorAll(".settings-subnav-link")
      ) as HTMLAnchorElement[]
      const linksById = new Map<string, HTMLAnchorElement[]>()
      for (const link of links) {
        const targetId = link.getAttribute("href")?.replace("#", "")
        if (!targetId) continue
        const bucket = linksById.get(targetId)
        if (bucket) bucket.push(link as HTMLAnchorElement)
        else linksById.set(targetId, [link as HTMLAnchorElement])
      }
      // Card subnav: bucket sub-links by their target card id and keep a
      // map from section id to the cards it contains (in DOM order).
      const subLinksByCardId = new Map<string, HTMLAnchorElement[]>()
      const cardsBySectionId = new Map<string, HTMLElement[]>()
      for (const link of subnavLinks) {
        const cardId = link.getAttribute("href")?.replace("#", "")
        if (!cardId) continue
        const bucket = subLinksByCardId.get(cardId)
        if (bucket) bucket.push(link)
        else subLinksByCardId.set(cardId, [link])
        const cardEl = document.getElementById(cardId)
        if (!cardEl) continue
        const ownerSection = cardEl.closest<HTMLElement>("section.settings-group[id]")
        if (!ownerSection) continue
        const cards = cardsBySectionId.get(ownerSection.id)
        if (cards) {
          if (!cards.includes(cardEl)) cards.push(cardEl)
        } else {
          cardsBySectionId.set(ownerSection.id, [cardEl])
        }
      }

      // Platform-gated cards (Android native/default player, desktop-only
      // Discord) are conditionally rendered or hidden, so their subnav links
      // start hidden; reveal each only while its target card is actually shown.
      const conditionalItems = Array.from(
        document.querySelectorAll<HTMLLIElement>("li[data-conditional]")
      )
      const syncConditionalNav = () => {
        for (const item of conditionalItems) {
          const cardId = item.querySelector("a")?.getAttribute("href")?.replace("#", "")
          const target = cardId ? document.getElementById(cardId) : null
          item.hidden = !target || target.offsetParent === null
        }
      }
      syncConditionalNav()
      if (conditionalItems.length) {
        const settingsRoot = document.querySelector(".settings-stack") ?? document.body
        let syncQueued = false
        const observer = new MutationObserver(() => {
          if (syncQueued) return
          syncQueued = true
          requestAnimationFrame(() => {
            syncQueued = false
            syncConditionalNav()
          })
        })
        observer.observe(settingsRoot, {
          childList: true,
          subtree: true,
          attributes: true,
          attributeFilter: ["class", "hidden", "style"],
        })
      }

      const scrollEl =
        (document.querySelector("main.flex-1.min-h-0.overflow-y-auto") as HTMLElement | null) ??
        document.scrollingElement ?? document.documentElement

      let scrollLock = false
      let currentId = ""
      let currentCardId = ""

      const setActiveCard = (cardId: string) => {
        if (cardId === currentCardId) return
        currentCardId = cardId
        for (const [otherId, bucket] of subLinksByCardId) {
          const isActive = otherId === cardId
          for (const link of bucket) {
            if (isActive) link.setAttribute("aria-current", "true")
            else link.removeAttribute("aria-current")
          }
        }
      }

      const setActive = (id: string) => {
        if (id === currentId) return
        currentId = id
        for (const [otherId, bucket] of linksById) {
          const isActive = otherId === id
          for (const link of bucket) {
            if (isActive) link.setAttribute("aria-current", "true")
            else link.removeAttribute("aria-current")
          }
        }
      }
      const writeHash = (id: string) => {
        try { history.replaceState(null, "", `#${id}`) } catch {}
      }

      const pickActiveCardWithin = (sectionId: string): string => {
        const cards = cardsBySectionId.get(sectionId)
        if (!cards || cards.length === 0) return ""
        const el = scrollEl as HTMLElement
        const clientHeight = el.clientHeight
        const containerIsRoot =
          el === document.scrollingElement || el === document.documentElement
        const containerRect = containerIsRoot
          ? { top: 0, bottom: clientHeight }
          : el.getBoundingClientRect()
        // Same 22%-from-top anchor as the section spy.
        const anchorY = containerRect.top + clientHeight * 0.22
        let best = cards[0]
        let bestDistance = Infinity
        for (const card of cards) {
          const rect = card.getBoundingClientRect()
          if (rect.bottom < containerRect.top) {
            best = card
            continue
          }
          if (rect.top > containerRect.bottom) break
          const distance = Math.abs(rect.top - anchorY)
          if (distance < bestDistance) {
            bestDistance = distance
            best = card
          }
        }
        return best.id
      }

      const pickActive = (): string => {
        if (sections.length === 0) return ""
        const el = scrollEl as HTMLElement
        const scrollTop = el.scrollTop
        const clientHeight = el.clientHeight
        const scrollHeight = el.scrollHeight
        if (scrollTop + clientHeight >= scrollHeight - 4) {
          return sections[sections.length - 1].id
        }
        // Use viewport-relative rects: section.offsetTop is measured from the
        // section's offsetParent, which is not always scrollEl - on Android
        // WebView in particular the offsetParent chain differs, so offsetTop
        // doesn't line up with scrollTop and the active id sticks.
        const containerIsRoot =
          el === document.scrollingElement || el === document.documentElement
        const containerRect = containerIsRoot
          ? { top: 0, bottom: clientHeight }
          : el.getBoundingClientRect()
        const anchorY = containerRect.top + clientHeight * 0.22
        let best = sections[0]
        let bestDistance = Infinity
        for (const section of sections) {
          const rect = section.getBoundingClientRect()
          if (rect.width === 0 && rect.height === 0) continue
          if (rect.bottom < containerRect.top) continue
          if (rect.top > containerRect.bottom) break
          const distance = Math.abs(rect.top - anchorY)
          if (distance < bestDistance) {
            bestDistance = distance
            best = section
          }
        }
        return best.id
      }

      let rafScheduled = 0
      const onScroll = () => {
        if (scrollLock) return
        if (rafScheduled) return
        rafScheduled = requestAnimationFrame(() => {
          rafScheduled = 0
          const nextSection = pickActive()
          setActive(nextSection)
          setActiveCard(pickActiveCardWithin(nextSection))
        })
      }
      scrollEl.addEventListener("scroll", onScroll, { passive: true })

      const armScrollUnlock = () => {
        const QUIET_MS = 200
        const SAFETY_MS = 2500
        let released = false
        let quietTimer = 0
        const onSettleScroll = () => arm()
        const release = () => {
          if (released) return
          released = true
          if (quietTimer) clearTimeout(quietTimer)
          scrollEl.removeEventListener("scroll", onSettleScroll)
          scrollLock = false
        }
        const arm = () => {
          if (released) return
          if (quietTimer) clearTimeout(quietTimer)
          quietTimer = window.setTimeout(release, QUIET_MS)
        }
        scrollEl.addEventListener("scroll", onSettleScroll, { passive: true })
        arm()
        window.setTimeout(release, SAFETY_MS)
      }

      // Async content above a deep-link target (playlist rows, hydrating
      // islands, unhidden cards) shifts layout after the first scroll, so
      // keep re-anchoring until the page height stops changing.
      const anchorWhileSettling = (target: HTMLElement) => {
        target.scrollIntoView({ block: "start" })
        if (typeof ResizeObserver === "undefined") return
        const contentEl = scrollEl.firstElementChild ?? scrollEl
        const QUIET_MS = 250
        const SAFETY_MS = 2500
        let released = false
        let quietTimer = 0
        const release = () => {
          if (released) return
          released = true
          if (quietTimer) clearTimeout(quietTimer)
          observer.disconnect()
          scrollEl.removeEventListener("wheel", release)
          scrollEl.removeEventListener("touchstart", release)
          scrollEl.removeEventListener("pointerdown", release)
          window.removeEventListener("keydown", release)
          scrollLock = false
        }
        const observer = new ResizeObserver(() => {
          if (released) return
          target.scrollIntoView({ block: "start" })
          if (quietTimer) clearTimeout(quietTimer)
          quietTimer = window.setTimeout(release, QUIET_MS)
        })
        scrollLock = true
        observer.observe(contentEl)
        scrollEl.addEventListener("wheel", release, { passive: true })
        scrollEl.addEventListener("touchstart", release, { passive: true })
        scrollEl.addEventListener("pointerdown", release, { passive: true })
        window.addEventListener("keydown", release)
        quietTimer = window.setTimeout(release, QUIET_MS)
        window.setTimeout(release, SAFETY_MS)
      }

      // D-pad activation leaves focus in the nav rail otherwise, so the next
      // press yanks the page back before the user can act on the section.
      function focusScrolledTarget(target: HTMLElement, event: Event) {
        const detail = (event as MouseEvent).detail ?? 0
        if (detail !== 0 && document.documentElement.dataset.tv !== "1") return
        const focusable = target.querySelector<HTMLElement>(
          "a, button, input, select, textarea, [tabindex]:not([tabindex='-1'])"
        )
        if (focusable) {
          focusable.focus({ preventScroll: true })
          return
        }
        target.setAttribute("tabindex", "-1")
        target.focus({ preventScroll: true })
      }

      for (const link of links) {
        link.addEventListener("click", (event) => {
          const targetId = link.getAttribute("href")?.replace("#", "")
          if (!targetId) return
          const target = document.getElementById(targetId)
          if (!target) return
          event.preventDefault()
          scrollLock = true
          setActive(targetId)
          // First card in this section starts as active on a section jump
          // so the subnav doesn't lag a frame behind the scroll.
          const firstCard = cardsBySectionId.get(targetId)?.[0]
          setActiveCard(firstCard?.id ?? "")
          writeHash(targetId)
          target.scrollIntoView({ behavior: "smooth", block: "start" })
          focusScrolledTarget(target, event)
          armScrollUnlock()
        })
      }

      // Card subnav links: scroll to the card, set both section + card
      // active immediately so the rail and the highlight pop without
      // waiting for the scroll-spy to catch up. If the target card is
      // a <details>, expand it so the user lands on its content rather
      // than its collapsed summary.
      for (const link of subnavLinks) {
        link.addEventListener("click", (event) => {
          const cardId = link.getAttribute("href")?.replace("#", "")
          if (!cardId) return
          const cardEl = document.getElementById(cardId)
          if (!cardEl) return
          event.preventDefault()
          scrollLock = true
          const ownerSection = cardEl.closest<HTMLElement>("section.settings-group[id]")
          if (ownerSection) setActive(ownerSection.id)
          setActiveCard(cardId)
          if (cardEl instanceof HTMLDetailsElement) cardEl.open = true
          writeHash(cardId)
          cardEl.scrollIntoView({ behavior: "smooth", block: "start" })
          focusScrolledTarget(cardEl, event)
          armScrollUnlock()
        })
      }

      // Deep-link from URL hash, else pick from current scroll position.
      // Hash may target a section (`#settings-watching`) or a card
      // (`#card-progress-retention`). Handle both.
      const hadInitialHash = !!location.hash
      const initialHashId = location.hash?.slice(1) || ""
      if (initialHashId && linksById.has(initialHashId)) {
        setActive(initialHashId)
        setActiveCard(pickActiveCardWithin(initialHashId))
        if (hadInitialHash) {
          requestAnimationFrame(() => {
            const target = document.getElementById(initialHashId)
            if (target) anchorWhileSettling(target)
          })
        }
      } else if (initialHashId && subLinksByCardId.has(initialHashId)) {
        const cardEl = document.getElementById(initialHashId)
        const ownerSection = cardEl?.closest<HTMLElement>("section.settings-group[id]")
        if (ownerSection) setActive(ownerSection.id)
        setActiveCard(initialHashId)
        if (cardEl instanceof HTMLDetailsElement) cardEl.open = true
        requestAnimationFrame(() => {
          if (cardEl) anchorWhileSettling(cardEl)
        })
      } else {
        const initialSection = pickActive()
        setActive(initialSection)
        setActiveCard(pickActiveCardWithin(initialSection))
      }
    }
  </script>

  <script>
    import "@/scripts/settings/settings_effects.ts"
  </script>

  <script>
    import "@/scripts/settings/settings_search.ts"
  </script>

  <script>
    import "@/scripts/settings/receiver-card.ts"
  </script>

  <script>
    import "@/scripts/settings/play-on-tv-card.ts"
  </script>
</Layout>
